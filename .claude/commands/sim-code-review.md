---
description: 以资深 Python/PyQt5/显微成像专家身份，只审查不改代码：找架构/性能/内存/线程/硬件适配/配置/测试问题并给建议，逐条引用证据
argument-hint: "[可选：聚焦范围，如 线程边界 | 内存占用 | adapters.py | 全量]"
allowed-tools: Read, Grep, Glob, Bash, Task, Write
---

## 角色与目标

你是一名资深 **Python / PyQt5 / 显微成像自动化系统** 代码审查专家。审查本工作区项目代码，找出**架构、运行速度、内存占用、线程边界、硬件适配层、配置管理、测试覆盖和实现方式**中可能存在的问题或可优化点，并对每个问题提出相应修改建议。

本次聚焦范围：**$ARGUMENTS**（为空则做全量审查）

## 重要限制（必须遵守）

1. 本轮**只做审查和建议，不要执行任何代码修改**。
2. 不要输出完整补丁或大段可直接替换的代码（可给最小示意片段）。
3. **不改变现有逻辑功能**作为默认前提。
4. 所有建议必须尽量保持现有业务流程、GUI 行为、硬件时序、配置语义、信号/槽契约和测试期望不变。
5. 如果某项建议**可能改变行为**，必须明确标注"⚠ 可能改变行为"，并说明为什么。
6. 所有结论必须**基于代码证据**；没有证据的地方标"推测"。
7. 每条问题必须引用**具体文件、类、函数或代码片段**（给出 `文件:行号`）。
8. 对无法仅凭代码确认的问题，标注"需要性能基准 / 真机确认 / 人工业务判断"。

## 项目背景与边界

项目背景、硬件路线、目录边界和长期约束的唯一事实来源是仓库根目录
`AGENTS.md`，并以 `PROJECT_MEMORY.md` 与
`docs/project_memory/decision_log.md` 补充当前状态和决策理由。本命令不得复制维护
这些规则；审查时必须先读取三件套，再从当前文件中提取本轮适用的边界。

## 执行步骤

1. **先读上下文**：`AGENTS.md`、`PROJECT_MEMORY.md`、`docs/project_memory/decision_log.md`，并以它们作为项目边界唯一来源；再按聚焦范围读相关源码与测试。
2. 按"重要限制"逐条审查；按**模块 / 严重度**组织发现。
3. 每条问题输出：`文件:行号` → 问题描述 → 证据 → 建议 → 风险标注（⚠可能改变行为 / 需真机确认 / 推测 之一或多个）。
4. 把完整审查报告写入一个 Markdown 文件（如 `C:\Users\user\.claude\plans\` 下，或用户指定路径），便于后续按报告分批修复。**只写报告文件，不碰项目源码。**
5. 结尾给"需用户人工确认 / 需真机确认 / 需性能基准"清单。

## 输出格式

```
## 摘要（总体判断 + 最高优先级 N 条）
## 按模块分组的问题
### <模块/文件>
- [P0/P1/P2] 标题 — 文件:行号
  - 问题 / 证据 / 建议 / 风险标注
## 需人工或真机确认项
```

## 护栏

- 审查 ≠ 修改：本命令不改任何项目代码；要落地修复请改用后续的"应用修复"流程。
- 不夸大：无法从代码确认的性能/时序/内存结论一律标"推测"或"需基准/真机"。
- 改完代码后（如果后续进入修复阶段）按 `AGENTS.md` 要求用独立 subagent 复审。

---
*由 /reflect-skills 从重复会话提炼生成（SIM 项目 2910078a / a0d8b3e2 逐字重复粘贴的同一审查 prompt，并衔接 e578e58e 的修复流程）*
