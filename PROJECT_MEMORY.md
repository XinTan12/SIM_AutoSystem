# SIM_AutoSystem 项目记忆

## 使用说明
- 新对话开始时，必须优先读取本文件、`AGENTS.md` 和 `docs/project_memory/decision_log.md`，作为项目上下文的固定启动步骤。
- 完成三件套读取后，如果任务涉及具体模块、配置或故障，再补充读取直接相关的代码、配置、测试和必要设计文档。
- 本文件记录当前有效的项目事实、约束、风险、待办和最近状态，不保存完整聊天记录。
- 如果本文件与代码、配置或更具体文档冲突，以更新且更具体的事实为准，并在本次会话结束前修正本文件。
- 只要本次会话对项目产生修改，结束前必须同步更新本文件；至少更新“最近更新”部分。
- 后续“最近更新”只使用中文；不要新增英文最近更新章节。

## 当前状态说明
- 当前内容以仓库内 `AGENTS.md`、当前代码、配置和最近实现结果为准。
- 2026-05-07 起，项目计划回到 NI USB-6423 单 DAQ 路线；不再开发、维护或接入 NI PXIe-7857R / NI-RIO 控制链路。
- 当前重点是让 SIM GUI、采集控制、波形生成、硬件适配和 SIM9 GPU Wiener 重建接口在无真实硬件时可通过仿真/配置校验路径运行，并逐步接入真实 Hamamatsu 相机、Kopin/FDD SLM、NI USB-6423 与 GPU 重建环境。
- 当前 Git 工作流固定为：`main` / `origin/main` 保存稳定主线，`dev` / `origin/dev` 保存当前大改动开发线，`E:\Intelligent_SR\SIM_AutoSystem-stable` 作为 detached `origin/main` 的稳定版运行 worktree。

## 项目目标
- 构建面向微流控捕获与 SIM 成像联动的自动化系统。
- 近期集成目标链路：
  `control_wangbo` 微流控捕获 -> `sim_control` 执行 9 帧 SIM 采集 -> 输出 `(9, H, W)` `numpy.uint16` 图像栈 -> 重建与特征分析 -> 将决策结果回传给微流控流程执行 `release/sort`。

## 目录职责
- `sim_control/`：当前 SIM 侧主开发区，包含 GUI、采集控制、波形生成、预览、适配器、配置读写、数据模型和占位 pipeline。
- `control_wangbo/`：队友历史微流控控制代码，默认只读；仅在用户明确要求或 SIM 集成入口必须调整时修改。
- `app.py`：项目集成 GUI 顶层启动入口，启动 `control_wangbo/main.py` 的主界面。
- `sim_control/sim_acquisition_app.py`：独立 SIM 采集 GUI 启动入口，用于单独调试 SIM 采集链路。
- `config/sim_control_config.json`：默认 SIM 配置文件，承载设备、时序、后端、SDK 路径和 DAQ 线位配置。
- `start.cmd`：Windows 下优先使用的项目集成 GUI 启动脚本。
- `SDK/`：本地厂商 SDK、驱动和资料目录；仓库只跟踪 `SDK/README.md` 的目录约定。
- `tests/`：回归测试、适配器测试和 UI 行为测试。

## 当前硬件与集成状态
- 相机方案：Hamamatsu ORCA-Fusion BT，通过 `DCAM-API` / `DCAM-SDK4` 控制。
- SLM 方案：Kopin / Forth Dimension Displays `QXGA-R11-STR`，通过 `R11CommLib` over WinUSB 控制。
- DAQ 方案：NI USB-6423，负责输出 `slm_enable_line`、`slm_trigger_line`、`slm_finish_line`、`camera_trigger_line` 和激光触发线的同步 TTL。
- SIM9 采集只使用 USB-6423 波形输出路径；PXIe-7857R / NI-RIO 相关 Python Host、bitfile 诊断、LabVIEW rebuild 工具和配置入口已从当前主流程移除。
- 当前 NI USB-6423 规范接线固定为：`slm_enable=port0/line0`、`slm_trigger=port0/line1`、`slm_finish=port0/line2`、`camera_trigger=port0/line8`、`laser_405=port0/line9`、`laser_488=port0/line10`、`laser_561=port0/line11`、`laser_red=port0/line12`（第四路红光 DAQ 角色键为中性 `laser_red_line`，物理 line 12 与波长数字解耦）。
- SIM 侧第四路红光波长**按机器可配置**（638 或 647，2026-06-27 决策）：两台系统前三档相同、红光不同，由 `AppConfig.red_laser_nm` 表达机器身份（默认 638），主界面 `cmb_main_red_laser` 下拉可运行时切换。638 与 647 在 `LASER_ROLE_MAP` 都映射到 `laser_red_line`；`SUPPORTED_LASERS=(405,488,561,638)` 仅默认四档，按机器红光取四档用 `supported_lasers_for(red_laser_nm)`。旧 `640` 错误命名归一到 638；旧 `647` 经 schema v13 迁移**保留为 647 机器身份**（不再压成 638）。
- SLM 正式采集路径使用预烧录 Running Order：主界面连接 SLM 后，按当前波长与相机曝光自动选择 `3.5/2d`、非 `_ang0` RO；红光 638↔647 互为等价（`RED_EQUIVALENT_WAVELENGTHS`），`find_best_running_order` 优先精确匹配本机红光波长命名的 RO、缺失时双向 fallback 到另一红光命名（warning 动态生成）；RO 模式以 `PatternPreparationResult(handles=[-1], metadata["mode"]="running_order")` 表示。
- 当前 `.repz11` RO 的 1ms/10ms/50ms 循环能力由 repertoire 内 `[HWA h]` 与 FINISH-controlled loop 定义提供；不要启用占用 SPI_1/SPI_2 的 RO Selection 替代模式。
- R11 官方时序事实（PD0011CA Table 7-1 p.26 / AN0027AD §3.26 p.33 / sequence timing report PDF）：`[HWA h]` RO 只在 EXT_RUN（即 `slm_enable_line`，SPI_0）拉高后才真正激活，硬件激活时间 `tHWAT = 5~500 µs`；TRIGGER→sequence 开始 `tTSS ≈ 3.5 µs`；1-bit Lit Pair 序列 trigger 后 `illumStart ≈ 270.19 µs` 图案才点亮（48030/48037 timing report 值）；软件 `R11_RpcRoActivate` 后 EXT_RUN 未拉高时激活状态为 `0x54 MHW`（仍在 Maintenance，显示黑屏）。`R11_RpcRoGetActivationState` 已绑定为 `KopinSlmAdapter.get_running_order_activation_state()`；R11CommLib 没有逐帧时间戳/触发计数查询，逐帧真实时序只能靠 sequence timing report、READY/SPO_0 输出 + 示波器或相机 DCAM 时间戳。
- `timing.slm_enable_guard_us` 默认与推荐值为 `1000 µs`（tHWAT 上限的 2 倍）；配置 schema v9 迁移会把 <1000 的旧值抬到 1000；guard ≤500 µs 时 waveform builder 在 `build()`/`build_z_scan()` 都会发 tHWAT 告警。2026-05-27 DAQ 页 SIM 采集测试第一帧黑帧 + 图案 3+3+2 错位的根因即旧 guard=50 µs 丢失首个 trigger。DAQ 页测试目标新增 `SLM激活时序`：DAQ 拉高 `slm_enable` 后轮询激活状态，实测 enable→ACT 延迟上界（USB 轮询毫秒级粒度）。
- SIM9 相机帧间 gap 自 2026-08-25 起由当前 ROI/曝光/位深/触发配置下的 DCAM 原始 `TIMING_READOUTTIME`、`TIMING_CYCLICTRIGGERPERIOD`、`TIMING_MINTRIGGERBLANKING` 驱动：`READOUTTIME` 只诊断、不相加；理论整数微秒下界按 `Tb>=MinTB` 与 `Tx<Tc` 时严格 `Tb>Tc-Tx` 计算，再固定加 `500 µs`。三项任一缺失/非法回退 `50_000 µs` 并告警；合法推荐值即使超过 50 ms 也不得截短。runtime timing 必须与设备、ROI、曝光、位深、触发模式完整签名匹配，preview 的固定 1 ms timing 不得污染正式采集。
- Z-Scan 正式采集采用三态合同：`Enable Z-Scan` 关闭时只在当前位置采一组普通 SIM9；开启且 `Select Focus Plane` 关闭时，仅手动 raw「SIM9帧采集」从当前 Z 起逐层采完整 SIM9 Z-stack，MCU/重建入口在任何硬件动作前明确阻止；两项都开启时，手动与 MCU 均逐层采三相位积分图、以 SML 离散最大值选焦，再在最佳层采一组普通 SIM9。`Layers` 是总层数，内部兼容字段仍为 `num_steps=layers-1`。主 GUI 的 Z-Scan 模块长期保留 legacy 四列横排、150 px 高度和既有控件宽度；功能迭代不得自行改成 2×2 或其他排布，除非用户再次明确要求视觉改版。
- Z-Scan 三相位 RO 已覆盖 405/488/561/红光：图案分别为 `0–2`、`9–11`、`18–20`、`27–29`，命名 `{wavelength}_3.5_2d_zscan3p_{5,8,14,20}ms`；repertoire 保留现有 488 并新增 405、561、647，638 机器按 638↔647 exact-first/fallback 规则复用 647 RO。四档 sequence 固定复用 `A±/F±/G±/D±`，实际曝光为 `4884/7884/13884/19884 µs`；不新增 sequence alias、不引入 48088、每次只播放一遍三相位且不使用 FINISH。
- Z-Scan TTL 波形沿用 USB-6423 同步路径：`slm_enable_line` 先拉高并等待 guard，`slm_trigger_line` 只发一次，`camera_trigger_line` 与当前波长对应激光线维持实际曝光窗，`slm_finish_line` 始终为低。波形在首次移动前构建；失败、取消与成功清理都要求相机 disarm、DAQ 全低，并恢复正式相机曝光与正式 SIM9 RO。
- Z-stack 保持单平面 `AcquisitionBatch(9,H,W)` 合同不变，另用轻量 `ZStackAcquisitionResult` 返回请求/实测 Z、完成层数、单层形状、状态与输出路径。`ZStackAsyncWriter` 通过有界队列异步接收完整 9 帧层块，按 `Z0/F0…F8 → Z1/F0…F8` 合并单 TIFF，超过阈值自动 BigTIFF；取消/失败只合并已提交层并用 `.partial-zXofN.tif` 命名。选焦诊断另存逐层 `zfocus` TIFF 与 SML CSV；诊断写盘失败只告警并保留可用 partial，不丢已经成功采得的最终 SIM9。
- 本机已确认 Nikon Ti2 SDK DLL 默认路径 `C:\Program Files\Nikon\Ti2-SDK\bin\Ti2_Mic_Driver.dll` 可由 64-bit Python 加载；关闭卡住的 Nikon SDK helper 后，`MIC_GetDeviceList()` 返回 `[11]`，`MIC_Open(0)` 成功，ZDrive 物理/逻辑范围均为 `0..10000 μm`，当前读数约 `2674.74 μm`；围绕当前 Z 做 `±0.3 μm` 往返 12 次全部 `retcode=0`，移动调用耗时约 `21.3..23.6 ms`，读回误差为 `0.0 μm`。
- SIM9 GPU Wiener 重建的稳定集成入口已由 `reconstruction.sim_wiener` **迁移到 `sim_control/sim_reconstruction.py`**（决策见 decision_log 2026-06-16）。该模块是项目内唯一依赖重建引擎私有方法的地方，只 `import` 引擎模块 `sim_wiener_gpu_emdapp_batchInGroup_batchBetGroup`，**绝不 import** `reconstruction/sim_wiener.py`（队友新版里它是 import 即执行整段重建、含硬编码 `F:\` 路径的计时 demo，不可当库）。`reconstruction/` 下 4 个 .py 是队友更新过的 GPU 重建算法（引擎已自带 `use_saved_params`/`estimated_params_path` 与 `_load_estimated_params_file`/`_wiener_reconstruct_with_saved_params`）；除 638 nm 默认 OTF 与红光参数兼容所需的最小更新外，引擎主逻辑仍由 `sim_control/` 集成层隔离。
- 核心机制：`WarmSIMReconstructor` 进程内常驻、跨细胞复用同一引擎实例，使 cuFFT plan / caching-allocator / 坐标网格缓存保留，从第二个细胞起进入热速度（启动后第一个细胞仍冷；自动按首帧形状预热）。`ReconstructionWorker` 持有该热实例；输入 `(9, H, W)` `numpy.uint16`，输出 dict 中 `reconstruction` 为 `float32`，pipeline 取首张 2D 图作为 `ReconstructionResult.preview_image`。延迟导入后端，GUI 启动不因缺 `torch/scipy` 崩溃。重建本身失败走 `signal_reconstruction_failed`。
- 参数双路（可切换，决定热速度）：estimate-per-cell（默认，~287ms 热，无需标定）与 saved-params（~45ms 热，需每波长标定 `.mat` + 固定 SLM RO/光路）。`saved_params_fallback`：`fail`（生产默认，缺/坏 `.mat` 直接报错并由配置校验阻止采集）或 `estimate`（回退每帧估计，记 `fallback_used`）。仓库默认 `use_saved_params=false`（仿真优先，可无 `.mat` 启动）；现场生产档见 `config/sim_control_config.production.example.json` 与 `docs/reconstruction_saved_params.md`。
- `config/sim_control_config.json` 当前 schema 为 v14（`models.AppConfig.config_version` 与 `config_store.CURRENT_CONFIG_VERSION` 均为 14）。v14 新增持久化 `z_scan.select_focus_plane`，v13→v14 默认设为 `true` 以保持旧版 Enable 后自动选焦语义，并把旧绝对 `start_um` 归一为 `null`；所有 GUI/正式采集任务仍会在执行时强制从平台当前 Z 起扫。v13 的双机红光档案、`laser_red_line`、638/647 OTF/参数字段与迁移规则继续有效。Recon 已迁入主 GUI 一级模块；重建结果仍先 emit、再由有界 writer 异步保存，当前尚未自动回传 `release/sort`。
- Z-Scan 测试耗时会追加写入 `data/z_scan_timing_history.jsonl`（运行数据不入 Git），历史文件兼容旧 per-step 与 `record_type="run"` 汇总。`Test Z-Scan` 在 Select OFF 时只遍历位移并停最后层，Select ON 时执行三相位 SML 扫描并停最佳层，均不追加正式 SIM9；取消停当前安全位置。正式 Z-stack 成功停最后层，正式自动选焦成功停最佳层；`z_scan.enabled=False` 时完全不调用 Z-stage。
- SIM9 正式采集完成后，raw `AcquisitionBatch.stack` 只通过 `signal_acquisition_ready(AcquisitionBatch)` 传给重建/后续分析 worker；GUI 状态更新改走 `signal_acquisition_summary_ready(dict)`，payload 只包含 `task_id`、`stack_shape`、`stack_dtype` 和 `metadata`。

## 环境与运行约定
- 当前工作目录根以本地实际工作区为准；本次实际工作区为 `F:\SIM_AutoSystem`。历史 GPU 真机环境记录可能来自其他 worktree/机器，运行重建前仍以当前 `.venv` 的实际依赖与 CUDA 探测结果为准。
- SIM 侧统一使用根目录 `.venv` 作为项目环境。
- 不依赖 `control_wangbo/.venv`，该旧环境不属于当前主流程。
- 默认配置路径为 `config/sim_control_config.json`。
- 启动项目集成 GUI：`.\.venv\Scripts\python.exe app.py`。
- Windows 启动脚本：`.\start.cmd`。
- 启动独立 SIM 采集 GUI：`.\.venv\Scripts\python.exe -m sim_control.sim_acquisition_app --config config\sim_control_config.json`。
- 基准测试命令：`.\.venv\Scripts\python.exe -m unittest discover -s tests -q`。
- pytest 可用时运行：`.\.venv\Scripts\python.exe -m pytest tests\ -q`。

## 当前关键约束
- 默认不修改 `control_wangbo/`；新 SIM 侧开发优先放在 `sim_control/` 或新的顶层模块。
- 日常开发默认在 `dev` 分支进行；`main` 只接收通过测试、review 和必要真机验收后的合并。
- stable worktree 只用于运行远程稳定版和真机对照，不在其中开发或提交。
- 配置驱动优先；设备路径、TTL 线位、SDK 定位和后端选择不要硬编码。
- 实时采集路径避免同步磁盘 I/O。
- 硬件适配逻辑尽量集中在 `sim_control/adapters.py`。
- 不再新增或恢复 `daq_backend`、`pxie7857r`、`rio_lines`、`nifpga`、`NIRioDaqAdapter`、NI-RIO bitfile preflight 或 PXIe-7857R LabVIEW rebuild 相关入口。
- 正式 SIM 采集要求 SLM 已连接并已能选择匹配 RO；未连接时应阻止采集，不回退到空 `pattern_files`。
- SIM 正式采集优先使用当前完整相机配置对应的 `recommended_inter_frame_gap_us`，不设 50 ms 上限；只有原始 timing 缺失/非法、推荐值异常或配置签名不匹配时才回退 `50_000 us`。
- `SimSettingsDialog` 不拥有 SLM 生命周期；集成主界面与 SIM controller 必须共享同一个 `slm_adapter`，避免 R11 WinUSB 设备被重复打开。
- SIM live 预览采用 `latest-frame-wins`：采集线程持续更新最新帧快照，GUI 端轮询显示最新帧，允许丢弃中间帧以避免旧帧积压。
- 正式 SIM9 采集的 raw stack 不进入 GUI 状态槽；GUI 不应暂存 `AcquisitionBatch`、取 `stack[0]` 做状态预览，或为了状态显示对 stack 执行 `copy()`、`tolist()`、压缩、统计或写盘。
- `ui_sim_settings_dialog.py` 由 `pyuic5` 从 `.ui` 生成；修改 UI 时编辑 `.ui` 后重新生成，不直接手改生成文件。
- 每次对项目进行了修改后，必须使用独立 subagent 对本次改动进行 review 审查；审查需明确列出已满足的标准和证据、未满足的标准和原因，以及需要用户人工确认、真机确认或额外业务判断的事项。若当前工具环境无法启动独立 subagent，主 agent 必须先向用户说明原因并请求确认，不得静默自审替代。

## 当前工作重点
- 保持 SIM GUI、采集控制与适配器在无真实硬件时仍可通过仿真模式运行。
- 将真实相机与 SLM SDK 逐步接入现有适配器层。
- 打通 9 帧采集到 `(9, H, W)` `numpy.uint16` 输出栈的稳定接口。
- 基于已接入的 SIM9 GPU Wiener 重建结果继续完善特征分析与最终 `release/sort` 决策回传接口。
- 保持 SIM live 预览为低延迟 `latest-frame-wins` 模式。

## 已知风险与待确认事项
- **488 SIM 采集存在 dir0（≈87°）方向静态光栅**（贯穿 9 帧、off 帧相位锁定 coh_off≈0.998、幅值≈照明帧 24%，多个 512/2048 文件、多曝光系统复现）：会污染 dir0 方向 SIM 重建（固定伪影/分辨率不均）。来源待真机区分（SLM dir0 图案残留/未清屏 vs 固定光学条纹），排查步骤见 `data/sim_9frames/cross_dir_fft_check/report.md`（同光路采 SLM-blank 背景 / widefield 平场，FFT 看 r≈160 dir0 位是否仍有峰；检查 dir0 RO 清屏/FINISH-loop 时序）。复测前不要把该采集判为"健康/零串扰"。判别 SIM 静态跨方向串扰须用 off 帧载波相位 coherence，勿用残差谱对角（循环论证）。
- 厂商 SDK 的真实 DLL 名称、导出函数和调用签名仍需结合本机安装内容最终核对。
- NI 触发时序、相机触发、SLM 完成信号和激光线之间的同步细节仍需真实硬件联调验证。
- GPU Wiener 重建接口已接入并改为进程内常驻热重建（`sim_control/sim_reconstruction.py`）；`E:\Intelligent_SR\SIM_AutoSystem` 的 `.venv` 已验证 `torch 2.12.0+cu130`、`scipy 1.17.1`、`cuda.is_available()=True`、RTX A4000（当前 NanKai 开发工作区 .venv 未装 torch，本仓库改动仅以 fake-backend 单测验证，未做真机 GPU 验证）。真实运行仍需：配置各波长 OTF/background；若用 saved-params 快路径还需每波长标定 `.mat`；并在真机确认热/冷速度（~45ms saved / ~287ms estimate 为参考值）、全幅显存占用、saved-params 数值正确性、`save_queue_maxsize=4` 与 `_shutdown_timeout_s=5s`/`recon_thread.wait(8000)` 对全幅 TIFF 落盘是否足够。
- README 和部分历史文档曾出现编码显示异常；若后续需要依赖 README 作为稳定文档源，应先统一编码。
- 真机 Hamamatsu + R11 + NI USB-6423 的 Stop 响应、DAQ 全低和下一次采集可启动仍需硬件验收。
- Z-Scan 的 Ti2 SDK DLL 路径和 ZDrive 小步进移动已在本机确认；新增 405/561/647 三相位 RO 仍需 MetroCon 编译/烧录并现场枚举。还需逐波长用示波器确认 guard、单次 SLM trigger、相机/激光曝光窗、FINISH 始终低、三相位顺序、SML 曲线/最佳 Z、正式 RO/曝光恢复，以及大 ROI Z-stack 的异步写盘、BigTIFF、Stop/partial 行为。

## 当前待办
- 核实 `config/sim_control_config.json` 中与 SDK 路径、设备配置和时序相关的字段是否足以支持真实硬件接入。
- 在 `sim_control/adapters.py` 中补齐 Hamamatsu `DCAM-SDK4` 的真实绑定实现。
- 在 `sim_control/adapters.py` 中补齐 `R11CommLib` 的真实绑定实现。
- 在真机上验证 NI USB-6423 规范线位 `0/1/2/5/8/6/7/9` 中 9 帧采集时序是否满足联动要求。
- 在真机上用示波器或图像结果验证 `3ms` 相机曝光 + `1ms RO` 时 SLM 是否持续循环到 `slm_finish_line` FINISH 到来，并确认实际触发/退出时序与 `.repz11` 定义一致。
- 在真机上用 DAQ 页「SLM激活时序」测试实测 `slm_enable`→ACT 延迟上界，并确认软件 activate 后初始状态为 `MHW`（验证 [HWA h] EXT_RUN 门控机理）。
- 在真机上重跑 DAQ 页「SIM采集」测试验证 guard=1000 µs 后第一帧有图案、FFT 分组恢复 3+3+3、第九图案不再丢失；如仍异常再用示波器查 READY/SPO_0 相对 `slm_enable` 上升沿的延迟。
- 在 MetroCon 编译/烧录并枚举 `405/488/561/647_3.5_2d_zscan3p_{5,8,14,20}ms`，确认总 RO 数与图案索引正确；逐波长、逐曝光档用示波器验证 z-scan guard、单次 trigger、camera/对应激光曝光窗和 FINISH 全低，并验证 638↔647 fallback。
- 在真实 Save Path（含可能使用的移动盘/网络盘）验证 Z-stack 大 ROI/多层 backpressure、单 TIFF 页序、>4 GiB BigTIFF、取消/异常 partial 命名、staging 清理失败与 terminal timeout 恢复；诊断 TIFF/CSV 部分发布时确认 GUI 明确显示保留路径。
- 在真机上确认 Z-Scan 取消时 ZDrive 保持在取消发生位置、DAQ 全部置低，并且每个 Z 位置相机只产生 1 张图；同时用真实测试历史分别校准 `预估总用时(仅位移)` 与 `预估总用时(位移+采图)` 是否接近对应测试弹窗总耗时。
- 为各波长配置真实 OTF 路径和可选 background 路径，通过 Recon 页确认输出目录后，在真机全幅/ROI 数据上测试 SIM9 GPU Wiener 重建耗时、显存占用、失败信息、输出文件和重建质量。
- 为各波长离线标定生成 `estimated_params_*.mat`（引擎 estimate 模式产出），填入生产档并启用 `use_saved_params=true`，在真机上确认 saved-params 重建与 estimate 路径数值一致、热速度达 ~45ms；GUI 内置标定入口为后续阶段任务。
- 在真机/`E:\Intelligent_SR` 环境用常驻 `WarmSIMReconstructor` 量化"第一个细胞冷 vs 其余热"的实际耗时曲线，并确认 `save_queue_maxsize` 与关停 drain 超时对全幅 TIFF 落盘足够、高细胞频率下不丢盘。
- 明确基于 GPU Wiener 重建图的目标细胞判据或分类模型，再接入 `release/sort` 决策回传。
- 在真机上对比 `Ti2ZStageAdapter` 加 RLock 前后 z-scan 每步 move_ms 分布（用 `data/z_scan_timing_history.jsonl`），确认串行化未拖慢节拍。
- 在 `E:\Intelligent_SR` 环境写 torch uint16 兼容性探针与微基准，再决定 `reconstruction/sim_wiener.py` 是否改 GPU 端 dtype 转换（审查条目 #17）。
- 在装有 Ti2 SDK 的真机上验证按 `backend.ti2_dll_path`/`ti2_sdk_module_path` 自定义路径加载 ZDrive 的端到端行为（审查条目 #11；仿真路径已验证空串回落与路径透传）。
- 在真机上验收诊断测试 worker 化后的取消行为（审查条目 #1 已完整实现）：SIM 采集/Z-Scan/相机触发/激光脉冲测试运行中点 Cancel，确认 GUI 不冻结、DAQ 全低、激光 TTL 提前拉低（示波器确认置位到拉低延迟 ≤50ms 分片 + 写线延迟满足激光安全）、Z 位移台回起始层、无错误弹窗。
- 在真机上做连续快速"停止→启动"SIM 预览压力测试，确认 preview generation 代数过滤（审查条目 #8 完整修复）在真实相机退出延迟下无残帧、按钮状态不错乱。
- 在真机 ORCA-Fusion BT 上确认 DCAM 真实绑定的 `buf_getframe` 返回结构、`timestamp.sec/microsec` 填充行为与 `get_last_hardware_timestamps()` 输出（审查条目 #24；固件不支持时整组全 0 已防御为 None）。
- 在真机 + 示波器上研究 z-scan 每层重复 `activate_prepared_patterns()` 是否可去重（审查条目 #18；未确认 RO 激活语义前不要改 `z_scan_core.py`）。
- 在 `simulation_mode=True` 下手动跑「开始预览→停止预览」，确认 #5 后仿真 preview 停止不再产生非预期 `signal_error`、按钮状态切换正常（自动化测试未直接覆盖该 GUI 信号路径）。

## 最近更新
- 2026-08-25：按用户确认方案把 SIM9 `inter_frame_gap` 改为 Hamamatsu 原始 timing 驱动。新增 `sim_control/camera_timing.py`：固定安全余量 `500 µs`，按 DCAM 正式条件 `Tx<Tc => Tb>Tc-Tx 且 Tb>=MinTB；否则 Tb>=MinTB` 计算整数微秒理论下界，`TIMING_READOUTTIME` 只做完整性校验和诊断；三项任一缺失、非有限或越界时生成明确 fallback metadata。`FusionBtCameraAdapter` 在当前 ROI/曝光/位深/触发属性应用后读取三项并回传 raw/theoretical/margin/recommended/fallback 字段；仿真 adapter 返回同一合同。`effective_inter_frame_gap_us` 取消旧 50 ms 封顶，合法值向上取整后原样采用，异常才回退 50 ms。controller 缓存绑定设备+ROI+曝光+位深+触发模式完整签名，正式入口强制每次重新 apply/read，worker 本次结果在建波形前覆盖旧值，断开即使 SDK 报错也在 `finally` 清缓存；preview 1 ms timing 不再污染正式摘要。DAQ SIM9 测试改为 `apply camera → derive gap → build waveform → capture`。GUI Summary 完整显示三项 raw、理论值、0.500 ms 余量、最终 gap 与 fallback 原因；fallback 发送非模态 `camera_timing_fallback_warning` 并写日志。未改配置 schema，保留末帧 trailing gap。TDD 证据：各子任务先出现预期 RED，再实现 GREEN；聚焦 pytest `tests/test_camera_timing.py tests/test_sim_camera_adapter.py tests/test_sim_summary.py tests/test_sim_daq_testing.py tests/test_sim_preview_restart.py -q -p no:cacheprovider` 为 **166 passed、56 subtests passed**；`compileall -q app.py sim_control control_wangbo tests tools` 通过。全量 pytest 为 **575 passed、138 subtests passed、5 failed**，5项均是本任务开始前已有 dirty `.ui`/生成 UI 与布局测试不一致（DAQ Refresh 文案、Recon 宽度、scroll content 宽度、footer 高度、SLM Refresh 宽度），与 timing 修改路径无关。独立实现审查最终为 **0 blocker、0 non-blocking 自动化缺口**；真机仍需按全部 ROI preset × 1/3/5/10 ms 做连续 SIM9、DCAM timestamp、示波器/READY/SPO 和条纹相位顺序验收，`500 µs` 在该验收前只视为软件工程余量，不视为生产硬件签收。
- 2026-08-24：按用户要求仅恢复主 GUI Z-Scan 的原始紧凑排布，不回退 2026-08-22 已实现的三态功能。`CellSorting_ui.ui` 恢复 150 px 高度、固定 `layoutWidget(0,20,312,121)`、`Direction / Step (nm) / Layers / Exposure (ms)` 四列横排、50/50/50/60 px 控件宽度，以及底部同一行的 `Select Focus Plane + Test Z-Scan`；继续保留 `Layers`、2–1001、三态 tooltip、schema v14 与所有 Z-stack/自动选焦逻辑。按 `.ui`→`pyuic5` 重新生成 `CellSorting_ui.py`，生成一致性 SHA-256 比对通过；`main.py` 仅移除 2×2 两列伸缩说明/操作，布局回归测试改为锁定 legacy 几何并避免依赖 pyuic5 自动编号。聚焦 pytest `116 passed + 12 subtests`；全量 pytest `550 passed + 106 subtests`，仍仅有任务前相同的 5 个 DAQ/Recon/Scroll/Footer/SLM GUI 基线失败；`compileall` 与 `git diff --check` 通过。独立 subagent review 为 `blocking=0`，确认需求、功能保留、pyuic5 生成链与记忆更新均满足；本次不需要真机硬件验证，但仍需用户在目标 Windows DPI/字体环境下目检实际 GUI 是否与原排布一致、文字是否裁切。
- 2026-08-22：完成主 GUI Z-Scan 双模式实施。三态分派为：关闭=当前位置普通 SIM9；Enable ON/Select OFF=仅手动 raw 按钮执行逐层完整 SIM9 Z-stack，MCU 在预览/激光/平台/硬件前阻止；两项 ON=手动与 MCU 逐层三相位积分图 + SML 离散最大值选焦 + 最佳层普通 SIM9。GUI 将 Steps 改为 Layers（最小 2），保存/加载持久化 `select_focus_plane`，正式采集期间禁用全部 Z-Scan 控件；schema 升 v14，旧配置默认保留自动选焦并清除固定起始 Z。SLM repertoire 新增 405/561/647 四档三相位 RO，488 保留，638/647 exact-first/fallback；DAQ 按当前波长驱动对应激光且 FINISH 恒低。新增轻量 `ZStackAcquisitionResult`、有界异步 `ZStackAsyncWriter`、层块 spool、单 TIFF/BigTIFF/partial、逐页元数据，以及 zfocus TIFF + SML CSV；诊断落盘失败软降级而最终 SIM9 不丢，非完整终态会报告 retained output/spool/staging，focus 后台写盘失败时只在已提交层中重选诊断 best 并发布 partial。最终软件复验：聚焦 writer/controller `46 passed + 5 subtests`，全量 pytest `550 passed + 106 subtests`、全量 unittest `551 tests`；两种 runner 均只剩任务前已有的 5 个 DAQ/Recon/Scroll/Footer/SLM GUI 基线失败，原 2 个 Z-scan layout 失败已修复且无新增错误。Writer 规格审查及最终独立实现审查均收敛到 `blocking=0`；真机仍需 MetroCon 烧录、四波长示波器、SML/最佳 Z、正式 RO/曝光恢复和大文件写盘验收。
- 2026-08-22：按用户要求更新主 GUI `Save Path` 的 SIM9 raw stack 文件名：统一为 `Prefix-波长-曝光ms-ROI-四位编号.tif`，程序自动在 Prefix 与波长之间补单个 `-`（Prefix 已以 `-` 结尾时不会重复）；方形 ROI 写单边长度，非方形写 `WxH`。`control_wangbo/main.py` 将原 `task_id -> 完整路径` 改为 `task_id -> folder/prefix/number` 轻量保存规格，batch 到达 GUI 路由时只读 `laser_wavelength_nm`、`exposure_us` 与 `stack.shape` 生成真实文件名，再通过既有 queued signal 交给 raw-save worker，未增加同步磁盘 I/O 或 raw stack GUI 状态暂存；Prefix/Start/波长/曝光/ROI 及配置加载均刷新 `Next:` 预览。配置键、四位编号递增/持久化、显式同名覆盖、空路径自动命名和 `.ui`/生成文件保持不变。TDD 首轮 4 项、配置加载联动 2 项均观察到预期 RED 后转 GREEN；根据独立 review 建议又补充同步后真实 `Next:` 文本断言。聚焦 `test_sim_preview_restart.py + test_efficiency_optimizations.py` 为 `100 passed, 8 subtests passed`，`test_ui_regressions.py` 为 `18 passed, 4 subtests passed`。全量 `pytest tests/ -q` 为 `438 passed, 26 subtests passed, 7 failed`，失败均来自本次未修改的既有 GUI `.ui`/生成代码与 DAQ/Recon/Scroll/Footer/SLM/Z-Scan 测试期望漂移；测试前后两个已有脏配置文件 SHA-256 未变。独立 subagent review 为 `0 blocker`，确认命名、实际 batch 优先、线程/大数组边界、清理/fallback/覆盖/持久化语义均满足要求；人工待确认真实 GUI 的 `Next:` 展示及一次真实/仿真 SIM9 TIFF 文件名与示例一致。
- 2026-06-30：按用户补充要求，回退上一轮中 `SIM Configuration` 除新增 `Save Path` 模块之外的视觉/布局改动。保留 `grp_savePath` 及 `main.py`/`pipeline.py` 的保存路径、prefix、start number、显式 raw stack 保存路由逻辑；将非 Save Path 的 SIM Configuration 子模块字体恢复为 `Times New Roman`，子模块 `QLabel` 字号恢复为 10pt，`grp_simConfiguration` / `layoutWidget_simConfiguration` 宽度恢复为 314/302，根内容宽恢复为 1800。为让新增 `Save Path` 在恢复后的窄列内可用，仅压缩该模块内部文案/宽度：`Save Folder`→`Folder`、`Start No.`→`Start`、Browse 按钮显示 `...`，并加测试护栏确保 `grp_savePath.layout().minimumSize().width() <= layoutWidget_simConfiguration.width()`。更新相关 UI/字体/宽度测试并用 `pyuic5` 重生成 `control_wangbo/CellSorting_ui.py`。独立 subagent review（Plato）确认 Save Path 保留、非 Save Path 字体/宽度已恢复；指出 DAQ Refresh 在 36px 宽下仍显示 `Refresh`、Recon 测试 docstring 残留 `SIM Runtime`，已修为 `↻` 并更新文档断言。验证：聚焦 pytest（UI regression、Save Path/raw save、DAQ/Z-Scan/Recon/scroll/footer 相关选择器）`157 passed`，仅 pytest cache 权限 warning。待确认：真实 GUI 目检 Save Path 在 302px 窄列内无裁切/重叠，`...` 按钮含义对操作者是否足够明确。
- 2026-06-30：按用户提供的 Claude 方案把主 GUI `SIM Configuration` 首项 `SIM Runtime` 替换为 `Save Path`。`control_wangbo/CellSorting_ui.ui` 静态删除 `grp_simRuntime`、新增 `grp_savePath`（保存文件夹、prefix、start number、next preview），并用 `pyuic5` 重生成 `CellSorting_ui.py`；`control_wangbo/main.py` 新增 Save Path 运行时接线、prefix 净化、Browse、`sim_save_next_number`、`task_id -> explicit path` pending 路由和 `lastConfiguration.json` 四键持久化；`sim_control/pipeline.py` 新增 `RawStackSaveWorker.slot_save_to_path(batch, path)` 支持显式路径保存（按方案，编号文件已存在时直接覆盖；空 path 仍走原日期目录自动命名）。SIM Configuration 子树英文字体统一 Arial、`QLabel` 12pt，根内容宽改 1840 以容纳右侧 360px 面板，并修复漏网 Calibri。测试更新覆盖 Save Path 控件/编号/路由、raw explicit path、Z-Scan/DAQ/Recon/scroll/footer 字体宽度护栏。验证：聚焦 pytest（8 个相关测试文件/测试选择器）`157 passed`；`git diff --check` 仅 CRLF warning；全量 `unittest discover` 因当前 Python 临时目录固定落到 `C:\Users\user\AppData\Local\Temp` 且权限拒绝未完成。独立 subagent review（Kant）4 项：宽度越界、Calibri 已修；覆盖行为按方案保留；Flow Rate/Collected ROI diff 属会话前 dirty/并发历史改动未回退。真机/人工待确认：GUI 目检、真实 SIM9 raw-only 采集保存到所选文件夹、编号递增/跨重启持久化和覆盖语义符合操作者预期。
- 2026-06-30：按用户要求放宽 Fast Camera Capture 细胞识别形状判据，仅修改 `control_wangbo/FastCameraThread.py` 的 `particle_detection_for_capture()`：圆度阈值从 `circularity >= 0.7` 改为 `>= 0.5`，长宽比从 `0.7 <= w/h <= 1.3` 改为 `0.5 <= w/h <= 2`，并同步更新注释。其他面积范围、贴边排除、单轮廓数量、Capture ROI X 范围、Trapped/Release/Collected、MCU 和 SIM 逻辑均未改。验证：`ast.parse` 通过，`git diff --check -- control_wangbo\FastCameraThread.py` 通过。
- 2026-06-30：**主 GUI DAQ Test 按钮改名 + Z-Scan 重排 2×2 + SIM Configuration 列再收窄到内容宽 302**（接 2026-06-29 列收 330 条）。①**按钮**：`btn_main_daq_test` 文本 "Pulse Test"→**"Test"** + 固定宽 **52**；`main.py` `_daq_test_btn_text` 初值与按钮恢复 fallback 一并改 "Test"（全文件无 "Pulse Test" 残留）。②**设备行收窄**（Refresh 按钮是真瓶颈）：`btn_main_daq_refresh` 文案 "Refresh"→**"↻"**(U+21BB) + 固定宽 **36**；`cmb_main_daq_device` maxWidth 110→95、`cmb_main_red_laser` 85→78、`lbl_main_red_laser` 加 maxWidth **60**（红光保持设备行）。③**Z-Scan 重排 2×2**：`gridLayout_zscan` 4 控件由 1 行改 2×2（label-on-top）——方向/步进 标签row1·控件row2、步数/曝光 标签row3·控件row4，`chk_main_zscan_enabled` colspan2、`hbox_zscan_run` row5、status row6；控件宽 76/82/76/76 不变；`chk_main_zscan_capture`("Select Focus Plane") 加 maxWidth **165**；`main.py` `setup_sim_zscan_module` 的 setColumnStretch 由 5 列改 **2 列**（col0/1=0、col2=1）。④**几何**：layoutWidget_simConfiguration 330→**302**、grp_simConfiguration 342→**314**、grp_simSummary x 1852→**1824**、窗口 2172→**2144**（以 DAQ Test 行 下拉235+Test按钮52 为准）。流程遵静态控件规则：**`.ui`/main.py/测试均用单次 Python 脚本改（不用并行 Edit——上个 DAQ 任务对同一 `.ui` 单条消息并行 9 Edit 触发写竞争丢 ~1600 行，已成教训）**→ pyuic5 重生成（grep 核对 18 项改动落地）。**测试**：test_main_window_zscan_module（row 2×2 断言 + chk maxWidth165 + 护栏改锁 `grid.minimumSize().width()<=302`）、test_main_window_daq_module（几何 314/302/1824 + 按钮/Refresh/Red 标签/下拉 maxWidth 断言 + 互锁恢复按钮文本 "Test"）、test_main_window_scroll_area（content 2144）；相关 **46 模块 OK**、全量 **434 OK**（无 fail）。**审查**：codex 双循环——第一循环多轮（审出 DAQ 设备行 Refresh~112 真瓶颈→缩 Refresh；其 Red 标签 98/Select Focus Plane 383 是**沙箱字体度量偏大假值**→用 maxWidth 护栏应对）、第二循环 **0 blocking**；**codex 沙箱 minimumSizeHint 偏大假值被真实 Windows 字体测试 46+434 OK 证伪**（重现 2026-06-26 offscreen 假值陷阱：真实 lbl_red~45/Select Focus Plane~119 ≪ 沙箱 98/383）；codex 进程多次卡僵尸 task（`--resume` 接到 7h 未退 task）、经用户确认改用 `--fresh` 全新会话。非高风险按去重规则免 subagent review。**flow rate**：本次重生成 `.py` 时并发"删除 Flow Rate Detection"工作已完成 `.ui` 删除，故全量 434 OK 无 flow rate fail——前述并发冲突（条目 127）随之解决。**真机/人工待确认**：① "↻"(U+21BB) 在真实 Times New Roman 下渲染（无方块/乱码，否则回退短文案 "Ref"）；② Z-Scan 2×2 重排后信号/运行正常（仅改 grid 位置未动 connect）；③ 真实字体目检列收窄后各模块整齐、DAQ Test 下拉+按钮同行、各模块不溢出/不重叠。**提交需协调**：工作区混并发 flow rate/SIM Summary/dir0 诊断未提交改动，提交前按任务拆 hunk。
- 2026-06-30（诊断/调查，**未改代码**）：用户怀疑「当前代码控制 SLM 图案逻辑导致 dir0 静态光栅残留/未清屏」（接 2026-06-28 dir0 静态光栅发现）。经 3 路并行 Explore + 研读 `sim_control/waveform.py`/`acquisition_core.py`/`adapters.py` + **codex 第一循环方案审查（blocking=0）**：判定**当前 Python 正式采集代码不是根因**——正式 SIM9 = SLM Running Order 模式，Python 不逐帧控图（`select_running_order`→`activate_prepared_patterns()` 一次 `R11_RpcRoActivate()`→DAQ 发 9 个 trigger，9 帧图案内容由预烧录 repz11 决定）；波形时序正确（`slm_enable` 全程高、每帧 1 trigger+1 finish、9 trigger、guard 1000µs>tHWAT、激光只在曝光窗）；代码缺陷（采集前后无清屏/`deactivate_running_order()`、`activate` 无幂等/不校验 `0x56 ACT` 就播波形）只影响第一帧或下次采集，且采集后 SLM 停 **dir2** 非 dir0，解释不了"贯穿 9 帧 24%"。codex 独立核实 `read_frame_sequence`(adapters.py:1690/1727/1787) 只读 DCAM、不碰 SLM。根因三选一（① repz11 RO 定义 / ② SLM 物理残留 image sticking / ③ 固定光学条纹），频域不可分、需真机。**离线 A1/A2**（用 off 帧 f3–8 均值谱）**弱指向"固定光学/相机来源"而非 SLM 残留**：A1 静态相位 −118.6° 不匹配 dir0 三移动相位（但扣除后步进非 120°、置信度低）；A2 静态幅值与曝光**负相关 −0.83**（不随曝光增长，偏向曝光无关固定成分；跨采集变异大）。**真机判别决策树**：B1 SLM-blank（决定性，关图案看 87° 是否还在）→ B2 **power-cycle 清场后**单方向非 dir0 显示 → B3 时间衰减；计划存 `~/.claude/plans/slm-slm-dir0-jaunty-steele.md`。codex non-blocking（本计划不改代码故**不实施**）：① 正式采集前 immediate-live 停止应在核心层硬保障（现依赖 GUI 层 `control_wangbo/main.py:3164` 先停）；② `validate_daq_line_config` 的 line 别名（`line1`/`line01`）重复检测偏软。**审查**：高风险领域诊断方案走 codex 第一循环（blocking=0）；不改代码故无第二循环。**数据路径变化**：原始 SIM9 `.tif` 已被移到 `data/sim_9frames/20260626/` 与 `20260629/` 子目录（顶层只剩 `cross_dir_fft_check/` 产物），后续分析脚本须指向日期子目录。详见 auto-memory `sim-488-dir0-static-grating`。
- 2026-06-30：按用户要求**彻底删除主 GUI「Flow Rate Detection ROI」（流速检测 ROI）模块及代码中所有相关内容**，不影响其他功能（`control_wangbo/`，用户明确授权）。该模块仅做细胞测速显示（细胞速度 μm/ms、液体流量 μL/h），不参与 capture/trapped/release/sort/collected/function 核心筛选分选。**改动**：①`CellSorting_ui.ui` 删 3 个顶层 widget 块 `grp_flowRateDetection`（参数面板）、`grp_releaseROIView_2`（显示面板，标题也是 Flow Rate Detection ROI）、`line_23`（分隔线），以及漏网的 `label_6`（"需要进入图像处理模式才能测速"提示），按 objectName 内容定位+`<widget>`深度配对删除，再 `pyuic5` 重生成 `CellSorting_ui.py`（生成文件未手改）。②`main.py` 删 flowRate 接线块、3 个槽方法、MCU 信号连接、`para` 流速键、`experiment_imfo[spb_cellSpeedValue]`、`roi_display_widgets`/`roi_display_lb`/`roi_way` 的 key 5/6、显示分支 `imageProcessing_way==5/==6`、run/stop 守卫、`flowRateROI_*` 赋值、配置存取、state init、`cmb_objective` 连接；删 `if imageProcessing_way not in (5,6)` 守卫并 de-indent record 块、删 record 的 `cell_speed(μm/ms)` 字段；清理 5/6 语义残留注释。③`FastCameraThread.py` 删死信号、flowRate state、para 读取、`roi_params`/`roi_Bg_frames` 的 key 5、dispatch `elif roi_choice in (5,6)`、整个 `particle_detection_for_flowRate` 方法（roi_choice 现仅 0-4）。④`PublicClassCamera.py` 删 `self.speed` 与两条 fast camera AVI 文件名的 `[(speed)μm_ms]` 段（**AVI 文件名格式变化**：去掉 speed 字段，视频仍正常保存）。⑤`MCUTriggerThread.py` 删信号 `signal_close_btn_flowRateImageProcessing_function` 与 `elif imageProcessing_way==6` 分支（保留 else）。⑥`sCMOSCameraThread.py` 删死信号 `signal_sendFlowRateDetectIndex`。⑦`tests/test_ui_regressions.py` `column_groups` 8→7、`lines` 7→6（删 line_23）、注释 8组7线→7组6线、新增 `test_flow_rate_detection_widgets_removed`（assertFalse 18 控件）。⑧`lastConfiguration.json` 删 flowRate 键。⑨`代码说明文档.md` 删 flowRate 章节。**验证**：中英文全覆盖零残留（含生成文件）、`ast.parse`/JSON 全过、setupUi 冒烟（20 控件确认不存在）、`pytest tests/test_ui_regressions.py` 18 passed、全量 `pytest tests/` **437 passed**。**审查**：codex 双循环全程 0 blocking（第一循环方案审查 2 轮、第二循环实现验证 2 轮，同会话 fresh→resume）；高风险（相机/触发线程）按规则补独立 subagent review，0 CRITICAL/HIGH/MEDIUM。**本条使前述 2026-06-29 条目（124/125/127）中「Flow Rate Detection 控件仍存在 / `test_flow_rate_detection_widgets_removed` 遗留失败」的描述失效——该测试现已通过。** ⚠️**并发污染**：本删除与一个并发「DAQ test row trim + SIM config column narrowing」会话共用同一工作区（基线 commit `410dbc4`）；该 DAQ 会话曾 `git restore` .ui/.py 到基线、覆盖回退过本删除（见条目 127 自述），经用户决定暂停 DAQ、不碰 flow rate 后，我基于其改动用内容定位脚本重做了 .ui/.py 的 flowRate 删除（避开其 DAQ 改动）。全量唯一失败 `test_main_window_zscan_module::test_module_fits_panel_width`（grp_zscan 367>面板330）属该 DAQ 会话的 SIM config 列收窄、**非本删除引入**。**真机/人工待确认**：①AVI 文件名去掉 speed 段后若有外部脚本按名解析需确认无依赖；②真机目检左列 collected 之后留白、无错位；③真机跑一轮筛选+录像确认各 ROI/视频保存/MCU 触发不受影响。**提交需协调**：工作区混有并发 DAQ/Summary 未完成改动，提交前须按任务拆分 hunk。
- 2026-06-29：按用户要求把主 GUI `Trigger Control` 的 4 个时间输入控件统一为 1ms 输入精度：`spb_triggerCapture_time`、`spb_triggerRelease_time`、`spb_triggerReleaseSort_time` 从 `decimals=1`/`singleStep=0.1`/最小 `0.1ms` 改为 `decimals=0`/`singleStep=1`/最小 `1ms`，`spb_triggerFunction_time` 保持最大 `600000ms` 且步进从 `10ms` 改为 `1ms`；按静态 UI 流程修改 `control_wangbo/CellSorting_ui.ui` 并用 `pyuic5` 重新生成 `control_wangbo/CellSorting_ui.py`，`tests/test_ui_regressions.py` 增加 4 个 Trigger 时间框的精度回归断言。
- 2026-06-29：按用户提供的旧版 `MCUTriggerThread.py` 和当前烧录固件 `Trigger输出程序 20260520 1ms精度 可中断盲推版本/main.c` 对齐 MCU 串口协议：`control_wangbo/MCUTriggerThread.py` 新增固件协议常量 `MCU_CMD_CAPTURE=0xFE`、`MCU_CMD_FUNCTION=0xFD`、`MCU_CMD_RELEASE=0xFB`、`MCU_CMD_SORT=0xF7`、`MCU_CMD_RELEASE_SORT=0xF3`、`MCU_CMD_OFF=0xFF`、`MCU_MAX_TIMED_MS=254`、`MCU_TIME_CONTINUOUS=0xFF`，并用 `_mcu_frame()` 统一生成 2 字节帧：第一字节为 P0 IO mask，第二字节为 1ms 延时，超过 254ms 转 `0xFF` 持续；所有当前串口写出路径改为 `_write_mcu_frame()`，Rinse 长开和 OFF 也使用同一协议。`Function` 按既定需求仍只作为等待时间，不发送 `0xFD`。新增 `tests/test_mcu_trigger_protocol.py` 锁定命令头、1ms 时间字节、`>254ms -> 0xFF`、`OFF=0xFF,0xFF`、Function 等待不写串口，以及 `Release+Sort` 两段发送顺序和长 Release 用 `0xFF` 后被 `ReleaseSort` 抢占。验证：`ast.parse` 覆盖 `MCUTriggerThread.py`、生成 UI 和相关测试通过；`unittest tests.test_mcu_trigger_protocol -q` 5 项通过；目标 UI 回归 `test_cellsorting_ui_removes_legacy_elasticity_widgets_and_restructures_sim_controls` 通过；`git diff --check` 仅有既有 CRLF 提示。未修改 SIM camera/SLM/DAQ 代码。
- 2026-06-29：按用户要求调整 Fast Camera Single Run 的 trapped ROI 失败分支：`control_wangbo/MCUTriggerThread.py` 中 `slot_finish_ROI_processing(imageProcessing_way == 1, finish_state != 1, runModel != 2)` 现在不再调用 `triggerRelease_autoVersion()`，而是直接 `emit(-1)` 关闭 ROI 处理并触发关闭 Single Run；`imageProcessing_way == 7` 的 Function 非目标/未检出分支保持先 `Release` 再停止，连续模式 trapped miss 仍保持 `Release` 后回到 capture。未修改 SIM camera/SLM/DAQ 或 UI 逻辑。验证：`ast.parse` 通过；轻量 QCoreApplication/monkeypatch 状态机检查通过；独立 subagent review 0 blocking，提醒提交时只纳入本次目标 hunk，避免混入历史 dirty 变更。
- 2026-06-29：按用户要求把 Fast Camera 左侧 `Collected Cells ROI` 控件尺寸和对齐恢复为与其他 ROI 控件一致：`control_wangbo/CellSorting_ui.ui` 中 `layoutWidget_4` 左边距改回 10、尺寸改为 285x51，四个 Collected ROI 标签与数值框固定为 50px 宽，`btn_collectedROI_view` 固定为 55px 宽，并用 `pyuic5` 重新生成 `control_wangbo/CellSorting_ui.py`；`tests/test_ui_regressions.py` 增加尺寸和同列上下中心对齐断言，同时继续断言 Collected ROI 角度控件不存在。本次未修改 `main.py`、Fast/MCU 线程、SIM camera/SLM/DAQ 或分选逻辑。验证：`ast.parse` 覆盖生成 UI 与测试通过；目标 pytest（Collected ROI 无角度/尺寸对齐、Background Diff Collected 标签对齐）2 项通过；当前全量 UI 回归仍有历史无关的 `test_flow_rate_detection_widgets_removed` dirty 期望未完成，本任务未处理。
- 2026-06-29：按用户要求把 Fast Camera 的 Collected ROI UI 也恢复为无旋转口径，且不修改 SIM 相关代码。`control_wangbo/CellSorting_ui.ui` 删除 `label_collectedROI_angle` 和 `spb_collectedROI_angle`，`btn_collectedROI_view` 左移到原 Angle 列位，并用 `pyuic5` 重新生成 `control_wangbo/CellSorting_ui.py`；`control_wangbo/main.py` 删除 Collected ROI angle 的信号连接、配置保存/加载、参数下发、ROI 数据记录字段、live 旋转绘制和 Background Diff 旋转裁剪，Collected ROI UI 显示与内部 Fast Camera 识别均回到轴对齐矩形；`control_wangbo/roi_geometry.py` 仅保留退役说明，不再提供旋转 helper；`tests/test_ui_regressions.py` 与 `tests/test_rotated_roi_geometry.py` 改为断言角度控件和运行时旋转路径不存在。本次未改 `sim_control/`、SIM camera/SLM/DAQ 采集链路或 MCU 时序。验证：`ast.parse` 覆盖相关 Python 文件通过；范围内 pytest（Collected ROI 无角度控件、Collected Background Diff 标签对齐、运行时旋转路径移除）3 项通过；`git diff --check` 通过（仅保留既有 CRLF 提示）。全量 `tests/test_ui_regressions.py` 仍有历史无关失败 `test_flow_rate_detection_widgets_removed`（当前 UI 仍存在 Flow Rate Detection 控件），未在本任务中处理。
- 2026-06-29：按用户要求将 Fast Camera 自动分选的核心线程行为恢复为旧版本口径，且不修改 SIM 相关代码。仅改 `control_wangbo/FastCameraThread.py` 与 `control_wangbo/MCUTriggerThread.py`：`FastCameraThread` 中 Collected ROI 内部背景 ROI 和当前帧 ROI 重新使用旧版轴对齐矩形裁剪，不再在内部识别链路调用 `crop_rotated_roi()`，也不再向 ROI 结果 payload 写 `roi_angle`；Capture/Trapped/Release/Collected 的面积、圆度、长宽比和扫描帧数判据保持不变。`MCUTriggerThread` 中目标细胞 Release ROI 后仍复用手动 `Rel+Sort` 时序，但 `isReleaseSaveVedio` 清理恢复为旧版在 `finish_releaseSort()` 中执行，不再额外在 `releaseTime + releaseSortTime` 处单独 `setattr` 清理。本次未修改 `main.py`、`sim_control/`、SIM camera/SLM/DAQ 采集链路或 UI 控件；当前 UI 层 Collected ROI 角度控件/显示仍属既有历史改动，内部 Fast Camera 分选识别已回到旧版轴对齐口径。验证：对 `control_wangbo/FastCameraThread.py`、`control_wangbo/MCUTriggerThread.py` 执行只读 `ast.parse` 语法检查通过；`compileall` 因 Windows `__pycache__` 写入权限被拒未完成，不是语法失败。真机/人工待确认：恢复旧版轴对齐 Collected ROI 内部识别后，现场是否还需要同步隐藏或停用 UI 上的 Collected ROI 角度显示控件。
- 2026-06-29：**主 GUI DAQ Test 行精简 + SIM Configuration 列收窄**。①**DAQ Test 行**（`control_wangbo` + `sim_control/daq_testing.py`）：删 `lbl_main_daq_test`（"Test"标签）；`build_daq_test_target_items` 移除 "Camera Trigger + Capture" 项（`DAQ_PULSE_TEST_ROLES` 去 `camera_trigger_line`、删死代码 `_run_camera_trigger_test`、`run_test` 末尾激光 fallback 前加 `if target_id not in DAQ_PULSE_TEST_ROLES: raise ValueError` 守卫堵 camera/未知 id；**保留** `ROLE_LABELS["camera_trigger_line"]`（DAQ wiring 仍引用、line8 角色不变））；下拉 `cmb_main_daq_test_target` 去 Expanding、固定宽 **235**（完整显示最长项 "Laser 561 → Dev1/port0/line11"，含 gridLayout_daq margin 算术 4+235+6+75+4=324≤330）；下拉默认项**跟随采集波长**——新增 `_daq_test_role_for_wavelength`（复用 `models.LASER_ROLE_MAP`，638/647→laser_red_line，未知回退 488）+ `_select_daq_test_target_for_wavelength`，`_refresh_main_daq_test_targets` 保留前次选中/无前次默认当前波长 role，`on_sim_camera_setting_changed` 波长分支（`sim_camera_connected` 早退前）调 `_select` 强制跟随（设备刷新/配置加载不覆盖手动选择）。②**SIM Configuration 列收窄**：内容宽 360→330（grp_simConfiguration 372→342、layoutWidget 360→330、grp_simSummary x 1882→1852、窗口 2202→2172）；Z-Scan row2 控件 80/90/80/80→**76/82/76/76** 使 `grp_zscan.minimumSizeHint()`=328≤330（真实字体实测，满足 `test_module_fits_panel_width` 护栏）。流程遵静态控件规则：改 `.ui`→pyuic5 重生成（零漂移 diff 核对）→main.py 仅接线→更新测试。**测试**：test_main_window_daq_module（下拉 6 项+默认 488+波长联动+几何 342/330/1852+删 label+下拉 235）、test_sim_daq_testing（build 6 项+run_test camera/未知→ValueError）、test_sim_preview_restart（删 3 个 `_run_camera_trigger_test` 测试）、test_main_window_scroll_area（content 2172）、test_main_window_zscan_module（控件宽 76/82/76/76）；相关 7 模块 **123 OK**。**审查**：用户要求 codex 双循环——第一循环（方案）2 轮（修 B1 下拉宽漏算 grid margin、B2 run_test fallback 未堵 camera）0 blocking 通过；第二循环（实现）1 轮 0 blocking 通过；非高风险按去重免 subagent review。codex 进程本轮多次超时/挂起（--resume 接未结束旧 task），经用户确认改用 `--fresh` 全新会话复审。⚠️**并发污染警示**：本会话期间存在**独立的"删除 Flow Rate Detection 模块"并发工作**（外部会话/工具：改 `tests/test_ui_regressions.py` 加 `test_flow_rate_detection_widgets_removed` 期望该模块控件不存在、删 `main.py` 大量 flowRate 逻辑；另有并发的 SIM Summary 精简）；我排查"误判的 .ui 损坏"时曾 `git restore` .ui/.py 到基线，**恢复了 flow rate 模块、可能覆盖了并发对 .ui/.py 的删除**；**用户决定暂停、本任务只交付 DAQ、不碰 flow rate**。故全量 `unittest discover` 429 **遗留 1 个无关 fail** `test_flow_rate_detection_widgets_removed`（属并发 flow rate 删除工作未完成，非本 DAQ 任务）。⚠️**经验教训**：**对同一大文件在单条消息并行多个 Edit 会写竞争损坏文件**（本会话 `.ui` 并行 5+4 Edit 丢失约 1600 行）；大文件改动改用单次读-改-写脚本或严格串行 Edit。**真机待确认**：GUI 下拉无 Camera Trigger 项、四路激光 pulse 各对应预期 line；真实字体下列收窄后各模块整齐/不溢出、下拉完整显示最长项。基线 commit `410dbc4`（WIP 隔离）含会话期未提交内容，后续提交需协调（含并发 flow rate / Summary 工作）。
- 2026-06-29：按用户计划精简主 GUI 右侧 `SIM Summary`，把摘要收敛为“主 GUI 其他模块没有直接显示的信息”。仅修改 `sim_control/summary.py` 与 `tests/test_sim_summary.py`，不改 `CellSorting_ui.ui` / `CellSorting_ui.py`。Summary 继续显示 `Pattern RO`、`TIMING_READOUTTIME`、`SIM9_ESTIMATED_TOTAL_TIME`、内部 `Timing` 四项，以及启用 Z-Scan 时的派生信息 `start_um`、`image_layers`、`total_distance_um`、两类 ETA 和 `actual_exposure_us`；删除主 GUI 已可见的 `Laser`、`Selected SIM Camera`、`Exposure`、`Bit Depth`、`ROI`、`DAQ` device 与全部 DAQ TTL line；Z-Scan 摘要删除 `enabled`、`direction`、`scan_gap_nm`、`scan_moves`、`exposure_preset_ms` 等模块内已有字段。测试契约同步断言保留字段与被删除字段，并逐项覆盖 `slm_*`、`cam/camera_trigger`、`laser_*` 等 DAQ line key 不再出现。验证：`.\.venv\Scripts\python.exe -m unittest tests.test_sim_summary -q` 通过（8 tests OK），`.\.venv\Scripts\python.exe -m compileall -q sim_control\summary.py tests\test_sim_summary.py` 通过（sandbox 下 `.pyc` 替换权限受限，已按需提权重跑），`.\.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（429 tests OK，sandbox 下用户 Temp 写入受限，已按需提权重跑）。本次为低风险显示文本调整，不涉及硬件控制、DAQ 波形或采集时序，不触发 Codex 双循环；独立 subagent review 0 blocking，指出的非阻塞测试覆盖与旧测试名残留已补齐。真机/人工确认：不需要硬件验收，仅可在实际 GUI 中目检 Summary 文本是否符合操作者预期。
- 2026-06-29：按用户要求把主 GUI「SIM9帧采集」(raw-only) 的 9 帧 raw stack 保存改为**按当天日期归档**——在 `data/sim_9frames/` 下按 `%Y%m%d`（如 `20260629`，与 `control_wangbo` 的 `20260629_video` 约定一致）建当天子文件夹，当天图像存入；当天文件夹已存在则**复用、不重建**。仅改 `sim_control/pipeline.py`：新增 `_dated_output_dir(base_dir, when)`（`Path(base_dir)/when.strftime("%Y%m%d")`、接收**显式时间快照**）；`_build_raw_stack_filename` 加可选 `when: datetime | None = None`（缺省回退 `datetime.now()`，保旧两参调用兼容），内部时间戳改 `(when or datetime.now())`；`RawStackSaveWorker.slot_save` 在 (9,H,W)+uint16 校验**之后**取一次 `save_time = datetime.now()`，同时喂给日期目录与文件名构造——**日期目录与文件名时间戳同源 save_time，消除跨午夜整点目录/文件名日期错配**；`mkdir(parents=True, exist_ok=True)` 既自动补建 `sim_9frames` 父目录、又在当天目录已存在时静默复用（不报错/不重建/不删不覆盖）；`_unique_output_path` 同名防覆盖在新日期目录内继续生效。日期口径=运行机**本地时间**。例：`data/sim_9frames/20260629/488_3.5_500ms_2048x2048_20260629154023.tif`。**不改范围**：重建 `data/reconstruction`、DAQ `data/test_captures`、`control_wangbo/main.py:553` 根路径注入均未动（仅 raw-only 主按钮路径）。**测试**（`tests/test_efficiency_optimizations.py`）：`test_raw_stack_save_worker_writes...` 断言改 `saved_path.parent.parent==output_dir` + 父目录名 `^\d{8}$`；`test_raw_stack_save_worker_rejects...` 加坏 stack 后 `output_dir.exists()` 为 False（不建空目录）；新增 `test_dated_output_dir_appends_today`、`test_raw_stack_save_reuses_existing_date_dir`（`mock.patch.object(pipeline,"datetime")` 锁同一秒→连续两次 `slot_save` 落同一日期目录、第二个文件名带 `_2`，真覆盖 `_unique_output_path` 防覆盖）。验证：`unittest tests.test_efficiency_optimizations` **28 通过**、`unittest discover -s tests` **429 OK**。**审查**：用户要求走 codex 双循环——第一循环（方案审查）**2 轮**（首轮 2 blocking[①目录日期与文件名时间戳须同一 `save_time` 快照 ②复用目录测试需 mock 同秒]→修订→0 blocking）；第二循环（实现验证）**1 轮 0 blocking**；非高风险按去重规则免独立 subagent review；无降级。承接 2026-06-25「SIM9 文件名规则」条（本次只在其外再套一层当天日期目录、文件名规则不变）。真机/人工待确认：真机/仿真点击 `btn_sim9_acquire` 后完成弹窗显示路径含当天日期子目录、图像确实落入 `data/sim_9frames/<当天日期>/`。
- 2026-06-28（诊断，非代码改动）：用户报告 `data/sim_9frames/488_3.5_100ms_512x512_20260626111516.tif` 的 9 帧 FFT 中 f4–f9（dir1/dir2 照明帧）出现 dir0 方向亮点。**诊断结论：dir0（≈87°、207nm）存在一条贯穿全部 9 帧的静态光栅**——未照明的 6 帧里该载波相位锁定（`coh_off=|mean(complex)|/mean(|complex|)=0.998`）、幅值≈照明帧的 24%、off/本底=14×；dir1/dir2 的 coh_off≈0.3–0.44（随机噪声值）= 干净。**关键判别法 = off 帧载波相位一致性 coherence；残差谱（帧−9帧均值）对角性不能作为"无静态串扰"证据（循环论证：跨帧恒定成分已被均值按构造减掉，残差对它必然失明）。** 跨文件检验：该 dir0 静态光栅在多个 512 文件（50/100/200/500ms）系统复现（coh 0.84–0.998），2048 文件亦有较弱 dir0 静态（coh 0.934）——系统性、与 87° 绑定，非偶发。来源频域不可分（需真机）：SLM dir0 图案残留/未清屏 vs 固定光学条纹。补充事实：512×512=相机 ROI 裁剪（非 binning，pixel≈65nm）；另有各向异性低频样品结构（r<30px，所有帧共有，是真实样品结构但**不是**用户所指的载波位方向亮点，勿混淆）。产物在 `data/sim_9frames/cross_dir_fft_check/`（report.md、cross_dir_numbers.json[已修正：旧版误存低频结构]、F_static_dir0_grating.png 等）。**连带修正**：早前 `data/sim_9frames/fov_check_20260626111113/report.md`（2048，2026-06-26）称"三方向健康"未做 coherence 检验、漏检了 dir0 静态成分，其"健康/无问题"结论应按本条修正。审查：本任务经独立 subagent review 推翻初版"零串扰健康"误判，再经主 agent 独立 coherence 复算确认（off 帧相位全锁定 112–122°）。
- 2026-06-28：接双机红光特性的**两项 DAQ 模块界面微调**（低风险纯 GUI，不动硬件/波形/时序/采集/配置逻辑）。①**红光机器切换控件移入 DAQ 第一行**：`control_wangbo/CellSorting_ui.ui` `gridLayout_daq` 把 `lbl_main_red_laser`+`cmb_main_red_laser` 从独立 row9（`hbox_main_red_laser` 整块删除）移入 row0 `hbox_daq_device`，置于 `cmb_main_daq_device`+`btn_main_daq_refresh` 右侧，顺序 [设备下拉 \| Refresh \| Red(nm) \| 638/647下拉 \| 水平 spacer]；`hbox_daq_test` row10→9、`lbl_main_daq_status` row11→10。**收窄**：`cmb_main_daq_device` sizePolicy Expanding→Preferred + min70/max110；`cmb_main_red_laser` 去 Expanding + max85；标签文案「Red laser(nm)」→「Red(nm)」；红光下拉显示文案 `main.py:935` `addItem(f"{nm} nm")`→`addItem(str(nm))`（显「638」/「647」，itemData 仍整数 nm，读取全走 `currentData()` 不受影响）。②**DAQ 线位行红光标签改回静态「Laser red」**：移除 `main.py` 动态方法 `_update_main_daq_red_laser_label` 及其两调用（`_build_sim_immediate_and_acquire_controls` 启动、`_apply_sim_red_laser_options` 内）+ 清理 docstring/Load 注释残留字样；`.ui` 静态文案本就是「Laser red」。流程：改 `.ui`→`pyuic5` 重生成 `CellSorting_ui.py`（before/after diff 零漂移，仅目标改动 + `spacerItem5→6` 顺序重编号）。**测试**：`test_main_window_daq_module.py`（布局断言 row9/10→hbox_daq_test/status + 新增红光控件在 hbox_daq_device、删 `_DaqHost` 同名 stub）、`test_main_window_sim_footer.py`（删 3 处 `RuntimeControlHost` 同名 stub）、`test_red_laser_machine.py`（删 `_RedLaserHost` 同名 stub、标签断言 `assertIn("647",…)`→`assertEqual(…,"Laser red")`）。验证：`unittest discover` **427 OK**、`pytest` **433 passed**；pyuic5 零漂移 diff 过。**行宽**：codex 第一循环（用户要求，2 轮：轮1 blocking=行宽截断→用户拍板"紧凑文案"、轮2 blocking=红光下拉 max60 偏小→改 max85）+ 独立 subagent review **0 blocking**——**离屏环境 `QFontDatabase` 不含 Times New Roman、回退字体约 2× 宽致行宽自测/codex 离屏测量均误报截断；真实 Windows 装 TNR 实测算得 4 控件+spacing ≈ 234px ≪ 可用 346px，一行宽裕**。低风险纯 GUI 按 AGENTS.md 与 codex 一致免第二循环 codex review。真机/人工待确认：真实 TNR 下 row0 设备行实际不换行/不截断、`Dev1` 完整显示；638↔647 切换联动（属红光特性范畴）。
- 2026-06-27：**双机红光波长支持（638/647）**——让同一套代码控制两台 SIM 系统（前三档 405/488/561 相同、第四路红光一台真 638 / 一台真 647），取代 2026-06-22「红光统一压成 638」。核心：第四路红光波长变成「每台机器固定、机器间不同」的可配置量 `AppConfig.red_laser_nm`（638/647，默认 638）。**models**：`LASER_ROLE_MAP` 638 与 647 都→中性键 `laser_red_line`；`SUPPORTED_LASERS=(405,488,561,638)` 改显式字面量（**勿** `tuple(LASER_ROLE_MAP.keys())`，否则含 647 变 5 元组）；新增 `supported_lasers_for(red_laser_nm)`/`RED_LASER_CHOICES`/`DEFAULT_RED_LASER_NM`/`RED_EQUIVALENT_WAVELENGTHS`；DAQ 角色键 `laser_638_line`→`laser_red_line`（line 12 不变）；`ReconstructionConfig` 加 `otf_647_path`/`estimated_params_647_path`；`AppConfig.red_laser_nm`+`config_version=13`。**config_store v13**：**命门**——移除 `app_config_from_dict` 迁移链后对 `_migrate_red_laser_aliases` 的二次调用（原每次加载把 647 压回 638）；`_migrate_red_laser_aliases` 改只归一 640；新增 `_infer_red_laser_nm`（按 647 证据，须在合并线键前）+`_migrate_v12_to_v13`（推断 red_laser_nm→合并红光线键到 laser_red_line→setdefault 647 字段→校正 selected∈supported_lasers_for）；validate 按机器红光+`red_laser_nm∈{638,647}`。**adapters/controller/sim_adapters**：`find_best_running_order` 638↔647 双向 fallback+动态 warning；`immediate_live_wavelength_matches` 双向 `{638,647}`；`SIMULATED_RUNNING_ORDERS` 加 647 正式段（追加列表末尾、不破坏 immediate 0..27 索引，68→74 项）。**主界面（control_wangbo/main.py + CellSorting_ui.ui/.py，走 pyuic5 零漂移）**：DAQ 红光控件 `cmb/lbl_main_daq_laser_638`→`_red`；新增红光机器切换下拉 `cmb_main_red_laser`（638/647）；采集/recon 波长下拉按 `supported_lasers_for`；新增 helper `_sim_runtime_busy`/`_update_main_daq_red_laser_label`/`_apply_sim_red_laser_options`(纯 UI blockSignals)/`_on_main_red_laser_changed`（**绝不提前写 selected_laser_nm**，只写 red_laser_nm+helper 重建后委托 `on_sim_camera_setting_changed` 经 sync 检测 old→new）；`on_sim_camera_setting_changed` 加 stop 失败硬互锁（关光失败不切 RO，保护所有波长切换）；`sync_sim_camera_config_from_ui` 补读 red_laser_nm；`merge_legacy_sim_control_payload` 补 red_laser_nm；`apply_loaded_sim_settings_payload` stop 先于 merge+硬互锁+helper 先于 findData 回显+重选 RO；`load_configure_settings` 入口忙碌互锁（采集/Z-Scan/DAQ 测试/连接进行中拒绝切换/Load 不部分写入）。**config**：默认与 production.example 升 v13、`laser_red_line`、`red_laser_nm`、`otf_647`。**测试**：既有断言更新（键名/版本/640→638 保留、647 语义反转、RO 双向、recon 647、immediate 双向、sim RO 74 项）+ 新增 `tests/test_red_laser_machine.py`（8 项：数据契约/647 波形拉 line12/helper 重建/忙碌拒绝/不提前写 selected）；`unittest discover` 427 OK、`pytest` 433 passed。**审查**：高风险，Codex 双循环（方案 4 轮 / 实现 1 轮 0 blocking）+ 独立 subagent review 0 blocking 双审。**真机/人工待确认**：两台红光 TTL 都接 line 12；现场 v12 已压 638 的旧配置无法自动恢复 647 标定路径，需人工重填 `otf_647_path`/`estimated_params_647_path`；647 机 repertoire 实际 RO 命名需 `list_running_orders()` 现场核对。`control_wangbo/lastConfiguration.json` 保留为 v12 legacy fixture（加载时自动迁移）。
- 2026-06-26：接布局微调，按用户要求把主 GUI **DAQ 模块 8 个通道下拉改为 label-on-top（标签在上、下拉在下，与 SIM Camera Settings 一致）+ 删第一行 "Device" 标签 + 自动收窄 SIM Configuration 列**（纯 GUI，不动硬件/波形/时序/采集/配置逻辑）。**`control_wangbo/CellSorting_ui.ui` `gridLayout_daq` 由 4 列「标签在左\|下拉在右」重排为 2 列 label-on-top（11 行×2 列）**：删 `lbl_main_daq_device`；row0 嵌套 `hbox_daq_device`(跨2列)=[`cmb_main_daq_device`(sizePolicy hsizetype=Expanding 吃余量), `btn_main_daq_refresh`]；row1-8「标签行\|下拉行」交替（col0=SLM enable/trigger/finish/Cam-Trig，col1=Laser 405/488/561/638，标签在下拉正上方一格）；row9 嵌套 `hbox_daq_test`(跨2列)=[`lbl_main_daq_test`, `cmb_main_daq_test_target`(Expanding), `btn_main_daq_test`]；row10 `lbl_main_daq_status`(跨2列)。**关键**：两 HBox margins 0/spacing 6、**不加 spacer**——combo 用 Expanding 吃余量、按钮靠右；加 spacer 会与 combo 抢空间致不拉伸（区别于 Z-Scan 全固定宽控件的 hbox_zscan_run 用 spacer）。8 通道下拉 min 宽 120、字体标签 10pt/下拉 12pt 不变。**几何收窄级联**（x=1480 左边界不变只右收，实测校准）：窗口 2285→**2202**、`grp_simConfiguration` 455→**372**、`layoutWidget_simConfiguration` 443→**360**、`grp_simSummary` x 1965→**1882**（宽 300 不变）。**真实字体（platform=windows，非 offscreen）实测各子组 `minimumSizeHint().width()`：grp_zscan 348（收窄后宽度下限，Z-Scan 本轮不改）、grp_recon 308、grp_daq 374→252、grp_slm 284、grp_runtime 263**；`verticalLayout_simConfiguration` margins 全 0，定 layoutWidget=360（≥grp_zscan 348，留 12px 余量，满足 `test_main_window_zscan_module.py::test_module_fits_panel_width` line251 护栏 `grp_zscan.minimumSizeHint()<=panel_w`）；列垂直 sizeHint 高 852<891 不溢出；config 右边界 1852/summary 左 1882 间距 30、summary 右 2182/窗口 2202 余量 20、无重叠。`control_wangbo/main.py` `setup_sim_daq_module` 的 `gridLayout_daq` setColumnStretch 由 `(0,0)(1,1)(2,0)(3,1)` 改 **`(0,1)(1,1)`**（2 列等分）。改 `.ui`→`pyuic5` 重生成 `CellSorting_ui.py`（重排用 scratchpad 脚本按 objectName 保留各 widget 的 font/minSize/text 不变、only 重排行列+包 HBox+删 device；before/after diff 仅 DAQ 区+4 处几何，零漂移）。测试：`tests/test_main_window_daq_module.py::test_layout_widths_fonts_columnstretch_and_geometry` 改 `columnCount()==2`+`columnStretch [1,1]`、新增 8 通道 `itemAtPosition` label-on-top 断言、`assertFalse(hasattr(ui,"lbl_main_daq_device"))`、Device/Test HBox 结构断言（`itemAtPosition(0,0).layout() is hbox_daq_device`/`(9,0) is hbox_daq_test`）、几何 372/360/1882；`tests/test_main_window_scroll_area.py` 窗口宽 2285→2202。验证：`unittest discover -s tests -q` **411 OK**（真实字体）；`QWidget.grab()` 真字体目检：DAQ 8 通道标签在上/下拉在下两列、Device 无标签 combo 拉伸 Refresh 靠右、Test 标签在左 combo 拉伸 Pulse Test 靠右、`Dev1/port0/line0..12` 完整显示、Z-Scan/Recon 未被裁、SIM 列收窄且与 Summary 列无重叠；**额外收益**：列收窄后 Recon 参数行右侧列间距随之变小、更紧凑。**审查**：低风险纯 GUI，按 AGENTS.md 走 **codex 双循环**（同会话 `--fresh`→`--resume`）——**第一循环（方案审查）2 轮 0 blocking**（轮1 1 blocking=Z-Scan 宽度护栏 line251 未纳入→已纳入；**codex 沙箱 offscreen 把 grp_zscan.minimumSizeHint 测成 ~536 假值，经核 offscreen 陷阱+真实字体 411 绿/panel=360 证明真实值 348，已澄清**）；**第二循环（实现验证）1 轮 0 blocking 通过**；非高风险按去重规则免独立 subagent review。codex NB（已落实）：本条记录最终采用宽度+真实字体实测 minHint，避免后续用 offscreen 假值误判。**既有测试隔离问题（非本轮引入）**：全量 `discover` 时既有测试写脏 `config/sim_control_config.json` 与 `control_wangbo/lastConfiguration.json`，已从基线 b0b0e14 恢复；本轮不碰它们。真机/目检待确认：目标 DPI/字体下 DAQ label-on-top 观感、两列线位名完整显示、SIM 列收窄后与 Summary 列无重叠、Z-Scan/Recon 不被裁、垂直不溢出。
- 2026-06-26：按用户要求修正 `SIM Camera Settings` 的 `Image Size` 下拉行为：用户选择非全幅 ROI 时，`ROI_X`/`ROI_Y` 会立即重置为当前完整视场的中心原点；运行时相机完整视场为 2048×2048 时，`1024×1024` 自动置为 `(512,512)`，`512×512` 自动置为 `(768,768)`。实现：`sim_control/sim_camera_presets.py` 新增纯函数 `centered_sim_camera_roi_origin()`（full frame 返回 `(0,0)`，非全幅按当前 `sensor_size` 与 4px 步进对齐居中）；`control_wangbo/main.py::on_sim_camera_size_activated()` 在用户激活尺寸下拉时先更新 `sim_app_config.camera` 的宽高与中心原点并同步 ROI spinbox，再沿用既有 `on_sim_camera_setting_changed()` 刷新预览/SLM RO/摘要；未修改 `.ui` 或生成的 `CellSorting_ui.py`，加载配置、硬件 applied ROI 回写、用户手动编辑 ROI 仍走原归一化逻辑。测试按 TDD 先红后绿：新增/更新 `tests/test_sim_camera_presets.py` 与 `tests/test_sim_preview_restart.py`；验证 `.\.venv\Scripts\python.exe -m pytest tests\test_sim_camera_presets.py tests\test_sim_preview_restart.py -q` 通过（76 passed, 11 subtests），`.\.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（411 tests OK）。独立 subagent review 0 blocking，确认本轮未新增实时采集 I/O、硬件控制或 DAQ/SLM 时序风险；仅提示 dirty worktree 中既有 `.ui`/生成文件改动不属本轮审查范围。真机/人工待确认：真实 Hamamatsu 返回 2048×2048 能力后，GUI 下拉切到 1024/512 时中心 ROI 显示与实际预览视野一致；若现场传感器尺寸或 ROI 步进不同，确认向下对齐的居中策略符合操作预期。
- 2026-06-26：接上一条迁入工作，按用户对新模块的**纯布局微调（4 项，不动任何硬件/采集/配置逻辑）**。①**SIM Runtime 移到 SIM Configuration 列最顶**：`verticalLayout_simConfiguration` 顺序由 SLM→Z-Scan→DAQ→Recon→**Runtime**→spacer 改为 **Runtime→SLM→Z-Scan→DAQ→Recon→spacer**（`.ui` 内 `grp_simRuntime` item 块移到首位）。②**字体放大到与 SIM Camera Settings 一致**：Z-Scan/DAQ/Recon 三模块所有标签/复选框/按钮→**10pt**（同 `label_69` "ROI_X"）、所有 `cmb_*`/`spb_*`/`edit_*` 控件→**12pt**（同 `spb_sCMOS_ROI_X`），共 52 控件；并把 Z-Scan 窄控件加宽避免 12pt 下裁切（`cmb_direction` 54→80、`spb_step_nm` 72→90、`spb_num_steps` 54→80、`cmb_exposure` 54→80；codex 实测 12pt minSizeHint=76>原 54）。③**DAQ 两列下拉均衡 + 完整显示线位名**：8 个 `cmb_main_daq_*` minWidth 84→**120**；`main.py` `gridLayout_daq` setColumnStretch 由 `col1=1,col3=0` 改为 **`[0,1,0,1]`**（左右两线位下拉列等分、等宽，解决左宽留白/右窄截断）；容器/窗口加宽：顶层窗口 2160→**2285**、`grp_simConfiguration` 340→**455**、内部 `layoutWidget_simConfiguration` 328→**443**、`grp_simSummary` x 1840→**1965**（宽 300 不变，右边界 2265<2285）。④**Recon**：`lbl_main_recon_wiener` "Wiener"→**"Wiener Para"**；`spb_main_recon_wiener/_na/_pixel_nm`、`cmb_main_recon_wavelength` 加 min/max 宽 **90** 收窄；`main.py` `gridLayout_recon` setColumnStretch col3 1→**0**（col1 保 1 供 OTF/Background/Output 的 `edit_*` 跨 col1-2 拉伸；参数列不再被拉宽）。流程：改 `control_wangbo/CellSorting_ui.ui`→`pyuic5` 5.15.11 绝对路径重生成 `CellSorting_ui.py`（before/after diff 仅含 resize 2285、grp_simRuntime addWidget 上移、52 处 setPointSize、8 处 84→120、4 处 recon 90、Z-Scan 4 控件加宽、3 处几何、Wiener Para，零漂移）；setColumnStretch 仍 `main.py` 运行时补。测试：`test_main_window_zscan_module`（顺序断言 idx_runtime=0/idx_zscan=2、字体 8→10/btn 9→10、宽度 80/90/80/80、控件 12pt、模块 docstring 更新）、`test_main_window_daq_module`（idx_daq 2→3、新增 `test_layout_widths_fonts_columnstretch_and_geometry` 断言 8 下拉 minWidth 120/字体 10·12/columnStretch [0,1,0,1]/几何 455·443·1965）、`test_main_window_recon_module`（新增 `test_layout_widths_fonts_and_wiener_para_label` 断言 Wiener Para/4 控件 maxWidth 90/columnStretch(1)=1·(3)=0/edit_otf>browse/字体 10·12）、`test_main_window_scroll_area`（content 宽 2160→2285）。验证：`unittest discover -s tests -q` **409 OK**（真实 Windows 字体，非 offscreen）；`QWidget.grab()` 真实字体目检 4 项全部确认（Runtime 在顶、字体增大统一、DAQ 两列均衡且 `Dev1/port0/line0..12` 完整显示无留白、Recon Wiener Para+spinbox 收窄）。**审查**：低风险纯 GUI（不碰相机/SLM/DAQ 控制·波形·时序·安全/破坏性），但用户本轮要求走 codex 双循环——**codex 第一循环（方案审查）2 轮 0 blocking、第二循环（实现验证）2 轮**（轮1 2 blocking[DAQ/几何测试覆盖未补全、Z-Scan 控件未随 12pt 加宽]→修复→轮2 **0 blocking 通过**，同会话 `--fresh`→`--resume`，无降级）；按去重规则非高风险免独立 subagent review。codex non-blocking：三测试文件 docstring 旧顺序已随手修。**已知外观点（非 bug）**：Recon 参数行左侧 spinbox 与右侧 "Illum NA/Ex(nm)" 标签间有列间距——因 col1 需容纳下方宽路径框而参数限宽 90 不填满该列的结构性副作用；满足用户"收窄参数"诉求，若需进一步贴紧可后续把右列参数左移+路径框跨满列重构。**测试隔离既有问题（非本轮引入）**：全量 `discover` 时某既有测试（实例化主窗口/相机流程、未用临时 config_path）会写脏 `config/sim_control_config.json`（roi 2304→2048、selected_running_order 置值），本轮已从基线恢复；我的新模块测试均用临时 config_path 隔离。真机待确认：真实主 GUI 目标 DPI/字体下 SIM 列各模块字体观感、DAQ 两列线位名完整显示、Recon 参数收窄+列间距可接受、2285 宽窗口在屏内（超出由 QScrollArea 滚动）。
- 2026-06-26：按用户要求**把 SIM 采集设置弹窗 `SimSettingsDialog` 仅剩的 DAQ 页与 Recon 页功能全部迁入主 GUI（`control_wangbo` 的 CellSorting 主窗口）作为一级模块，并整体移除该弹窗**；SIM 参数摘要栏 + Load/Save Config 按钮移到右侧新列、主窗口加宽。**布局**：`control_wangbo/CellSorting_ui.ui` 顶层窗口宽 1800→2160（高 1000 不变）；左侧 `grp_simConfiguration` 300→340、内部 `layoutWidget_simConfiguration` 288→328；删 `btn_openSimSettings`；`verticalLayout_simConfiguration` 顺序改为 SLM→Z-Scan→**DAQ(`grp_daq`)**→**Recon(`grp_recon`)**→SIM Runtime + 尾 spacer；新增 `grp_daq`（设备下拉+Refresh、8 路 TTL 线位 2 列 4 行、测试目标下拉+Pulse Test、状态 label，`*_main_daq_*` 前缀）与 `grp_recon`（wiener/NA/pixel/Ex 波长 + OTF/Background/Output 三路径+Browse，`*_main_recon_*` 前缀）；新增右侧 `grp_simSummary` `QRect(1840,40,300,930)` 承接 `pte_simSummary`+`btn_loadConfigureSettingsMain`+`btn_saveConfigureSettingsMain`（objectName 不变）。改 `.ui` 后 `pyuic5` 5.15.11 绝对路径重生成 `CellSorting_ui.py`（before/after 副本 diff 仅新增/删除目标控件、零漂移）；列伸缩 `setColumnStretch` .ui 不可表达、`main.py` 运行时补。**DAQ 测试逻辑抽到新模块 `sim_control/daq_testing.py`**：`DaqTestRunner`（注入与 controller 共享的 camera/slm/daq adapter + `AppConfig` 配置快照，原 `SimSettingsDialog._run_*_test` 方法体几乎原样迁入；契约：worker 内不读 Qt、各自 stop_event、**全 成功/异常/取消 路径 finally `set_all_low`**、`camera_externally_owned=True` 只 disarm 不 disconnect、SIM采集/激活时序选 RO 经 `exclude_indices=immediate_ro_indices` 排除 immediate RO——与正式采集 `controller.select_running_order_for_task` 语义一致）+ `_PulseTestWorker` + 结果 dataclass + 常量 + `build_daq_test_target_items` + `run_test` 分派；`sim_control/gui.py` 从该模块**重导出**这些符号，保 `SimControlWindow`（独立 SIM 调试 GUI，保留不动）与既有 `from sim_control.gui import ...` 调用点不破。**`main.py` 运行时接线（仿 Z-Scan 先例）**：`setup_sim_daq_module`/`_init_daq_module_from_config`/`on_main_daq_setting_changed`/设备线位刷新(`_shared_main_daq_adapter` 先 `ensure_sim_runtime` 用 controller 共享 daq_adapter，与 Z-Scan autofocus 一致)/`on_main_daq_test_clicked`(+`_on_main_daq_test_success/error/finished`)、`setup_sim_recon_module`/`_init`/`on_main_recon_setting_changed`/`_on_main_recon_wavelength_changed`(按波长存取 OTF、**不改采集波长 `selected_laser_nm`**)/`_browse_main_recon_*`；init 处加 `setup_sim_daq_module()`/`setup_sim_recon_module()`。**B1 DAQ 测试安全互锁**：`on_main_daq_test_clicked` 启动前 `stop_immediate_live_mode(reset_dropdown=False)`（诊断波形与 immediate-live 互斥，遵 2026-06-24 激光安全状态机）+ 检查 `sim_acquisition_in_progress` 互斥 + `stop_sim_preview(wait=True)` 暂停 preview 记 resume 标志，`_on_main_daq_test_finished` 成功/失败/取消全路径恢复 preview；后台 `_PulseTestWorker`+QThread、按钮切 Cancel、二次点击取消、强引用防 GC。**B6 Recon 跨线程（硬约束）**：`MainWindow` 新增 `signal_reconstruction_config_changed = pyqtSignal(object)`，`ensure_sim_reconstruction_worker` 改为 worker 已存在分支 `emit(reconstruction.snapshot())`、新建分支 connect 到 `slot_update_reconstruction_config`，三处下发（改值/Load/ensure）；**修掉了 `main.py` 原 worker 已存在时跨线程裸 `set_reconstruction_config` 的既有隐患**。**B2 删弹窗副作用映射**：删 `btn_openSimSettings.clicked.connect`/`btn_openSimSettings_function`/`apply_sim_settings`，其副作用映射到 DAQ/Recon/相机/SLM 既有 handler；修 `btn_sim9_acquire` 字体来源改用 `.ui` 静态字体（不再依赖已删按钮）。**B3 Save/Load**：Save(`persist_sim_app_config_from_ui`) + Load(`apply_loaded_sim_settings_payload` 合并 sim_control payload 后回填 DAQ/Recon/Z-Scan 三模块控件 + 同步 controller daq/z_scan + ensure recon worker，避免 Load 后配置与控件/后端分裂)。删 `sim_control/sim_settings_dialog.ui`、`ui_sim_settings_dialog.py`、`generate_ui_py.cmd`（弹窗专属死脚本）、`gui.py` 的 `SimSettingsDialog` 类（-769 行）与孤儿 recon helper（`read/write_reconstruction_config_from_widgets` 等 5 个无调用者函数）。**测试**：新增 `tests/test_main_window_daq_module.py`(10)/`test_main_window_recon_module.py`(9，覆盖 B1 互锁次序、B6 snapshot-非裸setter+queued signal、按波长 OTF 不改采集波长、638+saved-params 保留、Load 回填)；`test_sim_daq_testing.py` 改指 `DaqTestRunner`；`test_sim_preview_restart.py` 删 9 个 dialog 生命周期测试 + 重定向 12 个 DAQ 测试到 `DaqTestRunner`；`test_ui_regressions.py` 删 12 个 dialog UI 测试；小修 footer/zscan-index(2→1)/scroll-width(1800→2160)；**`test_efficiency_optimizations.py` 补 QApplication setUpClass 守卫**（它是套件唯一无 QApplication 的 Qt 测试类，全量 `discover` 下无父 `FeatureWorker` 的 C++ 对象被跨测试 GC 删除导致假失败——非生产代码缺陷，由本次测试集合变化暴露）。**测试隔离修复**：新模块测试 host 的 `config_path` 指向临时文件，避免改值触发的 `save_app_config` 写脏真实 `config/sim_control_config.json`（曾被冒烟/单测写过、已从基线恢复）。**重要约定**：含 Qt `minimumSizeHint` 的布局测试必须用**真实 Windows 字体**跑（不加 `QT_QPA_PLATFORM=offscreen`）——offscreen 回退字体度量被放大会让此类断言假失败。验证：`.venv\Scripts\python.exe -m unittest discover -s tests -q` **407 OK**（真实字体）。**审查**：高风险（DAQ/SLM/相机/波形/时序 + 删弹窗 + 跨线程 recon）走 **Codex 双循环 + 独立 subagent review 双审**——codex 方案审查 1 轮 0 blocking、实现验证 2 轮（轮1 2 blocking[Load 未回填 DAQ/Recon、`_run_laser_pulse_test` 缺 runner 层 `set_all_low`]→修复→轮2 **0 blocking 通过**；中途一次 codex 复审进程工具层挂起，经用户确认后以 `--resume --wait` 重试取得 0-blocking 结论）；独立 subagent review（4 子代理分区深审）**0 blocking**。真机待确认：①DAQ 测试取消/异常后 NI USB-6423 全低、R11 不重复打开、共享相机交还后 preview 恢复；②`immediate_ro_indices` 须在 SLM 扫描后才填充（测试入口经 `ensure_sim_runtime`→connect/scan 应先于测试）；③SLM 1s 激光脉冲/SIM9/激活时序真机时序；④2160 宽下新 DAQ/Recon 模块 + 右侧 SIM 摘要列真实字体目检。
- 2026-06-25：**主 GUI 控件静态化 阶段 2 完成——SLM 连接区 2×2**（接下方阶段 1 条目）。把 SLM 2×2 从“运行时重排 .ui 既有控件 + 运行时建 `cmb_SLM_immediateRO`”改为“.ui 静态 2×2”。①`CellSorting_ui.ui` `gridLayout_SLMConnection` 静态 2×2：`cmb_SLM_device`(0,0)110、`btn_SLM_connection`(0,1)154、`btn_SLM_refresh`(1,0)90、**新增** `cmb_SLM_immediateRO`(1,1)154×25、margins(6,4,6,4)/hspacing6/vspacing4 静态；`lbl_SLM_status` 从 grid (1,1) 移到 **layout 外**作 `grp_hardvareConnection_SLM` 直接子(geometry(0,0,10,10)/visible=false，使 `grid.indexOf==-1`)；重生成确认 immediateRO 走 addWidget(1,1)、status 走 setGeometry(不 addWidget)。②`main.py` SLM 段删 removeWidget/addWidget 重排 + 控件宽度 + immediateRO 创建/font/fixedSize + margins/spacing（.ui 已定），只保留运行时补 `setColumnMinimumWidth(0,110)/(1,154)/(2,0)`+`setColumnStretch(0,0)/(1,0)/(2,1)`（pyuic5 不生成列属性）、`combo_ro.view().setMinimumWidth/setMaximumWidth(190)/ElideRight`（QComboBox.view 运行时对象、.ui 无法表达）、tooltip、`_reset_immediate_ro_dropdown()`、信号；删 `ro_font` + 因此 unused 的 `_build` 内 `from PyQt5 import QtGui`，`_build` docstring 更新为“三控件全静态、仅接线”。③测试 `test_runtime_slm_immediate_ro_layout` **改名** `test_static_slm_2x2_layout_and_wiring`：新增 setupUi 后静态结构断言（immediateRO 在 (1,1)、status `grid.indexOf==-1`，防运行时重排回退漏检）；**B-1 护栏删 `grp.minimumSizeHint<=288`**（含 QGroupBox 装饰开销、跨机字体度量波动 284/299/468、作硬阈值不可靠），改保留 `grid.minimumSize<=288`(282) 真护栏。验证：footer+zscan 22/22；**全量 `unittest discover` 409 OK（0 失败）——顺带修复阶段1 遗留预存失败**（原 test_runtime_slm 299>288）。审查：**Codex 双循环全过**（第一循环 重生成步骤 blocking→清零；第二循环 0 blocking，同会话 `--fresh`→`--resume`、无降级）；非高风险（纯 GUI）按去重规则免 subagent review。**至此主 GUI 控件静态化全部完成**（btn_sim9 + 阶段1 波长/Z-Scan + 阶段2 SLM）。真机待确认：目标 DPI 目视 SLM 110/154/90/154 宽度可接受。**并发提示**：本会话做 SLM 静态化期间，另一会话并发做了 Z-Scan 文案英文化（见上一条）；两者改 `.ui` **不同部分**（SLM 2×2 vs Z-Scan 文案），当前文件已合并、全量 409 OK；后续提交需协调两份未提交改动。
- 2026-06-25：按用户要求将主 GUI Z-Scan 模块全部用户可见中文文案英文化（Literal 用词）：`Enable Z-Scan`、`Direction`、`Step (nm)`、`Steps`、`Exposure (ms)`、`Select Focus Plane`、`Test Z-Scan`、`Cancel`，并同步运行时状态标签、弹窗提示和 tooltip；`Z-Scan` 标题保持不变。按主 GUI 静态控件规则修改 `control_wangbo/CellSorting_ui.ui` 后用 `pyuic5` 重生成 `control_wangbo/CellSorting_ui.py`，`control_wangbo/main.py` 仅替换用户可见字符串，未改 objectName、itemData、配置写回、ZDrive/相机/SLM/DAQ 控制路径或 SIM9 联动逻辑。因英文 `Exposure (ms)` 较长，四个参数标签字体降到 8pt；为避免 `Test Z-Scan` 在 88px 按钮内截断，按钮调整为 100px/9pt；最终 `grp_zscan.minimumSizeHint().width()` 实测 287px≤288px。验证：`.venv\Scripts\python.exe -m pytest tests\test_main_window_zscan_module.py -q` 15 项通过；`.venv\Scripts\python.exe -m pytest tests\test_main_window_zscan_module.py tests\test_main_window_sim_footer.py -q` 22 项通过；`.venv\Scripts\python.exe -m unittest discover -s tests -q` 409 项通过；`git diff --check -- control_wangbo\CellSorting_ui.ui control_wangbo\CellSorting_ui.py control_wangbo\main.py tests\test_main_window_zscan_module.py PROJECT_MEMORY.md` 通过，仅有 CRLF/LF 规范化提示。审查：本次为低风险 GUI 文案调整，豁免 codex 双循环；最终独立 subagent review 0 blocking，确认 `.ui`/生成文件/运行时提示/测试覆盖均满足，未发现硬件路径或配置行为变化。人工待确认：真实 Windows DPI/字体下 `Select Focus Plane`、`Exposure (ms)`、`Test Z-Scan` 等英文长文本不溢出且观感可接受。
- 2026-06-25：按用户要求**把主 GUI 动态创建控件改为 `.ui` 静态组件（功能/位置不变），并确立长期规则**（以后主 GUI 加控件都用静态组件）——分阶段、删动态后备分支纯静态。**阶段 1（本条）静态化波长下拉 + Z-Scan 模块**。现状澄清：`grp_simRuntime`+LED+状态标签、`lbl_z_position_label/value` 早已 `.ui` 静态（运行时走静态分支/hasattr 跳过、动态分支是死代码）；真正纯动态的是波长下拉、Z-Scan 模块、`cmb_SLM_immediateRO`（阶段2）。改动：①`CellSorting_ui.ui` 在 `gridLayout_32 (2,1)/(3,1)` 加 `lb_sCMOS_laser`/`cmb_sCMOS_laser`，在 `verticalLayout_simConfiguration`（SLM 后 Runtime 前）加 `grp_zscan`+`gridLayout_zscan`+全部控件（chk_enabled/4标签/cmb_direction/spb_step_nm[decimals0/range1-100000/step10]/spb_num_steps[range1-1000]/cmb_exposure/HBox[chk_capture+btn_run+spacer]/lbl_status；固定宽54/72/54/54/88、TNR9/10、tooltip/wordWrap）；`pyuic5` 5.15.11 绝对路径重生成（before/after diff：波长 +17 行、Z-Scan 仅新增控件、零漂移）。②`main.py`：波长下拉/Z-Scan 删动态创建改对静态控件接线——`blockSignals+clear` 填带 `itemData` 的下拉项、保留信号/配置同步/worker 变量/`_init_zscan_module_from_config`；`setup_sim_zscan_module` 由“创建+接线”改“静态存在则接线”（前置 `grp_zscan` 检查 + `_zscan_module_wired` 接线后置位防重复 connect）；**`columnStretch` 不能 `.ui` 静态表达**——pyuic5 5.15.11 把 `<string>0,0,0,0,1</string>` 误生成非法 `setColumnStretch(_translate(...))` 致 `retranslateUi` 崩溃，故 `.ui` 删该 property、`main.py` 运行时补 `setColumnStretch(0-3=0,4=1)`。③清理死分支：`setup_sim_runtime_status_widgets` 删动态创建分支纯复用静态 LED/标签；`setup_sim_z_position_widgets` 简化为空方法（`lbl_z_position` 已静态，几何 122/142,486+对齐+文本与原动态一致）；`create_sim_runtime_led` 因此 unused（暂保留工具函数）。④测试：`test_sim_camera_settings_host_resolvable` 的 (2,1)/(3,1) 空闲断言改为已被波长控件占用；新增 `test_static_laser_dropdown_populated_and_single_callback`（count 4/itemData/默认 488/blockSignals 期无回调/单次切换一次回调）；`test_main_window_zscan_module` 15 项未改、全绿。验证：`test_main_window_zscan_module` **15/15**（含 ≤288、幂等、SLM<Z-Scan<Runtime 顺序、配置往返）；全量 `unittest discover` **409 仅 1 预存失败**（即下方无关的 `test_runtime_slm_immediate_ro_layout` SLM 2×2 WIP）。审查：**Codex 双循环**（第一循环 2 轮：3 blocking[重生成步骤显式/Z-Scan 幂等/spinbox 属性不全]→清零；第二循环 1 轮：唯一 blocking=本记忆/规则未落地→本次落地后清零，同会话 `--fresh`→`--resume`，无降级）；规则写入 `AGENTS.md`「关键约束」+ `decision_log` 决策。**取代**本文件此前多条“纯 `control_wangbo/main.py` 运行期、不改 `.ui`/`CellSorting_ui.py`”做法（方向转变）。阶段 2 待做：SLM 连接区 2×2 静态化。真机待确认：真实主 GUI 目视波长下拉、Z-Scan 模块位置/外观/交互与改前一致、Z-Scan ≤288 高 DPI 不溢出。
- 2026-06-25：调整「Codex 双循环」与「修改后审查机制」关系——由**无条件叠加双审**改为**有条件去重**：codex 完成**第二循环（实现验证）并通过**后，**非高风险**改动免独立 subagent review；**高风险**改动（硬件相机/SLM/DAQ、波形/时序、安全/破坏性）即使 codex 通过**仍做一次** subagent review 双保险（用户经 AskUserQuestion 确认）。**兜底硬约束**：简单/文档记忆类/codex 降级/仅第一循环 等无 codex 实现审查的情形仍必做 subagent review，任何实质改动至少经一种实现审查、**绝不两者都跳过**。改 `AGENTS.md`「修改后审查机制」（+去重/兜底两条）、codex 节首互补条（L86）、「判定终止降级」条（L120）+ `docs/project_memory/decision_log.md`（新增 2026-06-25 去重决策并覆盖分级决策旧句“subagent review 仍对每次改动执行”）+ auto-memory。本次属文档/记忆类、豁免 codex 双循环，已走独立 subagent review。仅改 `AGENTS.md`、`docs/project_memory/decision_log.md`、`PROJECT_MEMORY.md` 与 auto-memory。
- 2026-06-25：修复**缩小主 GUI 窗口时「SIM9帧采集」按钮消失**。根因：该按钮原在 `control_wangbo/main.py::_build_sim_immediate_and_acquire_controls` 运行时编程创建、**父对象错设为 `self`(主窗口)** 并用绝对坐标 `setGeometry(1290,158,160,36)` 定位；而整个历史界面被 `setup_scrollable_cellsorting_ui()`(main.py:84) 包进 `QScrollArea`(内容容器 `cellsorting_content_widget` 固定 1800×1000、`setWidgetResizable(False)`)。按钮浮在滚动区之上、不属于滚动内容，窗口宽 <~1290px 时 x 超出主窗口右边界被裁剪消失（同函数内波长下拉、RO 下拉、Z-Scan 模块父对象都在内容容器内、不消失）。**用户选定改成静态组件**：①`CellSorting_ui.ui` 顶层 `Single_Cell_Sorting` 下、`grp_rinseChannel` 前新增 `QPushButton btn_sim9_acquire`(geometry 1290,158,160,36 / Times New Roman 12 / 文本「SIM9帧采集」)；②`pyuic5` 5.15.11 **绝对路径**重生成 `CellSorting_ui.py`(before/after 副本 diff 确认仅新增按钮 setupUi 7 行+retranslateUi 1 行、零漂移；父=`Single_Cell_Sorting`=内容容器)；③`main.py` 删除该按钮动态创建块、改为对静态按钮接线(`getattr` 取按钮→沿用 `btn_openSimSettings` 字体→`clicked`→`trigger_sim_raw_9frame_acquisition("sim9_button")`)，更新 docstring，删除因此 unused 的 `QRect` import；④`tests/test_main_window_sim_footer.py` 原 `test_runtime_sim9_button_...` 改名 `test_static_sim9_button_geometry_settings_font_and_wiring`(接线前先断言 setupUi 已静态建按钮+几何)、新增 `test_static_sim9_button_parented_into_scroll_content`(经 `setup_scrollable_cellsorting_ui` 包装断言 `btn_sim9_acquire.parentWidget() is content_widget`，锁定原 bug 回归)。**项目结构陷阱(易再犯)**：主界面经 `setup_scrollable_cellsorting_ui` 把固定 1800×1000 历史 UI 包进 `QScrollArea`，今后任何新增控件必须以 `cellsorting_content_widget`(或其内子控件)为父、或写进 `.ui` 由 `setupUi` 创建——**绝不能以 `self`(主窗口)为父+绝对坐标**，否则缩小窗口必被裁剪消失。验证：`unittest tests.test_main_window_sim_footer tests.test_main_window_scroll_area` 7 测试 6 通过(含 2 个 SIM9 + 滚动区)，唯 1 失败 `test_runtime_slm_immediate_ro_layout` 是**与本次无关的预存失败**(SLM 2×2 布局 WIP；`git show HEAD` 既无该测试也无 SLM 2×2 代码 count=0；本机 299>288 / codex 复跑 468>288、字体度量敏感；同下方 Z-Scan 条目记载)。**审查**：跨 `.ui`/重生成 `.py`/`main.py`/测试多文件且触及 `control_wangbo`，走 **Codex 双循环**(第一循环方案审查 1 轮 0 blocking、第二循环实现验证 1 轮 0 blocking，同一 codex 会话 `--fresh`→`--resume`，无降级)+ **独立 subagent review**(`code-documentation` reviewer，0 blocking，7 项标准附证据)。提交建议：本次 SIM9 静态化单独成 commit(只含 `.ui`/生成 `.py`/`main.py` SIM9 接线段/两支 SIM9 测试)，不与未提交 SLM 2×2 WIP 混提。真机待确认：①真实主界面把窗口宽缩到 <1290px，「SIM9帧采集」按钮经水平滚动条可到达、不再消失；②正常尺寸下按钮位置/字体与改前一致、点击仍触发 9 帧采集、与 `grp_simConfiguration`(x=1480) 间 30px 不重叠。
- 2026-06-25：按用户要求把主 GUI **Z-Scan 模块布局从「标签在左」改为「标签在上、控件在下」**（参照 SIM Camera Settings 的 `gridLayout_32` label-on-top 模式），控件窄、仅显示数字、整体更紧凑美观。**纯 UI 栅格排布**，不动运行/采集/配置逻辑。`control_wangbo/main.py::setup_sim_zscan_module`：row0 `开启Z-Scan` 开关跨全宽；row1 四标签(方向/步进(nm)/步数/曝光(ms)) 占 col0..3；row2 四控件(direction/step/num/exposure) 同列对应；row3 嵌套 HBox[选择焦面 + 测试Z-Scan] 跨全宽；row4 状态标签跨全宽；`setColumnStretch(4,1)` 末列吸收余量使窄控件左对齐；`setContentsMargins(4,4,4,4)`+`setHorizontalSpacing(2)`（从 6/4 收紧，抵消「(nm)/(ms)」中文括号标签较宽带来的列宽，使 `grp_zscan.minimumSizeHint().width()=286 ≤ 288` 不撑宽固定面板）；删除原 `_add_label` 改用 `_make_label`+循环。控件窄宽度(direction 54/step 72/num 54/exposure 54/button 88)、文案(选择焦面/测试Z-Scan)、曝光裸数字(5/8/14/20)、步进无后缀均沿用（此前已就绪）。`tests/test_main_window_zscan_module.py` 行断言更新（direction/step/num/exposure→row2、status→row4、chk_enabled 仍 row0）并新增「每控件正上方一格是其文字标签」的 label-on-top 校验；宽度/文案断言不变。验证：`tests.test_main_window_zscan_module` **15/15 通过**，`grp_zscan` 实测 286≤288。审查：纯 UI 低风险、单文件(+测试)→按 AGENTS.md 分级**豁免 codex 双循环、仅走独立 subagent review**（APPROVE，0 blocking；non-blocking：标签 9pt vs 相机 10pt 可选统一）。真机待确认：①本机 286/288 余量仅 2px，高 DPI/不同系统字体下中文「步进(nm)/曝光(ms)」标签变宽可能逼近 288，需目标显示器目视无横向溢出；②整体美观目视。**注意（与本任务无关的预存失败）**：全量 `unittest discover` 余 1 失败 `test_main_window_sim_footer::test_runtime_slm_immediate_ro_layout`（`grp_hardvareConnection_SLM.minimumSizeHint()=299>288`）——该测试只调 `_build_sim_immediate_and_acquire_controls`（SLM 区，本任务未碰），失败源于工作区既有未提交的 SLM 找样品 RO 下拉 `view().setMinimumWidth(190)` 等 WIP 改动撑宽 SLM 组，与本次 Z-Scan 布局无关，需另行处理（回退 popup 宽或调整该测试阈值）。
- 2026-06-25：按用户要求把主 GUI「SIM9帧采集」(raw-only) 保存的 .tif **文件名规则**改为 `采集波长_mask直径_曝光_图像大小_时间戳.tif`（原 `sim_9frames_<task_id>.tif`）。仅改 `sim_control/pipeline.py`：`RawStackSaveWorker.slot_save` 在 (9,H,W)+uint16 校验**之后**改用 `output_path = _unique_output_path(output_dir, _build_raw_stack_filename(batch, stack))`；新增 3 个纯函数——`_build_raw_stack_filename(batch, stack)` 拼 `{波长}_{pitch}_{曝光}_{W}x{H}_{时间戳}.tif`（波长=`batch.laser_wavelength_nm`；pitch 从 `metadata['running_order_name']` 解析 RO 名第 2 段、缺失回退 `Path(pattern_files[0]).name` 再解析、否则 `'NA'`；曝光=`f"{batch.exposure_us/1000:g}ms"`、≤0→`'NAms'`；尺寸=`shape[2]x shape[1]`(WxH)；时间戳=`datetime.now().strftime("%Y%m%d%H%M%S")`）、`_parse_pitch_token`（split `'_'` 后要求**首段 `\d+` 波长**且第 2 段 `\d+(\.\d+)?`，否则 `'NA'`）、`_unique_output_path`（同名已存在追加 `_2/_3…`，防止同秒碰撞静默覆盖原始数据）。输出目录 `data/sim_9frames`、`tifffile.imwrite`、signal、完成弹窗均不变。例：`488_3.5_500ms_2048x2048_<14位时间戳>.tif`、`638_3.5_1ms_2048x2048_…tif`。`tests/test_efficiency_optimizations.py`：更新 `test_raw_stack_save_worker_writes...`（batch 补 `metadata["running_order_name"]`、对 emit 路径 basename 正则断言新名）+ 新增 3 测试（命名规则、pattern_files 回退含下划线目录、parser 负例 `lus_3.5_x→NA`、`_unique_output_path` 碰撞 `_2/_3`）。**审查**：Codex 双循环——第一循环 2 轮（1 blocking=同秒撞名静默覆盖→加 `_unique_output_path` 清零）；第二循环（1 blocking=`_parse_pitch_token` 未校验首段波长→加 `re.fullmatch(\d+, parts[0])`+负例测试清零；原 codex 会话因后台超时丢失，末轮以 `--fresh` 复审实现 **0 blocking**）；独立 subagent review **0 blocking**。验证：`unittest tests.test_efficiency_optimizations` **26 通过**。**待用户确认**：「曝光时间」用相机真实 `exposure_us`（当前实现）——非整档曝光（如 35ms）会与 RO 名义档 1/10/50ms 不同；若要 RO 名义档需改。时间戳取保存时刻（与采集结束差秒级）。**注意（与本任务无关）**：本会话期间全量 `unittest discover` 出现 ~52 个 `Ui_SimSettingsDialog` 缺 `check_zscan_enabled`/`tab_zscan` 的 error + `test_sim_settings_ui_removes_camera_tab_and_timing_controls`（`['DAQ','Recon'] != ['DAQ','Z-Scan','Recon']`）失败——系并发 Z-Scan 迁移把 `ui_sim_settings_dialog.py` 重生成为「无 Z-Scan tab」但工作区内 `test_sim_preview_restart.py`/`test_ui_regressions.py` 仍是旧 Z-Scan 期望（新 UI + 旧测试不一致）；与本文件名改动无关，需该并发工作把对应 Z-Scan 测试同步到位（参见下方两条 Z-Scan 条目）。本任务全程未触碰 `ui_sim_settings_dialog.py`/`gui.py`/Z-Scan 测试。
- 2026-06-25：按用户要求压缩主 GUI 的 Z-Scan 模块布局（仅运行期创建控件，不改 `.ui`/`CellSorting_ui.py`）：`control_wangbo/main.py::setup_sim_zscan_module()` 从 7 行改为 4 行，第一行放 `开启 Z-Scan` 开关与方向下拉，第二行放 `步进(nm)` 与 `步数`，第三行放 `曝光(ms)`、`选择焦面` 开关与 `测试Z-Scan` 按钮，第四行保留状态标签；步进 spinbox 不再显示 `nm` suffix，曝光下拉显示 `5/8/14/20` 且 itemData 仍为毫秒整数；控件宽度收窄并用跨列 HBox 防止 `选择焦面` 撑宽固定 288px 面板。同步更新 `tests/test_main_window_zscan_module.py`，新增第一行位置、无单位显示、新文案和紧凑宽度断言；未改 `ZScanConfig`、ZDrive/相机/SLM/DAQ 控制路径、SIM9 采集联动或配置 schema。验证：`.venv\Scripts\python.exe -m pytest tests\test_main_window_zscan_module.py tests\test_main_window_sim_footer.py -q` 20 项通过；`.venv\Scripts\python.exe -m unittest discover -s tests -q` 407 项通过。审查：本次为低风险主 GUI 文案/布局微调，豁免 codex 双循环；独立 subagent review 已完成，0 blocking，review 仅提出记忆文件状态需回写，本条已处理。真机/人工待确认：真实 Windows DPI/字体下 Z-Scan 模块是否符合操作者视觉预期且不溢出。
- 2026-06-25：按用户要求**把 Z-Scan 主功能搬到主 GUI 一级界面**（SIM 配置面板「Hardvare Connection (SLM)」与「SIM Runtime」之间），并**整页移除 SIM 设置对话框的 Z-Scan 页**（**这也补完了上一条「字体」更新里提到的工作区未提交 z-scan 重构半成品**：`tab_zscan`/`read_z_scan_config_from_widgets`/`Z_SCAN_TEST_*` 等已缺失符号现已随对话框页移除被彻底清理、相关测试改/删完毕，全量测试转绿）。三子任务：(A) `sim_control/z_scan_core.py` 新增可复用纯函数——`preflight_z_scan_positions`（连 stage + 全量越界预检，发起任何移动前 raise；HardwareError/ValueError 分型）、`run_z_scan_stage_only`（仅移动位移台、不回起点停终点、每步移动前取消检查、emit `z_scan_stage_positioned`）、`run_z_scan_autofocus`（preflight + **camera/SLM `is_connected()` 方法校验**[stage 的 `is_connected` 是布尔属性、camera/SLM 是方法，二者不对称]+函数体内局部 import `find_z_scan_running_order` 选 z-scan RO + 转调 `run_z_scan`，run_z_scan 内部已移到最佳焦面）。(B) 移除对话框 Z-Scan 页：`sim_settings_dialog.ui` 删 `tab_zscan` 并 `pyuic5` 重生成 `ui_sim_settings_dialog.py`（仅余 DAQ/Recon 两页）；`gui.py` 删 SimSettingsDialog 全部 z-scan 代码（控件别名/信号/方法群~600 行/模块级转换函数），`_sync_config_from_widgets` **不再覆盖 `config.z_scan`**（保存时原样保留，z_scan 改由主 GUI 编辑），`__init__` 保留 `stage_adapter` 形参但不再使用（兼容 main.py 调用）；独立 app `SimControlWindow` 仅 `controller.z_scan_config=config.z_scan` 不受影响。(C) `control_wangbo/main.py` 运行期编程式新增 Z-Scan 模块（用户授权改 SIM 控件、不改 `CellSorting_ui.py`）：`setup_sim_zscan_module`（QGroupBox+QGridLayout 建 7 控件[方向下拉/步进 nm/步数/曝光预设/「是否开启 Z-Scan」开关/「拍图」开关/「运行 Z-Scan」按钮]+状态标签，`insertWidget(2,...)` 得 SLM<Z-Scan<Runtime；字段 `setMaximumWidth(120)`+按钮/拍图跨列嵌套 HBox 使 `grp_zscan.minimumSizeHint().width()≤288` 不撑宽固定面板)，配置双向同步（`_init_zscan_module_from_config` blockSignals 防回环、direction/exposure 用 itemData/findData、step nm=µm×1000；`on_main_zscan_setting_changed` `_loading_configure_settings` 守卫→写 `sim_app_config.z_scan`→`save_app_config`→同步 `controller.z_scan_config`→刷新摘要；`apply_sim_settings` 用 getattr 守卫刷新模块控件），后台执行（`signal_zscan_status` pyqtSignal 跨线程回主线程、`_ConnectWorker`+QThread 仿 SLM 连接范式、强引用防 GC、finished 里 quit/wait+清引用+复位+`poll_sim_stage_position(force=True)`；`_run_zscan_blocking` 无 Qt 薄方法承载分派[拍图 ON→autofocus、OFF→stage_only]便于单测；capture 模式在 GUI 线程预快照 z_scan/daq/camera/timing 避免运行中改动竞态、并先 `stop_sim_preview(wait=True)`+要求 SLM/相机已连接)。**行为**：「运行 Z-Scan」拍图开关 OFF→仅移动位移台（按方向/步进/步数，停最后一步终点）、ON→完整 Z-Scan（移动+采图+SML对焦+移到最佳焦面）；运行中按钮变「取消」（取消只在单次移动之间生效，已发出的 move_z_um 不可中断）。**「SIM9帧采集」按钮接「是否开启 Z-Scan」开关**：`trigger_sim_formal_acquisition` raw_only 块 `z_scan_enabled` 由硬编码 `False` 改为 `bool(app_config_snapshot.z_scan.enabled)`（ON→采 9 帧前先做完整 Z-Scan 自动对焦选焦面，由现有 controller worker/acquisition_core 链路自动选 z-scan RO→run_z_scan→恢复正式 RO；OFF→直接采 9 帧）；与 Z-Scan 运行双向互斥、`poll_sim_stage_position` 运行期让路。测试：新增 `tests/test_main_window_zscan_module.py`（15 用例）；`tests/test_z_scan_core.py` 新增 15 用例（stage_only 10 + autofocus 5；fake camera/slm 的 is_connected 做方法、stage 做属性匹配真实 API）；`tests/test_ui_regressions.py` 改 tab→`["DAQ","Recon"]`+断言 z-scan 控件不存在+删 4 个对话框 z-scan 布局测试+daq/zscan combo 测试改仅 daq；`tests/test_sim_preview_restart.py` 删 SimSettingsDialogTests 内 26 个对话框 z-scan 用例、SIM9 测试断言 `z_scan_enabled` False→True。验证：全量 `unittest discover -s tests` **407 通过**（修复了上一条遗留的 31 项 z-scan 预存失败）。审查：硬件控制改动走 **codex 双循环**（第一循环 3 轮 0 blocking：行号漂移/autofocus 缺相机前置检查/越界 preflight 不完整/`is_connected` 方法非属性；第二循环 1 轮 0 blocking，同一 codex 会话 `--fresh`→`--resume`，无降级）+ **独立 subagent review APPROVE**（0 blocking，11 项标准附证据）。**实现注记**：子任务 B 委托 subagent 中途工具调用解析失败、只完成一半，gui.py 方法体删除与全部测试清理由主 agent 手动补完。真机待确认：①Ti2 ZDrive 后台移动与 GUI 轮询经 adapter `_sdk_lock` 串行化不卡死、小步进/零位移不报错；②拍图模式完整链路（选 488 三相位 z-scan RO→对焦→恢复正式 SIM9 RO）+ SIM9 开关 ON 整链真机时序与 SML 焦面判据；③方向 positive/negative_z 物理上下、step µm 标定、量程越界保护；④高 DPI 下 Z-Scan 组在 288px 面板不溢出。
- 2026-06-25：按用户「字体再大一些」把 SIM9 完成弹窗字号从 **11pt 增大到 13pt**（接上一条弹窗外观条目；中文 `Microsoft YaHei`、英文/数字/路径 `Arial` 的 RichText 分段方案已由上一条实现，本轮只调字号）。`control_wangbo/main.py::_show_sim_raw_stack_saved_dialog()` 的局部变量 `font_size_pt` 是唯一真相源（同时驱动 `QFont` 中英文字号、HTML `font-size:{}pt` span、`_measure_sim_dialog_line_width` 逐字符宽度测量），改 `font_size_pt = 11`→`13` 即整体放大，且保存路径仍按 13pt 测量宽度、`setWordWrap(False)` 单行不折。`tests/test_sim_preview_restart.py::SimRawStackSavedDialogTests` 三个 helper 同步：`_assert_dialog_widths` 的 `QFont(...,11)`→13、`_assert_dialog_fonts` 四处 `pointSize()==11`→13、`_assert_message_label_html` 的 `font-size:11pt`→13pt。验证：`SimRawStackSavedDialogTests`(3) + `test_main_window_sim_footer`(5) 共 8 项通过。审查：纯字号单变量微调、低风险，**豁免 codex 双循环、仅走独立 subagent review**（0 blocking）。**重要（与本次无关的预存破损）**：本次 `discover -s tests` 全量出现 31 项失败（27 ERROR+4 FAIL），全部来自 `SimSettingsDialogTests`/`UiRegressionTests` 的 z-scan 用例，根因是工作区未提交的 `sim_control/gui.py`+`ui_sim_settings_dialog.py` z-scan 重构半成品（`read_z_scan_config_from_widgets`/`Z_SCAN_TEST_*`/`tab_zscan` 等符号定义已缺失但引用仍在），与字号改动无任何因果关系（subagent review 已逐项核验，`SimRawStackSavedDialogTests` 不在失败列表）；提交本次字号改动时应只 stage `control_wangbo/main.py` 与 `tests/test_sim_preview_restart.py`，z-scan 重构需另行补齐符号定义并重新 `pyuic5` 生成 UI 后才能转绿。真机待确认：13pt 是否符合用户「大一些」预期（偏小可继续上调单一 `font_size_pt`）、中英文混排观感。
- 2026-06-25：按用户要求把找样品 immediate Running Order 从仅 `488` 扩展为 `405/488/561/647` 四波长。`tools/add_preview_ro.py` 现在生成 40 个 RO（每波长 `f1..f9` + `3dir`），图片索引固定为 `405:0..8`、`488:9..17`、`561:18..26`、`647:27..35`，仍复用 `A+`/`A-` 的 `48030 500us 1-bit Lit Pair ±` 成对 DC 平衡，不新增 sequence/image，不启用 SPI RO Selection；已运行脚本更新 `patterns/2d_3.5.repz11`，内部确认 `image_count=36`、`ro_count=68`，新增 `405_3.5_2d_imm_*`、`561_3.5_2d_imm_*`、`647_3.5_2d_imm_*`。`sim_control/controller.py` 新增统一波长匹配 helper：`parsed == selected`，或 `selected == 638 and parsed == 647`；主 GUI 下拉和 controller 激活前校验复用该规则，因此当前 GUI 选择 `638 nm` 时可选择旧命名 `647_..._imm_*`，但实际仍点亮 `laser_638_line`，payload 的 `wavelength_nm` 保持 638、RO 名保留 647。`sim_control/sim_adapters.py` 仿真 RO 同步为 68 个，正式/Z-scan 索引 `0..27` 不变，488 immediate 仍在 `28..37`，新增波长追加在后。独立 subagent review 第一轮发现 1 个 blocking：同名 RO 已存在时脚本只比较 `<...>` 帧 token，可能漏掉 `[HWA h]` 或额外 body；已修复为同名 block 必须唯一且完整规范化内容与 `_preview_block()` 一致，并新增同名同帧但 `[HWA h]` 应失败的回归测试。验证：`.venv\Scripts\python.exe -m unittest tests.test_preview_ro tests.test_immediate_live tests.test_simulation_mode tests.test_running_orders tests.test_main_window_sim_footer -q` 通过（59 项）；`.venv\Scripts\python.exe -m unittest discover -s tests -q` 曾在 blocking 修复前通过（416 项），修复后重跑全量被本任务外既有 dirty 的 `sim_control/gui.py`/`ui_sim_settings_dialog.py` Z-Scan 符号缺失阻塞（`DEFAULT_Z_SCAN_TIMING_HISTORY_PATH`、`Z_SCAN_TEST_*`、`tab_zscan` 等），聚焦路径仍通过。真机待确认：在 MetroCon 中把新增 30 个 `405/561/647` immediate RO 的 activation type 设为 `Immediate / ACT_IMMEDIATE`，编译并 `Send to board` 后，主 GUI 重新 `Refresh/Connect SLM` 应显示对应波长可选项；`638 nm` 下 `647_..._imm_*` 应可用于找样品红光，正式 SIM9 仍只选普通非 `_imm_` RO。
- 2026-06-25：继续按用户反馈微调 SIM9 完成弹窗外观：在自定义 `QDialog` 方案基础上，显式把正文中文设置为 `Microsoft YaHei`、英文/数字/路径设置为 `Arial`，并把字号统一增大到 11pt。`control_wangbo/main.py::_show_sim_raw_stack_saved_dialog()` 现在用 RichText span 分段渲染正文（同时保留 `sim_plain_text` 属性作为测试用纯文本逻辑行），`dialog`、信息图标 label、正文 label 使用 11pt 微软雅黑，`QDialogButtonBox` 与 OK 按钮使用 11pt Arial；正文仍 `setWordWrap(False)`，最长行宽度改用同一套中英文字体逐字符 `QFontMetrics.horizontalAdvance()` 测量，正文 label 最小宽度为 `longest_px + 96`，外层 dialog 最小宽度为 `longest_px + icon_size + 96 + 80`，继续为路径末端保留 96 px 右侧留白。`tests/test_sim_preview_restart.py::SimRawStackSavedDialogTests` 同步断言 RichText、`white-space: nowrap`、微软雅黑/Arial span、11pt 字号、纯文本保存路径仍为同一逻辑行，以及 label/dialog 宽度按混合字体最长行撑开。验证：`.venv\Scripts\python.exe -m unittest tests.test_sim_preview_restart.SimRawStackSavedDialogTests -q` 通过（3 项）；`.venv\Scripts\python.exe -m unittest discover -s tests -q` 已运行但当前 dirty worktree 中 `tests/test_immediate_live.py::ImmediateRunningOrderScanTests` 有 2 条与 immediate RO 扫描预期相关的失败，和本次 SIM9 完成弹窗显示改动无共享路径。真机待确认：真实 Windows 弹窗中中文为微软雅黑、英文/路径为 Arial、字号和右侧留白观感符合预期，高 DPI/极长路径下窗口宽度仍可接受。
- 2026-06-25：按用户反馈把主 GUI「Hardvare Connection (SLM)」区 RO 下拉的**弹出列表(浮层)加宽 154→190**，修复展开时最长 RO 名（`488_3.5_2d_imm_f10` / `_3dir`，≈141px）在出现竖向滚动条(17px)时**后缀被截**。仅改 `control_wangbo/main.py::_build_sim_immediate_and_acquire_controls()`：**闭合下拉 `cmb_SLM_immediateRO` 仍固定 154**（保持 2×2 对齐、`grp.minHint=284≤288` 不变），把 `view()` 由 `setMaximumWidth(154)` 改为 `setMinimumWidth(190)`+`setMaximumWidth(190)` 强制浮层 190（机制同历史 `view.setMinimumWidth(380)`），`ElideRight` + per-item `ToolTipRole` 保留（长诊断项仍截断+tooltip 全文）。`tests/test_main_window_sim_footer.py` 的 B-2 断言改为 `view().minimumWidth()==190 且 maximumWidth()==190`。实测（真实 windows 平台渲染）：popup view=190/viewport=173，f10/3dir 完整显示（即便滚动条在，173-17-8=148≥141）；闭合下拉/2×2/`grp.minHint` 不受影响（浮层不进栅格布局）；全量 `unittest discover -s tests` 396 通过。**审查**：按 AGENTS.md 分级属简单低风险 UI 微调、**豁免 Codex 双循环、仅走独立 subagent review**（0 blocking；2 条 non-blocking：① offscreen/CI 因字体回退使 `combo.sizeHint` 超 190、浮层改由内容宽决定而与真机不同——真机 sizeHint≈168<190 故 190 生效，且测试只断言 `view` 输入属性故跨平台稳定；② `view.maximumWidth` 不严格约束浮层容器、靠真机 sizeHint<190 成立）。真机待确认：展开时浮层右缘小幅悬出面板(~23–38px)盖到右侧预览图是否可接受（瞬态、远小于历史 380 越界）；高 DPI/字体下 f10/3dir 全名显示与悬出量。
- 2026-06-25：按用户复测截图反馈**第三次修复 SIM9 完成弹窗保存路径换行**：上一版即使给 `QMessageBox` 内部 `qt_msgbox_label` 设置 `setWordWrap(False)` 与 `setMinimumWidth(longest_px)`，真实 Windows 弹窗仍会在 `F:\` 后折行，说明 `QMessageBox` 内部布局仍限制文本区宽度。本次改 `control_wangbo/main.py::slot_handle_sim_raw_stack_saved`：不再用 `QMessageBox` 承载正文，新增 `_show_sim_raw_stack_saved_dialog(message_lines)`，用自定义 `QDialog` + 信息图标 `QLabel` + 正文 `QLabel` + `QDialogButtonBox.Ok`；正文 label 设置 `setTextFormat(Qt.PlainText)`、`setWordWrap(False)`、`setTextInteractionFlags(Qt.TextSelectableByMouse)`，并按 `text_label.fontMetrics().horizontalAdvance()` 计算最长逻辑行后设置 `text_label.setMinimumWidth(longest_px)` 与 `dialog.setMinimumWidth(longest_px + icon_size + 80)`，从根本绕开 `QMessageBox` 的自动折行。`tests/test_sim_preview_restart.py::SimRawStackSavedDialogTests` 改用真实 Qt `QDialog`，仅 patch `QDialog.exec_` 防止阻塞，断言保存路径是最后一条完整逻辑行、正文 label 禁止换行、文本格式为 PlainText、label/窗口最小宽度覆盖最长行；失败路径仍走静态 `QMessageBox.warning`。验证：`.venv\Scripts\python.exe -m unittest tests.test_sim_preview_restart.SimRawStackSavedDialogTests -q` 通过（3 项），`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（396 项）。审查：本次为 GUI 单点低风险显示修复，Codex 直接实施不触发 codex 双循环；仍需独立 subagent review。真机待确认：真实主 GUI 点击「SIM9帧采集」完成后，`保存路径: F:\...` 整行在同一行显示，窗口宽度随路径变宽。
- 2026-06-25：按用户要求**增强已实现的 SIM9 帧采集完成弹窗信息并加宽**（在「存盘完成弹窗」条目基础上）。`control_wangbo/main.py` 三处：`trigger_sim_formal_acquisition` 的 raw_only 块缓存 `_sim_raw_acquisition_pending={start_perf(time.perf_counter()), laser_wavelength_nm, exposure_us}`（采集任务启动前记起点+曝光+波长，取自 `app_config_snapshot`）；`slot_handle_sim_acquisition_ready` raw_only 分支把 pending 并入 `sim_last_raw_acquisition_summary`；`slot_handle_sim_raw_stack_saved` 弹窗从静态 `QMessageBox.information` 改为实例 `qw.QMessageBox(self)+exec_()`，第二行显示「图像格式: {W}X{H}（{bits}位）」（尺寸取 `stack_shape[2]×[1]`、位深由 `np.dtype(stack_dtype).itemsize*8` 推导 uint16→16）+「曝光时间: {us/1000:g} ms」+「激光波长: {nm} nm」，新增「总耗时: {perf_counter()-start_perf:.2f} s」行（口径=采集任务启动→9 帧落盘完成，含硬件初始化+采集+异步落盘，**不含**停 preview/按钮点击等同步预处理），并用 `msg.layout()` 末行插入 `QSpacerItem(min(1000,max(560,int(len(path)*7.5))),0,Minimum,Expanding)` 加宽使保存路径尽量单行；task_id 不匹配缓存时降级只显示「已完成」+路径。仍只影响 raw_only（SIM9 按钮）路径、不影响 MCU 重建采集；成功（实例 information）/失败（`slot_handle_sim_raw_stack_save_failed` 静态 warning，未改）互斥；槽在主线程、线程安全。测试：`tests/test_sim_preview_restart.py::SimRawStackSavedDialogTests` 3 个测试改用整类 mock `QMessageBox`，断言实例化+`setText` 各字段+`exec_`+`layout().addItem`（加宽）、task_id 不匹配降级、失败走静态 warning。验证：该类 3 项 + 全量 `discover -s tests` 396 通过。审查：GUI 层低风险、**豁免 codex 双循环、仅走独立 subagent review**（0 blocking；3 条 non-blocking 均生产不可达/经验估算：np.dtype 对字符串 dtype 的位深、像素宽度估算、0 值 falsy 守卫）。真机待确认：真机最长路径在加宽后是否单行、总耗时口径是否符合用户对「整个采集流程」的预期。
- 2026-06-25：按用户要求把主 GUI「Hardvare Connection (SLM)」区改为 **2×2 布局**：第一行 [设备名(缩短) | Connect SLM]，第二行 [Refresh(宽不变 90) | RO 下拉(**无 "RO:" 标签**)]。实现（**纯 `control_wangbo/main.py` 运行期，用户明确授权；不改 `.ui`/生成文件**）：`_build_sim_immediate_and_acquire_controls()` 的 SLM 段——列约束改 2 列（`col0` min 110=设备/Refresh、`col1` min 154=Connect/RO、`col2` stretch 1 吸收余量）；`cmb_SLM_device` 由固定 170 缩短为 110（仍完整显示序列号 `0175000916`）；对 `btn_SLM_refresh`/`btn_SLM_connection`/`lbl_SLM_status` 先 `removeWidget` 再重排——Connect→(0,1) 固定 154、Refresh→(1,0) 宽不变 90、隐藏的 `lbl_SLM_status` 移出布局腾出 (1,1)（控件仍存在、保持隐藏）；**不再创建 `lbl_SLM_immediateRO`（"RO:" 标签）**；RO 下拉 `cmb_SLM_immediateRO` 固定 154/高 25 + `view().setMaximumWidth(154)`+ElideRight+tooltip，置于 (1,1)；按钮 `clicked`/combo `activated`/`currentTextChanged` 连接与 `_reset` 均不变；方法 docstring 同步。`tests/test_main_window_sim_footer.py::test_runtime_slm_immediate_ro_layout` 断言改 2×2：位置 device(0,0)/connect(0,1)/refresh(1,0)/RO(1,1)、col0=110/col1=154/col2 stretch1、device 110/connect 154/refresh 90/RO 154/高 25、`hasattr(lbl_SLM_immediateRO)==False`、`lbl_SLM_status` 仍存在且 `grid.indexOf==-1`、保留 B-1（grid/grp 最小宽 ≤ 动态面板宽）/B-2（view≤154+ElideRight）护栏与 (none) tooltip、activated 信号。实测（真实 Ui_ + 改后代码渲染）：`grp.minHint=284≤288`；R0 device[7..116]w110 / connect[123..276]w154；R1 refresh[7..96]w90 / RO[123..276]w154（**Connect 与 RO 左右边对齐 123..276**）；popup 154<288 不越界；status 移出栅格、无 RO 标签；全量 `unittest discover` 396 通过。**审查**：按 AGENTS.md 复杂度/风险分级，本次为单文件低风险 UI 布局调整，**豁免 Codex 双循环、仅走独立 subagent review**（`code-documentation` reviewer，0 blocking：逐项确认 removeWidget 无 reparent/断连、`lbl_SLM_status` 移出布局安全[全仓 grep 仅 setVisible/hasattr]、不创建 `lbl_SLM_immediateRO` 无 AttributeError、测试与代码一一对应）。真机待确认：真实设备名 / 「Disconnect SLM」在 110/154 内不被截断；高 DPI(125%/150%) 下 grp 4px 余量不越界、两行右列对齐、popup 不盖预览图；面板宽固定 288 故 RO 固定 154 等价于「填满剩余空间」。
- 2026-06-25：主 GUI 点击「SIM9帧采集」并 9 帧**存盘完成后新增完成弹窗**（用户需求）。在已有 raw-only SIM9 + `RawStackSaveWorker` 基础上，`control_wangbo/main.py` 两处改动：`slot_handle_sim_acquisition_ready` 的 raw_only 分支新增缓存 `self.sim_last_raw_acquisition_summary={task_id,stack_shape,stack_dtype}`（保存完成槽只有 task_id/path、拿不到帧形状，故在更早到达的 summary 槽先缓存）；`slot_handle_sim_raw_stack_saved` 末尾追加 `qw.QMessageBox.information(self,"SIM9 帧采集", …)`，正文「SIM9 帧采集已完成。」+ 可选「图像栈: (9,H,W)」+「保存路径: <path>」，缓存 task_id 与当前不匹配时降级为只显示路径。边界：该槽只在 raw_only（SIM9 按钮）路径 connect/触发（`RawStackSaveWorker` 仅 raw_only 时连），不影响 MCU `mcu_function_trigger` 正式重建采集；保存成功弹 information、失败仍走既有 `slot_handle_sim_raw_stack_save_failed` 的 warning，二者互斥；槽经 queued 信号在主线程执行，弹窗线程安全。测试：`tests/test_sim_preview_restart.py` 新增 `SimRawStackSavedDialogTests`（3 项：成功含形状+路径、task_id 不匹配降级只显路径、失败仍只 warning），复用 `MainWindow.__new__`+mock `QMessageBox` 模式；新增过程中一度误把 `test_laser_pulse_test_forwards_stop_event_to_pulse_line` 插错类，已修回 `SimSettingsDialogTests`。验证：`unittest tests.test_sim_preview_restart` 100 通过、全量 `discover -s tests` 396 通过。审查：按 `AGENTS.md` 复杂度/风险分级属 GUI 层低风险、**豁免 codex 双循环、仅走独立 subagent review**（0 blocking；2 条 non-blocking 均设计预期：模态弹窗仅在未来无人值守自动采集才可能成卡点、空 stack 的 `if shape:` falsy 降级正确）。真机待确认：真机点「SIM9帧采集」结束后弹出「SIM9 帧采集」完成框、`data/sim_9frames` 路径正确显示；若后续要连续无人值守多次采集，再评估改非模态/状态栏提示以免模态框阻塞主线程。
- 2026-06-25：按用户要求把主 GUI「Hardvare Connection (SLM)」区 Running Order 下拉**从两行改回与标签同一行**、标签由「Running Order:」**改为「RO:」**、下拉宽度相应调整以美观。实现（**纯 `control_wangbo/main.py` 运行期，用户明确授权；不改 `.ui`/生成文件**）：`_build_sim_immediate_and_acquire_controls()` 的 SLM 段改用嵌套 `QHBoxLayout`（`ro_row`：`addWidget(label)`+`addWidget(combo)`+`addStretch(1)`）`addLayout(ro_row, 2,0,1,3)`，取代上一版的两行（标签 `(2,0,1,3)`+下拉 `(3,0,1,3)`）；短标签 `"RO:"`（≈25px）不再占满 170px 的 col0，故单行放得下；下拉 `setFixedWidth`/`view().setMaximumWidth` 220→**235**（25+6+235=266 ≤ Row0 宽不撑栅格；实测 ≤238 安全、245 起 `grp.minHint=290>288` 会压缩），`setContentsMargins(6,4,6,4)`、列宽 170/90/0、列伸缩 0/0/1、`ElideRight`、tooltip、`_reset_immediate_ro_dropdown()`、`activated`/`currentTextChanged` 连接均不变；方法 docstring 同步。`tests/test_main_window_sim_footer.py::test_runtime_slm_immediate_ro_layout` 断言同步：改用「遍历 `grid.itemAt(i).layout()` 找含下拉的嵌套 HBox」定位（取代对嵌套 widget 失配的 `grid.indexOf`），断言 HBox 跨 `(2,0,1,3)`、含 label+combo、`itemAtPosition(3,0)` 为 None、label=="RO:"、combo 固定 235、`view().maximumWidth()==235`；保留 B-1（grid/grp 最小宽 ≤ 动态面板宽）/B-2（ElideRight + view≤235）护栏。实测（真实平台渲染）：`RO:` 与下拉同行、**下拉右缘 272 与上方 Refresh 右缘 272 对齐**、device→Refresh `gap=7px` 无重叠、popup 235<288 不盖预览图、组高 138→112；全量 `unittest discover -s tests` 393 通过。**审查**：按 AGENTS.md 复杂度/风险分级，本次为单文件低风险 UI 微调，**豁免 Codex 双循环、仅走独立 subagent review**（`code-documentation` reviewer，0 blocking：三诉求达成、行为零变化、测试定位升级正确；non-blocking：235 距 238 上限仅 3px、长名靠 tooltip）。真机待确认：目标机（尤其 125%/150% DPI）目视 `RO:` 与下拉同行无错位、不重叠、popup 不盖预览图；长 RO 名在 235px 内的可读性。
- 2026-06-25：修复主 GUI「Hardvare Connection (SLM)」区 **Refresh 与设备序列号(如 0175000916)重叠** + **Running Order 下拉框及弹出列表过宽越界盖右侧相机预览图**（同日上一条把该下拉改成「最小 300px / popup 最小 380px 可扩展宽度」正是本次重叠/越界的直接诱因）。根因同源：面板宿主 `layoutWidget_simConfiguration` 固定宽 **288px**，旧代码把下拉逼宽（`min300`/`Expanding`/`minimumContentsLength28`/`view().setMinimumWidth(380)`）且把「Running Order:」标签塞进被设备下拉锁死 170px 的 col0，使 `gridLayout_SLMConnection` 最小需求宽撑过 288 → Qt 压缩各列致固定宽 170px 的设备下拉溢出到 Refresh（重叠），下拉自身 + popup 越界进预览图。修法（**纯 `control_wangbo/main.py` 运行期改造，用户明确授权改主 GUI SLM 模块；不改 `.ui`/生成 `CellSorting_ui.py`**，Running Order 控件本就运行期创建）：`_build_sim_immediate_and_acquire_controls()` 的 SLM 段改为**紧凑两行**——`lbl_SLM_immediateRO` 独占 `(2,0)` 跨 3 列、`cmb_SLM_immediateRO` 独占 `(3,0)` 跨 3 列；`setContentsMargins(6,4,6,4)`、`columnMinimumWidth 170/90/0`、`columnStretch 0/0/1`（挤压量由最右空列吸收，device/refresh 不漂移）；下拉 `setFixedWidth(220)`+`setFixedHeight(25)`（220 ≤ col0+col1=266，不撑栅格）、`view().setMaximumWidth(220)`+`view().setTextElideMode(Qt.ElideRight)`（弹出列表跟随 220px、过长诊断项右截断、全文仍在 per-item `ToolTipRole`），移除旧 `min300`/`Expanding`/`minimumContentsLength`/`view380`。`tests/test_main_window_sim_footer.py::test_runtime_slm_immediate_ro_layout` 断言同步到新布局并新增 **B-1 护栏**（`grid.minimumSize()`/`grp.minimumSizeHint()` 宽 ≤ 动态取的面板实际宽）与 **B-2 护栏**（`view().maximumWidth()==220` + `ElideRight`）。验证：PyQt5 实测 device→Refresh `gap=7px`、闭合下拉与 popup 均 220px(<288)、ElideRight 生效；`unittest tests.test_main_window_sim_footer tests.test_immediate_live tests.test_sim_preview_restart` 128 通过、全量 `discover -s tests` 393 通过；并用真实 `Ui_` 类 + 改后代码离屏渲染闭合/展开态 PNG 目视确认无重叠、不越界。**审查**：Codex 双循环——第一循环 2 轮（2 blocking：`setMaximumWidth` 不降 `minimumSizeHint`、不约束 popup → 实测改为 `setFixedWidth`+`view().setMaximumWidth` 后清零）、第二循环 1 轮 0 blocking（同一 codex 会话 `--fresh`→`--resume`，无降级）；独立 subagent review **APPROVE**（0 CRITICAL/HIGH；2 MEDIUM 测试健壮性建议——护栏改用动态面板宽、删除冗余 `minimumWidth≤220` 断言——均已采纳；并反向复现旧写法证实两条护栏确能抓回归）。真机待确认：真实 Windows 主界面连接 SLM 后目视 Refresh 不贴死设备序列号、Running Order 下拉与弹出列表不盖右侧相机预览图、长名经 tooltip 可读（high-DPI/字体回退下 286≤288 裕量约 2px，留意）。
- 2026-06-25：按用户提速诉求做两类调整。(1) **harness 提速**（改 `~/.claude/settings.json` 全局配置、非项目源码）：禁用与 SIM 无关或功能重叠的插件 `claude-mem`/`claude-hud`/`claude-reflect`/`oh-my-claudecode`/`ppt-master`，减少每轮注入的 skill/MCP schema 与每次工具调用的 hook 进程；omc 走"先验证替代再关"——`npm install -g pyright`（`pyright-langserver 1.1.411` 已入 PATH）接管 LSP 诊断、修改后 subagent review 改用 `code-documentation:code-documentation-code-reviewer`（替代已禁的 `oh-my-claudecode:code-reviewer`，本次审查已实测可用）；需重启 Claude Code 生效。(2) **codex 双循环分级**：`AGENTS.md`「Codex 双循环审查工作流」从"每次实质修改强制"改为按复杂度/风险分级——只有复杂任务才走双循环，简单单步/低风险改动豁免双循环（仍走 subagent review），硬件/波形时序/安全/破坏性改动即使简单也按复杂任务走（可升 `/codex:adversarial-review`）；节副标题改"分级审查流程"、节首"才触发"改"才可能触发"、适用范围首条拆为三条；`docs/project_memory/decision_log.md` 新增 2026-06-25 决策；auto-memory `codex-dualloop-review-workflow.md`/`harness-perf-plugin-trim.md` 同步。codex 规则修改本身属"文档微调＋用户显式指令"，豁免 codex 双循环、仅走独立 subagent review（0 blocking）。需人工确认：工作区仍混有跨多会话未提交改动，提交前按任务拆分；"复杂/简单"门槛由 Claude Code 当场判定、以"拿不准从严"兜底。
- 2026-06-25：修复主 GUI 点击「SIM9帧采集」后 SLM immediate Running Order 下拉框只剩 `(none)` 的问题。根因是 `trigger_sim_formal_acquisition()` 采集前默认调用 `stop_immediate_live_mode()`，该默认路径会清空 `cmb_SLM_immediateRO`；采集完成只恢复按钮/预览状态，没有重新填充 cached immediate RO。`control_wangbo/main.py` 新增 `_select_immediate_ro_none_without_clearing()`，SIM9 采集前改为 `stop_immediate_live_mode(reset_dropdown=False)`，并在确认 immediate-live 已关闭后只把当前项退回 `(none)`、保留已扫描的 immediate RO 条目；断开 SLM、连接失败、重建 controller、波长切换和激活失败等安全/失效路径仍保留清空或重建下拉的行为。新增回归覆盖 `stop_immediate_live_mode(reset_dropdown=False)` 成功关光但不清空下拉，以及 raw SIM9 入口保留 combo 条目和 `sim_immediate_ro_items`。验证：`.venv\Scripts\python.exe -m pytest tests\test_immediate_live.py -q` 通过（28 项）；`.venv\Scripts\python.exe -m pytest tests\test_sim_preview_restart.py -q` 通过（97 项、11 个 subtests）；`.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py -q` 通过（33 项、167 个 subtests）；`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（393 项）。独立 subagent review 0 blocking，确认保留下拉、关光互锁、raw stack 不进 GUI 状态路径和记忆更新均满足要求。真机待确认：真实 R11/SLM 连接后执行「SIM9帧采集」结束，Running Order 下拉仍可重新选择 `*_imm_*` immediate RO，且采集前找样品激光已关闭、不与 SIM9 波形并存。
- 2026-06-25：修复集成主界面 live preview 复用 `SIM Camera Settings` 正式曝光导致长曝光预览 `DCAM preview wait failed: TIMEOUT` 的问题。`control_wangbo/main.py` 新增固定预览曝光 `SIM_PREVIEW_EXPOSURE_US=30_000`，`start_sim_preview()` 仍先同步 UI 中 ROI、Image Size、bit depth、device 和正式 SIM9 曝光，但启动预览时只对 `CameraConfig` 副本覆盖 `exposure_us=30_000`，并把该副本下发给 `apply_camera_config()` 和 `SimPreviewController.start()`；`self.sim_app_config.camera.exposure_us` 保持用户在 `SIM Camera Settings` 中设置的正式采集曝光，点击「SIM9帧采集」后仍用于 Running Order 选择、波形和 9 帧采集。`apply_connected_sim_camera_config()` 增加可选 `camera_config` 参数，默认调用保持原正式配置语义。测试覆盖预览配置副本不污染正式曝光、正式采集 task 保留 500 ms UI 曝光。验证：`.venv\Scripts\python.exe -m pytest tests\test_sim_preview_restart.py -q` 通过（97 项、11 个 subtests）；`.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py -q` 通过（33 项、167 个 subtests）；`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（392 项）。独立 subagent review 0 blocking，确认正式配置未被预览曝光污染、正式采集仍走 UI 曝光、summary 风险可控；真机待确认：真实 Hamamatsu 预览在用户把 SIM9 曝光设为 >100 ms/500 ms 时仍维持约 20-30 FPS 且不再弹 DCAM preview timeout，正式 SIM9 采集仍按 UI 曝光执行。
- 2026-06-25：修复主 GUI「SIM9帧采集」与 SLM immediate RO 下拉可读性。`control_wangbo/main.py` 将运行时 `cmb_SLM_immediateRO` 从窄 `220px` 限制改为可扩展宽度（最小 300px、popup 最小 380px），并为 `(none)` 与每个 RO 条目设置完整 tooltip，便于区分 `488_3.5_2d_imm_f1/f2/...` 后缀；未改变 immediate RO 的 activation/wavelength fail-closed 过滤和 preview 出帧后才点灯的安全状态机。「SIM9帧采集」按钮改走 raw-only SIM9 入口：采集前仍关 immediate-live 激光、停 preview、要求 SLM 已连接、自动选择正式非 `_imm_` RO，并复用正式 SIM9 硬件链路；本次任务级传入 `reconstruction.enabled=False` 与 `z_scan_enabled=False`，不修改全局重建配置、不触发 `reconstruction_starting`，采集完成后 raw `AcquisitionBatch` 只连到新增 `RawStackSaveWorker` 独立 QThread，验证 `(9,H,W)` 且 `uint16` 后保存为 `data/sim_9frames/sim_9frames_<task_id>.tif`，保存成功/失败只更新运行状态和必要弹窗。`sim_control/controller.py` 的 `start_single_acquisition()` 新增可选任务级 `reconstruction_config` 校验覆盖，默认仍用 controller 全局重建配置，因此正式重建路径继续缺 OTF fail-closed。`sim_control/pipeline.py` 新增 raw stack 保存 worker，采集核心仍无磁盘 I/O。测试覆盖 raw-only 启动参数、summary 不触发重建、late summary 不覆盖已保存状态、controller 任务级禁用重建校验、raw TIFF 写入/坏 shape/dtype 拒绝和 RO 下拉宽度/tooltip。验证：`.\.venv\Scripts\python.exe -m unittest tests.test_main_window_sim_footer tests.test_sim_preview_restart tests.test_config_validation tests.test_efficiency_optimizations -q` 通过（131 项）；`git diff --check -- control_wangbo\main.py sim_control\controller.py sim_control\pipeline.py tests\test_main_window_sim_footer.py tests\test_sim_preview_restart.py tests\test_config_validation.py tests\test_efficiency_optimizations.py` 通过。Codex 双循环不触发（本次为 Codex 直接实施，非 Claude Code 主导）；独立只读 subagent 预审提醒 raw stack 必须只进 worker、不可接 GUI summary 槽，已按该约束实现。真机待确认：连接真实 SLM/camera/USB-6423 后点击主 GUI「SIM9帧采集」能在 `F:\SIM_AutoSystem\data\sim_9frames` 生成 9 页 `uint16` TIFF，且不弹 OTF 缺失错误、不启动重建；正式 SIM9 重建采集仍需从正式重建入口或后续专门按钮进入。
- 2026-06-25：修复集成主界面 `SIM Camera Settings` 中 `Image Size`、`ROI_X`、`ROI_Y` 重启后被旧 `control_wangbo/lastConfiguration.json` 扁平字段覆盖的问题。启动自动加载旧 CellSorting 配置时不再把 `spb_sCMOS_pixelWidth/Height/ROI_X/ROI_Y` 写回 `sim_app_config.camera`，而以 `config/sim_control_config.json` / `sim_app_config` 为 SIM 相机设置权威来源；用户手动 Load 旧 JSON 时仍保留扁平字段兼容。`load_configure_settings()` 增加加载保护，批量 `setValue()` 期间 `on_sim_camera_setting_changed()` 不触发 SIM 配置同步、SLM RO 刷新、相机下发或 preview 重启；保存/同步前会提交 ROI 与曝光 spinbox 待编辑文本；手动保存 JSON 额外写入完整 `sim_control` payload。新增回归覆盖自动/手动旧配置分流、pending text 提交和加载保护副作用抑制。验证：`.venv\Scripts\python.exe -m pytest tests\test_sim_preview_restart.py -q`、`.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py -q`、`.venv\Scripts\python.exe -m unittest discover -s tests -q` 均通过；`git diff --check -- control_wangbo\main.py tests\test_sim_preview_restart.py PROJECT_MEMORY.md` 退出 0，仅提示 `PROJECT_MEMORY.md` 未来被 Git touch 时 CRLF 会转 LF。
- 2026-06-25：按现场 SLM 连接 warning 完成 `ACT_IMMEDIATE` 问题闭环。根因不是 WinUSB/R11 SDK 连接失败，而是板卡上 `488_3.5_2d_imm_f1/f2` 曾为 `HARDWARE(0x04)`，找样品链路按安全策略只允许 `ACT_IMMEDIATE(0x01)`，因此 GUI fail-closed 禁用并弹 warning；已用 MetroCon V4.1 加载 `patterns/2d_3.5.repz11` 并 `Send to board` 到 R11 serial `175000916`，Status 页确认 repertoire `2d_3.5`，default 仍为 `0 405_3.5_2d_50ms`。发送后发现 R11 `SetSelectedBlind/SetSelected` 后立即读 `R11_RpcRoGetActivationType` 会偶发返回上一 RO 的 activation type，导致刚修正的 `f1/f2` 在完整扫描中仍被误报为 `HARDWARE`；`sim_control/adapters.py` 新增 `R11_RO_SELECTION_SETTLE_S=0.05`，在 `list_running_orders_with_activation()` 和 `select_running_order()` 选中 RO 后短暂等待再读 activation metadata，仍然只在 SDK 最终返回 `ACT_IMMEDIATE` 时启用，fail-closed 策略不变。`tests/test_immediate_live.py` 新增 fake SDK 回归，覆盖扫描和点灯前二次 select metadata 的 stale-read 场景。验证：`.venv\Scripts\python.exe -m pytest tests\test_immediate_live.py -q` 通过（27 项），`.venv\Scripts\python.exe -m pytest tests\test_slm_activation_state.py -q` 通过（9 项），真实 R11 完整扫描输出 `488_3.5_2d_imm_f1..f9` 与 `488_3.5_2d_imm_3dir` 全部 `IMMEDIATE (OK)`，`select_running_order(28)` 返回 `IMMEDIATE(1)`；`git diff --check -- sim_control\adapters.py tests\test_immediate_live.py` 通过。独立 subagent review 第一轮 0 blocking，并建议把 settle 扩展到 `select_running_order()`；已完成，后续复审与最终注释复审均为 0 blocking。仍需人工/真机确认：回到主 GUI 重新 `Refresh/Connect SLM` 后 warning 消失且 immediate RO 下拉可选；相机 live preview 真正出帧后再点亮 488 找样品激光；正式 SIM9 仍选择普通 `488_3.5_2d_{1/10/50}ms` RO，不选择 `*_imm_*`；旧 DLL 无 `SetSelectedBlind` 时 fallback 普通 select 扫描是否无显示/激光副作用。
- 2026-06-25：澄清「Codex 双循环审查工作流」的**触发主体**——只在 **Claude Code** 对本工作区做实质性代码/配置修改、或产出修改方案时触发，由 codex 作为外部只读交叉审查者审 Claude 的方案/实现；用户**直接用 Codex 或其他 AI 工具直接改代码时不触发本工作流、不强制 codex 自审**（codex 自审自己的方案会丧失“外部交叉”的独立性，与初衷相悖）。**根因**：原 `AGENTS.md` 节标题“代码修改强制流程”与适用范围首条均未限定主体，而 `AGENTS.md` 是所有 AI 工具（含 Codex CLI 约定加载）共读的主指令文件，Codex 读到后把“改代码就走双循环”套到自己头上自审方案。**修法**：`AGENTS.md` 节标题改“（Claude Code 代码修改强制流程）”、节首新增“触发主体（关键）”要点、适用范围首条加 Claude Code 主体 + 新增“Codex/其他工具直接改不触发”排除项；`docs/project_memory/decision_log.md` 2026-06-24 决策追加 2026-06-25 澄清；auto-memory `codex-dualloop-review-workflow.md` 同步。本次属文档/记忆类（豁免 codex 双循环、不豁免 subagent review），仅改 `AGENTS.md`、`docs/project_memory/decision_log.md`、`PROJECT_MEMORY.md` 与 auto-memory，未触碰任何源码/配置/`reconstruction/`/`control_wangbo/`。
- 2026-06-24：修复主 GUI SLM Running Order 下拉中 `488_3.5_2d_imm_f1` 不显示的诊断与安全边界。确认 `patterns/2d_3.5.repz11` 内已有 `f1`，问题原因是 GUI 只展示 R11 SDK 扫描为 `ACT_IMMEDIATE(0x01)` 的找样品 RO；若 `f1` 缺失，说明当前板上/MetroCon 编译结果大概率未把 `f1` 设成 immediate，而不能按名字强行启用。实现改为 fail-closed：`controller` 只把严格命名 `^(\d+)_3\.5_2d_imm_(f[1-9]|3dir)$` 且 SDK 返回 immediate 的 RO 放入可点灯集合；命名正确但非 immediate 的 `f1` 会进入下拉 disabled 诊断项并弹 warning；点灯前重新 `select_running_order()` 并复核返回名称与 `activation_type`，非 immediate 时拒绝写 DAQ 激光线；正式 SIM9 继续排除所有 SDK 判定 immediate 的索引。仿真 SLM 更新为 `f1..f9+3dir` 共 10 个 immediate RO，测试覆盖 `f1` 非 immediate / `f2` immediate、旧 `live_imm` 排除、激光前复查失败不写 DAQ、GUI disabled 项和仿真 38 RO。验证：`pytest tests\test_immediate_live.py tests\test_simulation_mode.py -q` 31 passed；`pytest tests\test_running_orders.py tests\test_main_window_sim_footer.py -q` 13 passed/4 subtests；`pytest tests\test_preview_ro.py tests\test_sim_preview_restart.py -q` 96 passed/11 subtests；`pytest tests\ -q` 386 passed/192 subtests。Codex 方案审查 1 轮有 blocking，经实现消除；Codex 实现验证主审查 1 轮 0 blocking（另有一次误用 `--last` 的旁路审查也 0 blocking，作为降级/串台风险记录）；待独立 subagent review 复核。真机待确认：在 MetroCon/R11 工具中把 `488_3.5_2d_imm_f1` 编译/发送为 `ACT_IMMEDIATE(0x01)`，重连 SLM 后确认 `f1` 可选；修正前应只显示 disabled 且不能点亮激光；还需确认 activation 扫描的 blind select/fallback select 无显示或激光副作用。
- 2026-06-24：**找样品 immediate RO 方案演进——原“合并 488 文件”方案（下方旧条目的 `merge_immediate_ro.py`）经 MetroCon 实测不可行、已废弃**：(a) 加 48088 序列别名撞 R11 序列上限（MetroCon 报 `sequence number (16) out of range`，实际上限 12）；(b) immediate 激活类型必须 MetroCon GUI 设、编译进 `.repc`，脚本改 `.rep` 的 `[HWA ]` 控制不了（`[HWA ]` 无 h 在官方 HWRO Example 是 hardware-select、配 `RO_SELECT`、占 SPI）。**新方案 `tools/add_preview_ro.py`**（取代已删的 `merge_immediate_ro.py`+`test_immediate_ro_merge.py`）：在 `2d_3.5.repz11` 只追加 10 个找样品 RO 骨架（`488_3.5_2d_imm_f1..f9`+`_3dir`），**复用现有 Lit Pair 别名 `A+`/`A-`（48030）成对做 DC 平衡**——Lit Pair `+`/`-` 是同图案正/反相（`b0`//`b0`），FLC 必须成对驱动净 DC=0、否则损坏液晶（`AN0028BA` p.14），故 `f1..f9`=`<(A+,n) (A-,n) >`、`3dir`=`<(A+,9) (A-,9) (A+,12) (A-,12) (A+,15) (A-,15) >`（token 带空格）+ **复用现有 488 图案（IMAGES 全局 9-17）**，**不加序列/图**（产物 38 RO/12 别名/36 图）；幂等按 RO 名精确帧比对、拒绝旧 `L` 残留、`.bak` 校验通过后才建。**审查**：codex 双循环（第一循环多轮含 DC 平衡关键修正、第二循环 codex 独立复核产物 0 blocking）+ subagent review 0 blocking（独立复核 + 幂等字节级稳定）；`pytest tests/test_preview_ro.py` 6 passed。**真机硬门槛/待确认**：① 用户须在 MetroCon 把这 10 个 `[HWA ]` 骨架 RO 激活类型设为 immediate 并编译烧录（脚本控制不了激活类型）；② 真机 `list_running_orders_with_activation()` 返回 `0x01`、下拉显示这 10 个；③ Lit Pair 成对 loop 亮度（嫌暗换 `D+/D-`=48039 3ms 保持成对）、相机曝光 vs pair 周期正反相平均、immediate 中断须在 sequence 边界；④ `3dir` 9/12/15 方向目视。仅改 `tools/add_preview_ro.py`、`tests/test_preview_ro.py`、`patterns/2d_3.5.repz11`（产物）。
- 2026-06-24：(1) 把 `488-3.5mm-9frame-tset.repz11` 的 10 个 immediate RO 合并进 `patterns/2d_3.5.repz11`（新增 `tools/merge_immediate_ro.py`，复用 `add_zscan_ro.py` 的 ZIP 解包→改 `.rep`→重打包+`.bak` 模式 + `tests/test_immediate_ro_merge.py` 用合成 28-RO target 可移植测试）：RO 重命名为 `488_3.5_2d_imm_f1..f9`/`_3dir`（带前导波长 488、不撞正式正则、`parse_leading_wavelength_nm`==488，使主界面 immediate 下拉能识别）；9 张 bitplane 追加 IMAGES 段、帧引用按**全局索引**重编号到 36-44；序列 48088 已在 zip、只加别名 `L`；保留 `[HWA ]`(无 h)、仅一个 DEFAULT；幂等用 RO 名/png 名判断（**绝不用 `[HWA ]` 子串**——正式 `[HWA h]` 含该子串）；写回前自校验 38 RO/45 图/13 别名。(2) 美化主界面 Hardvare Connection (SLM) 区（纯 `control_wangbo/main.py` 编程、不改 `.ui`）：`gridLayout_SLMConnection` 加 spacing(6/0)+列宽/伸缩修 Refresh 与设备序列号重叠；`cmb_SLM_immediateRO` 从越界 (1,2) 移到组下方独立行 (2,1) 跨 2 列 + 新增 `lbl_SLM_immediateRO`（"Running Order:"）(2,0)；护栏测试进 `tests/test_main_window_sim_footer.py`。**审查**：Codex 双循环——第一循环（整 plan）2 轮（3 blocking→0）；任务 1、任务 2 各第二循环 1 轮 0 blocking，同一 codex 会话 `--fresh`→`--resume` 串行；两任务独立 subagent review 均无 blocking。**验证**：`pytest tests/` 全量 382 passed/192 subtests；合并产物 zipfile 校验 38/45/13/10 immediate/L→48088/9 png/DEFAULT 1。仅改/增 `tools/merge_immediate_ro.py`、`tests/test_immediate_ro_merge.py`、`tests/test_main_window_sim_footer.py`、`control_wangbo/main.py`（用户授权）、`patterns/2d_3.5.repz11`（产物，`.bak` 已 gitignore）。**真机硬门槛/待确认**：`[HWA ]`(无 h) 烧录后是否真返回 `ACT_IMMEDIATE(0x01)`、能否进 GUI immediate 下拉（须连 SLM 用 `list_running_orders_with_activation()` 验 `is_immediate`，否则这 10 个 RO 不显示）；合并后无 `.repc` 是否需 MetroCon Build 重编译；SLM 区新增一行的 DPI 目视不溢出。
- 2026-06-24：按用户要求将 Trigger Control 中 `Function` 时间控件 `spb_triggerFunction_time` 的上限从 `60000 ms` 提高到 `600000 ms`。修改范围限定为 `control_wangbo/CellSorting_ui.ui` 中该 `QDoubleSpinBox` 的 `maximum`，并通过 `control_wangbo/generate_ui_py.cmd control_wangbo/CellSorting_ui.ui` 重新生成 `control_wangbo/CellSorting_ui.py`；`tests/test_ui_regressions.py` 新增断言 `ui.spb_triggerFunction_time.maximum() == 600_000`。未修改 `main.py`、`MCUTriggerThread.py`、Function 等待/自动分选逻辑、SIM camera 或 MCU 时序。验证：`.\.venv\Scripts\python.exe -m compileall -q control_wangbo\CellSorting_ui.py tests\test_ui_regressions.py` 通过；`.\.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py -q` 通过（33 项、167 个 subtests）；`git diff --check -- control_wangbo\CellSorting_ui.ui control_wangbo\CellSorting_ui.py tests\test_ui_regressions.py` 通过（仅 `CellSorting_ui.py` CRLF 规范化提示）。Codex 方案审查 1 轮无 blocking；Codex 实现验证 1 轮无 blocking。需注意当前工作区仍混有此前未提交的 unrelated dirty changes，提交前建议按任务拆分范围。
- 2026-06-24：修复主 GUI 连接 SLM 后因正式 RO 不匹配而进入半连接状态的问题。`control_wangbo/main.py` 在 Connect SLM worker 报错路径中先把 `sim_slm_connected` 置回 `False`，清空 `selected_running_order` 与 immediate RO 缓存/下拉，并 best-effort 调用共享 `SimAcquisitionController.disconnect_slm()` 释放已打开的 R11 句柄；清理失败只打印日志，弹窗仍显示原始 RO 不匹配信息。随后 best-effort 保存清空后的配置、刷新 SIM 摘要与 SLM 控件，`_on_slm_finished()` 继续释放 QThread 引用、恢复按钮并重新枚举设备，因此用户关闭报错弹窗后可重新 Refresh/Connect；保存配置失败也只打印日志，不阻断原始 RO 不匹配弹窗。`tests/test_sim_preview_restart.py` 新增异步回归测试，模拟 `connect_slm()` 成功后 `select_running_order_for_task()` 抛出 `HardwareError("No matching SLM running order found...")`，断言原始弹窗、`disconnect_slm()` 调用一次、RO 状态清空、immediate 下拉复位、按钮恢复可用且重新枚举设备。验证：`.\.venv\Scripts\python.exe -m unittest tests.test_sim_preview_restart tests.test_ui_regressions -q` 通过（123 项）；`.\.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（371 项）；`git diff --check -- control_wangbo/main.py tests/test_sim_preview_restart.py PROJECT_MEMORY.md` 通过。审查：Codex 方案审查 1 轮无 blocking；Codex 实现验证 2 轮无 blocking；独立 subagent review 无代码行为 blocker，仅提示已修正本记忆条目并建议未来可补 `disconnect_slm()`/`save_app_config()` 抛异常的非阻塞回归。真机待确认：真实 R11 WinUSB 句柄在 RO 不匹配后能被释放，随后 Refresh/Connect 可重新连接；现场确认 RO 不匹配后强制断开重连的 UX 符合实验操作预期。
- 2026-06-24：按用户反馈微调主 GUI 运行时创建的「SIM9帧采集」按钮外观，覆盖上一轮记录中 `setGeometry(1290,158,200,36)` 的尺寸：`control_wangbo/main.py` 中 `btn_sim9_acquire` 改为复用 `btn_openSimSettings.font()`（缺失时 fallback 为 `Times New Roman` 12pt），几何改为 `QRect(1290, 158, 160, 36)`，右边界退到 `grp_simConfiguration` 左侧之外；未修改采集逻辑、SLM/DAQ/immediate-live 状态机、`.ui` 或 `CellSorting_ui.py`。`tests/test_main_window_sim_footer.py` 新增运行时控件回归测试，按现有主界面测试方式 stub 硬件 import，并断言按钮几何、字体与 `btn_openSimSettings` 一致且不越过 `SIM Configuration` 栏。验证：`.\.venv\Scripts\python.exe -m unittest tests.test_main_window_sim_footer tests.test_ui_regressions -q` 通过（36 项）；`.\.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（370 项）。待用户目视确认：160 px 宽度在实际主 GUI 中是否符合红框位置预期。
- 2026-06-24：再次强化「Codex 双循环审查工作流」——新增**承载方式**约束：同一任务的 codex review（不论第几轮、第一/第二循环）必须由主 agent 在主对话用 `/codex:rescue` 串行承载（首轮 `--fresh`、后续 `--resume`），落在同一 codex 会话；**不得**用 Workflow/parallel/后台 spawn 跑需要多轮续接的 codex review（会另开独立会话、`--resume-last` 接不回主线——reconstruction 验证曾误用 Workflow 跑 codex、仅因 0 blocking 单轮未暴露断链），并行多视角只用 Claude subagent 旁路。同步在「任务执行方式（并行子代理）」节加例外指针、第二循环 `/codex:review --base` 旁路边界澄清。独立 subagent review：0 blocking。仅改 `AGENTS.md` 与记忆文件。
- 2026-06-24：修复 `tests/test_main_window_sim_footer.py` 失败（报 `27 != 24 : layoutWidget27`）。**根因不是 footer 布局回归**：footer 五个命名控件与灰阶上限容器几何完好（容器仍 `(360,486,128,24)`）。真因是 `control_wangbo/CellSorting_ui.ui` 工作区新增 ROI 控件栏后用 pyuic5 重生成 `CellSorting_ui.py`，导致**位置自动名 `layoutWidgetNN` 整体重排**：灰阶上限容器 `layoutWidget27`→`layoutWidget24`，SIM Camera Settings 容器（持 `gridLayout_32`）`layoutWidget28`→`layoutWidget25`，`layoutWidget28` 消失，新 ROI 栏抢占 `layoutWidget27`（几何 `(532,172,383,27)`，h=27→触发断言）。测试与 main.py 都把易漂的自动名写死。**连带隐患**：同一漂移使 `control_wangbo/main.py:811` `_build_sim_immediate_and_acquire_controls()` 的 `host=getattr(self.ui,"layoutWidget28",None)` 恒为 None，`if grid is not None and host is not None` 整块被跳过，"采集波长唯一主入口" `cmb_sCMOS_laser` 下拉不再创建（下游 `main.py:1063,1159-1163` 用 `getattr(...,None)` 防御读取→**静默降级**到默认波长，无报错、无测试覆盖，故套件未报）。**修复**（用户选"按子代号定位"方案，不改 `.ui`、不重生成 `CellSorting_ui.py`）：①`tests/test_main_window_sim_footer.py` 改用稳定子控件 `spb_sCMOS_displayGray_max.parentWidget()`/`label_79.parentWidget()` 定位 footer 灰阶容器替换硬编码 `ui.layoutWidget27`，h==24/y==486/互不重叠断言不变；新增护栏测试 `test_sim_camera_settings_host_resolvable_via_gridlayout_parent`（断言 `gridLayout_32.parentWidget()` 非 None 且其 parent 即 `grp_hardvareConnection_2`，并断言 `(2,1)/(3,1)` 网格空闲——显式化 main.py 插入波长控件的隐性假设）。②`control_wangbo/main.py:811`（用户显式授权该处）改为 `host = grid.parentWidget() if grid is not None else None`（`grid=gridLayout_32` 为 Qt Designer 稳定语义名，其宿主即 HEAD 的 layoutWidget28 对应容器），对自动名漂移免疫并恢复波长下拉。**审查**：Codex 双循环全程无 blocking（第一循环方案审查 1 轮通过、第二循环实现验证 1 轮通过 + 护栏加固增量复核 1 轮通过，同一 codex 会话 `--fresh`→`--resume`，无降级）；独立 subagent review 结论 **APPROVE**（0 CRITICAL/0 HIGH），其 MEDIUM/LOW 建议（护栏升级为"宿主即 SIM Camera Settings 组" + 显式化空闲格假设）已采纳。**验证**：`pytest tests/test_main_window_sim_footer.py -q` → 2 passed；`pytest tests/ -q` → 369 passed, 192 subtests passed；`unittest discover -s tests -q` → Ran 363, OK。仅改 `tests/test_main_window_sim_footer.py` 与 `control_wangbo/main.py`（各经基线 `git diff` 隔离确认无越界），未碰 `.ui`、`CellSorting_ui.py`（生成文件）、`reconstruction/*` 或其它 `control_wangbo/` 文件。**真机/人工待确认**：GUI 冒烟确认 `cmb_sCMOS_laser` 在实际主窗口 SIM Camera Settings 组 `(2,1)/(3,1)` 显示、含 405/488/561/638 默认 488、切换触发 `on_sim_camera_setting_changed` 写回 `selected_laser_nm`，且不挤压现有控件。
- 2026-06-24：按用户要求为 Fast Camera 的 `Collected Cells ROI` 增加旋转角度，并确保 UI 显示和内部识别使用同一张旋转后的 ROI 图像。`control_wangbo/CellSorting_ui.ui` 在 Collected ROI 组内新增 `spb_collectedROI_angle`（`-180..180`，默认 0）和 `Angle` 标签，并重新生成 `control_wangbo/CellSorting_ui.py`；`control_wangbo/main.py` 将角度纳入配置保存/加载、参数下发、live ROI 轮廓绘制、Background Diff 的 Collected 小图生成和 ROI 记录字段，且 Collected Background Diff 小图使用与 `FastCameraThread` 相同的 3x3 椭圆形态学核；`control_wangbo/FastCameraThread.py` 在 `imageProcessing_way == 4` 时用同一个 `crop_rotated_roi()` 对背景帧和当前帧做旋转裁剪，再执行高斯模糊、`absdiff`、阈值和形态学识别，因此 Collected ROI 的算法输入与 UI 中显示的旋转 ROI 保持一致。新增 `control_wangbo/roi_geometry.py` 统一维护旋转 ROI 点位、裁剪和绘制；新增 `tests/test_rotated_roi_geometry.py`，并在 `tests/test_ui_regressions.py` 增加 Angle 控件回归。未修改 SIM camera 操作、Sort ROI 复活逻辑、Release+Sort/MCU 分选时序或非 Collected ROI 的轴对齐识别路径。验证：`.\.venv\Scripts\python.exe -m compileall -q control_wangbo\roi_geometry.py control_wangbo\FastCameraThread.py control_wangbo\main.py control_wangbo\CellSorting_ui.py tests\test_ui_regressions.py tests\test_rotated_roi_geometry.py` 通过；`.\.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py tests\test_rotated_roi_geometry.py -q` 通过（36 项、167 个 subtests）；`.\.venv\Scripts\python.exe -m pytest tests\test_main_window_scroll_area.py tests\test_legacy_sim_config_migration.py -q` 通过（25 项）；`git diff --check -- ...` 通过（仅 `CellSorting_ui.py` CRLF 规范化提示）。待真机/人工确认：旋转角度的正负方向是否符合现场直觉、边界外插采用 `BORDER_REPLICATE` 是否适合实际视野边缘、旋转裁剪后 Collected ROI 的面积/圆度阈值是否需要重新标定。
- 2026-06-24：按用户要求继续清理 Fast Camera 背景差分 ROI 显示区的 Sort 残留标签并对齐 Collected 显示：`control_wangbo/CellSorting_ui.ui` 删除 Background Diff._Fast Camera 区域的 `lb_cameraState_48`（文本 `Sort`），将 `lb_collectedROIView_BgDiff_Bi` 从 `gridLayout_4` 的 row=3,column=2 移到 row=3,column=1，并把 `lb_cameraState_49`（文本 `Collected`）调整为 85 px 宽、居中显示，使标签与 85x85 Collected ROI 图像中心线对齐；随后重新生成 `control_wangbo/CellSorting_ui.py`。同步在 `tests/test_ui_regressions.py` 增加回归测试，断言 `lb_cameraState_48` 不存在、Collected 标签与图像宽度和中心线一致。本次未修改 `main.py`、`FastCameraThread.py`、`MCUTriggerThread.py`、Rinse Channel 的 `Sort` 按钮、Release+Sort 或 SIM camera 逻辑。验证：`.\.venv\Scripts\python.exe -m compileall -q control_wangbo\CellSorting_ui.py tests\test_ui_regressions.py` 通过，`.\.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py -q` 通过（32 项、167 个 subtests），`git diff --check -- control_wangbo\CellSorting_ui.ui control_wangbo\CellSorting_ui.py tests\test_ui_regressions.py` 通过（仅 `CellSorting_ui.py` 行尾规范化提示）。独立 subagent review 未发现 blocking；仅提示当前工作区混有此前未提交 UI/测试 diff，提交前需确认范围。
- 2026-06-24：按用户要求删除历史微流控主界面中的 Sort ROI 界面入口和主窗口显示代码：`control_wangbo/CellSorting_ui.ui` 删除左侧 `grp_sortROI`、中部 `grp_sortROIView` 和背景差分中的 Sort ROI 显示块，并重新生成 `control_wangbo/CellSorting_ui.py`；`control_wangbo/main.py` 删除 Sort ROI 按钮状态/绑定、配置读写、ROI 显示映射、live overlay、背景差分显示、`imageProcessing_way == 3` 的主窗口显示分支和对应统计计数；左列布局收紧，`Collected Cells ROI` 与 `Flow Rate Detection ROI` 上移，UI 回归测试同步改为断言 Sort ROI 控件和标题不存在。为避免触碰 Fast Camera 线程业务逻辑，`main.py` 仅保留 `sortROI_X/Y/width/height=0/0/1/1` 隐藏兼容字段供 legacy `FastCameraThread` 参数接收，不再由界面控制，也不参与主窗口显示/分选路径。未修改 `FastCameraThread.py`、`MCUTriggerThread.py`、`Release+Sort`、硬件 `Sort` 按钮或 SIM camera 逻辑。验证：`.\.venv\Scripts\python.exe -m compileall -q control_wangbo\main.py control_wangbo\CellSorting_ui.py tests\test_ui_regressions.py` 通过，`.\.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py -q` 通过（31 项、167 个 subtests），`git diff --check -- control_wangbo\CellSorting_ui.ui control_wangbo\CellSorting_ui.py control_wangbo\main.py tests\test_ui_regressions.py` 通过（仅 `CellSorting_ui.py` 行尾规范化提示）。独立 subagent review 未发现 blocking；待用户确认是否需要另开任务继续清理 `FastCameraThread.py`、`lastConfiguration.json` 或文档中的 legacy Sort ROI 残留。
- 2026-06-24：按用户要求强化「Codex 双循环审查工作流」的**会话连续性**——同一任务的所有 codex 审查在**同一 codex 会话**内完成（首次 `/codex:rescue --fresh`、后续每轮 `--resume` 续接），不同任务各自 `--fresh` 开独立会话；第二循环主审查从 `/codex:review` 改为 `/codex:rescue --resume` 续接（让 codex 对照它已批准的最终方案、基于 `git diff <基线 ref>` 审实现），`/codex:review --base` 降为可选的独立结构化复审。**关键限制**（独立 subagent review 抓出 2 个 blocking 已修）：`--resume` 实为 `task --resume-last`，只续“最近一次”codex 调用、不能按任务 id 寻址，故必须**串行处理任务**、不在同一 Claude 会话交错多任务，否则 `--resume` 接错会话；同时修掉「判定终止」节与新口径冲突的旧句“实现验证统一用 `/codex:review`”、汇报口径改“实现验证轮数”、第二循环补 review-only 声明。仅改 `AGENTS.md`、`docs/project_memory/decision_log.md` 与记忆文件。
- 2026-06-24：按用户要求配通 OpenAI 官方 Codex 插件并把“Codex 双循环审查工作流”固化为 `AGENTS.md` 长期规则。**环境**：`npm install -g @openai/codex` 装 `codex-cli 0.142.0` 进 PATH（`%APPDATA%\npm`，当前 Claude 会话 PATH 已含、无需重启），复用 `~/.codex` 现有 ChatGPT 登录；0.142 正式版支持 `service_tier="priority"`，**未改** `~/.codex/config.toml`（仅旧 `.sandbox-bin` alpha `0.119` 不认 priority）；`node codex-companion.mjs setup --json` 复检 `ready:true`、`auth.loggedIn:true`。**规则**（`AGENTS.md` 新增“## Codex 双循环审查工作流（代码修改强制流程）”节，置于“## 修改后审查机制”之后）：对 `sim_control/` 源码、`config/*.json`、`tests/`、波形/时序、重新生成的 `ui_*` 等实质性修改必须走 第一循环 `/codex:rescue` 方案审查（codex 按 blocking/non-blocking/待确认三类输出，blocking 清零即通过）+ 第二循环 `/codex:review` 实现验证（核对本次任务对照基线的 diff）；各循环默认上限 5 轮、到限交用户裁决；codex 全程只读、改动由 Claude 执行；diff 范围限定本次任务并排除无关未提交改动；codex 误判可附理由申辩复审一次；用户显式授权或 codex 不可用经确认可降级并记入摘要；stop-time review gate 默认不启用；与既有 subagent review 叠加，任一存在 blocking 都不算完成；纯记忆文件/文档微调豁免双循环但不豁免 subagent review。该规则本身已用 codex 跑两轮方案审查定稿（v1→18 点意见全部吸收，v2→0 blocking）。决策见 `docs/project_memory/decision_log.md` 2026-06-24。本次仅改 `AGENTS.md`、`docs/project_memory/decision_log.md` 与本记忆文件 + 系统层 npm 全局安装 codex，未触碰任何源码、配置、`reconstruction/*` 或 `control_wangbo/`。
- 2026-06-24：实现主界面「immediate-RO 实时找样品 + 采集波长下拉迁移 + SIM9 采集按钮」三件套（经 25 轮 Codex review 收敛的方案，计划见 `C:\Users\user\.claude\plans\gui-sim-slm-pattern-mask-sim-slm-f-488-joyful-wreath.md`）。**动机**：正式 SIM9 RO 全是 `[HWA h]` 外触发，实时预览（相机 free-run、不发 DAQ 触发）下 SLM 不出图→画面黑、无法找样品。**核心改动**：①**找样品 immediate RO**：`sim_control/adapters.py` 加 R11 激活类型常量（`ACT_IMMEDIATE=0x01`/`SOFTWARE=0x02`/`HARDWARE=0x04`，源自 `SDK/.../rpc.h:16`）、`is_immediate_activation_type`（仅 ==0x01）、`parse_leading_wavelength_nm`、`R11_RpcRoSetSelectedBlind` 绑定 + `KopinSlmAdapter.list_running_orders_with_activation`（记录/恢复选中、try/finally、`activation_type=None` fail-closed、无 Blind 回退 plain select 并记 `fallback scan`）；`find_best_running_order` 加 `exclude_indices` 显式排除 immediate。`sim_control/controller.py` 加 immediate-live 私有运行态（不落盘）+ `refresh_immediate_running_orders`（后台连接 worker 内扫描并缓存）/`list_immediate_running_orders(wavelength)`（纯读缓存 + 前导波长过滤标 `selectable`）/`activate_immediate_running_order`（controller 级校验 index∈immediate 且波长匹配→**先记 arming line 再 set_line 高**，失败 best-effort `set_all_low` 后清态再抛）/`stop_immediate_live`（未激活严格 no-op，不写整 port）/`reset_all_daq_low`；`exclude_indices` 同时经 **worker payload** 与 `select_running_order_for_task` 传入；`disconnect_slm` 清 immediate 缓存。②**主 GUI**（`control_wangbo/main.py`，编程加控件、不重生成 `CellSorting_ui.py`）：`grp_hardvareConnection_2`(SIM Camera Settings) 网格空闲格 (2,1)/(3,1) 加波长下拉 `cmb_sCMOS_laser`（默认 488，采集波长唯一主入口，接入 sync 双向）；Connect SLM 右侧 `gridLayout_SLMConnection`(1,2) 加 `cmb_SLM_immediateRO`（首项「(none)」，按当前波长过滤、不匹配/不可解析项 disable）；Rinse Channel 与 Live View 间 `setGeometry(1290,158,200,36)` 加 `btn_sim9_acquire`(「SIM9帧采集」) 复用现有 `trigger_sim_formal_acquisition`。**激光安全状态机**（set_line 整 port 写→找样品激光绝不能与波形并存）：激光只在收到 `preview_started`（`sim_preview_started_confirmed`，不用早置位的 `sim_preview_active`/`controller.active`）后才点亮；未确认走两阶段 pending（`QTimer.singleShot` 捕获 token、取消/兑现 bump token 失配忽略、点灯前再验相机已连+下拉仍选同 RO）；`preview_stopped` 区分 planned restart（ROI/曝光内部重启保激光，reconfirm pending+看门狗，restart 静默失败即关）与异常停止（关激光复位下拉）；`on_sim_camera_setting_changed` 先存旧波长再判，immediate-live 期间不重选正式 RO，换波长先关激光再重过滤；强制关激光入口：停 Live/开 SIM9/断 SLM/换波长/开设置弹窗/`closeEvent`(存配置前、best-effort、不复位下拉)/异常/preview error。冷启动连接前 best-effort `set_all_low`（仅 controller 不忙时）。③**弹窗**（Agent B）：删 Laser 页（`.ui` + 重生成 `ui_sim_settings_dialog.py`），`_selected_laser_nm()` 改读 `config.selected_laser_nm`，Recon 波长下拉解耦（仅配 OTF、不回写 `selected_laser_nm`），保留模块级 helper（独立 `SimControlWindow` 仍用）。**测试**：新增 `tests/test_immediate_live.py`（17 项：激活类型/前导波长解析、扫描只筛 0x01、exclude_indices 含 worker 路径、激光线拉高/拉低/未激活 no-op、波长错配/非 immediate index 拒绝且不开激光、激活失败拉低、相机未连不开激光、下拉波长过滤 disable、disconnect 清态）；更新 `test_simulation_mode.py`（仿真 RO 28→30，末尾追加 `488/561_3.5_2d_live_imm`，index 7 不变）；`test_ui_regressions.py` Laser 页移除断言（由 Agent B 更新）。给现有 fake-self 测试用的方法加 `getattr+callable` 守卫（沿用本文件既有 `refresh_sim_slm_devices` 模式，真 MainWindow 不受影响）。**验证**：全量 `unittest discover` **Ran 360, OK**；`MainWindow` 离屏构造冒烟通过（波长下拉落在网格 (3,1)、SIM9 按钮 (1290,158,200,36)、下拉波长过滤 488 enabled/561 disabled）。独立 subagent review 结论 **APPROVE WITH COMMENTS**（0 CRITICAL/0 HIGH，7 项激光安全不变量全满足且有测试），已据其修掉 3 MEDIUM（pending token 真正校验 + 点灯前再验相机/RO；看门狗改捕获-token `singleShot` 无竞态；`disconnect_slm` 清 controller immediate 态）+ 1 LOW（closeEvent 不复位下拉）。**前置（用户手动/MetroCon）**：把 immediate RO 合并进 `patterns/2d_3.5.repz11` 并烧录，内部 RO 名须用 `488_3.5_2d_live_imm` 这类前导波长、不撞正式正则的形式。**真机/人工待确认**：`ACT_IMMEDIATE` RO 选中(SetSelectedBlind)是否立即出图/是否仍需 `R11_RpcRoActivate`/是否真不需 EXT_RUN（若需则改组合 mask 静态写）、`SetSelectedBlind` 在生产 DLL 是否存在及扫描无副作用、8s 看门狗 vs Fusion BT 首帧延迟、持续拉高激光 TTL 安全规程（Oxxius 输出快门/相干性）、SIM9 按钮坐标与红框对齐、连接 SLM 与设置弹窗诊断波形并发时冷启动 set_all_low 的 busy 门是否够（`is_busy` 不覆盖弹窗测试波形）。仅改 `sim_control/`（adapters/controller/protocols/sim_adapters/gui/.ui/ui_*）、`control_wangbo/main.py`（用户授权）与 `tests/`，未碰 `reconstruction/*`、`CellSorting_ui.py`（生成文件）、`models.py`/`config_store.py`/config JSON。
- 2026-06-24：按用户要求把"沟通与回复语言"固化为长期规则，写入 `AGENTS.md` 新增章节"## 沟通与回复语言"（位于"## 任务执行方式（并行子代理）"之前）：每次执行任务前先读该节；回答用户与展示思考过程统一使用中文，除非用户在当前请求中明确要求其他语言；代码、命令、文件路径、标识符、API/函数名、英文专有名词和必要英文术语保持原样不强行翻译；该规则只约束面向用户的自然语言表达，不改变代码、注释、配置或提交信息既有语言约定。仅改 `AGENTS.md` 与本记忆文件，未触碰任何源码、配置、`reconstruction/*` 或 `control_wangbo/`。
- 2026-06-23：按用户要求删除 Rinse Channel 中无实际通道对应的 `Function` 按钮及其控制链路：`control_wangbo/CellSorting_ui.ui` 删除 `btn_rinseChannelFunction`，并重新生成 `control_wangbo/CellSorting_ui.py`；`control_wangbo/main.py` 删除对应信号、状态变量、按钮连接、互斥启停逻辑和进入/退出 Rinse 模式时的处理；`control_wangbo/MCUTriggerThread.py` 删除 `slot_btn_rinseChannelFunction()`，因此 Rinse Channel 不再存在向 MCU 写 `0xFD` 的 Function/给压通路。Rinse Channel 保留 `Capture`、`Release`、`Sort` 三个按钮及其原有互斥控制。验证：`rg -n "btn_rinseChannelFunction|signal_btn_rinseChannelFunction|slot_btn_rinseChannelFunction|rinseChannelFunction" -S control_wangbo tests` 无匹配，`rg -n "0xFD" -S control_wangbo\MCUTriggerThread.py control_wangbo\main.py` 无匹配，`.venv\Scripts\python.exe -m compileall -q control_wangbo\MCUTriggerThread.py control_wangbo\main.py control_wangbo\CellSorting_ui.py` 通过，`git diff --check -- control_wangbo\CellSorting_ui.ui control_wangbo\CellSorting_ui.py control_wangbo\MCUTriggerThread.py control_wangbo\main.py PROJECT_MEMORY.md` 通过。待确认：真实界面中 Rinse Channel 三按钮布局是否符合现场操作习惯。
- 2026-06-23：按用户确认的 Function 语义清理历史微流控主界面代码并修正手动按钮误入自动筛选的问题：`control_wangbo/main.py` 中手动 `Function` 按钮不再发 `signal_btn_triggerFunction` 到 MCU worker，而是在 UI 线程用 `QTimer.singleShot(spb_triggerFunction_time, ...)` 本地等待，等待结束只重新启用按钮，不读取 `chb_isTarget`，不发 `signal_isTarget`，因此不会触发 Release 或打开/关闭 Single Run；`slot_btn_triggerFunction_finish()` 仅保留给自动筛选状态机内的 Function 等待完成后使用，继续读取 checkbox 做目标/非目标判定。`control_wangbo/MCUTriggerThread.py` 删除未使用的 `send_data_function` 和旧 `signal_btn_triggerFunction_start`，保留自动筛选状态机内的 Function 等待，但明确不构造、不写出任何 Function 单片机串口指令。验证：`.venv\Scripts\python.exe -m compileall -q control_wangbo\MCUTriggerThread.py control_wangbo\main.py` 通过，`git diff --check -- control_wangbo\MCUTriggerThread.py control_wangbo\main.py` 通过；独立 subagent review 未发现阻塞问题，并确认旧 `signal_btn_triggerFunction` / `send_data_function` 路径均无命中。真机/业务待确认：手动 Function 按钮只作为等待时间后重新启用按钮是否完全符合现场操作习惯。
- 2026-06-23：按用户要求只修改 Fast Camera 相关的目标细胞自动分选路径：`control_wangbo/MCUTriggerThread.py` 中 `slot_finish_ROI_processing()` 的 `imageProcessing_way == 2` 分支不再调用旧 `triggerReleaseSort_autoVersion()` 直接发送 ReleaseSort，而是复用手动 `Rel+Sort` 按钮时序 `slot_btn_triggerReleaseSort()`，先发送长时 Release，再在 `releaseTime` 后切到 Release+Sort，并在 Release+Sort 写入后约 1 ms 进入 Collected ROI (`imageProcessing_way=4`)；手动 Rel+Sort 函数本体、SIM 侧、sCMOS 侧、非目标/空释放路径未改。验证：`.venv\Scripts\python.exe -m compileall -q control_wangbo\MCUTriggerThread.py` 通过，`git diff --check -- control_wangbo\MCUTriggerThread.py` 通过；独立 subagent review 未发现阻塞问题。真机/业务待确认：长 Release 到 Release+Sort 接管的实际串口/MCU 时序，Collected ROI 是否应在 ReleaseSort 发出后立即开始，以及自动复用手动函数带来的既有 UI/录像信号是否符合现场预期。
- 2026-06-22：按真实 Oxxius 红光通道与当前 DAQ 接线，把 SIM 侧第四路红光从项目内外的 `647` 全量迁移为 `638`，并将默认 NI USB-6423 接线改为 `Dev1`：`slm_enable=line0`、`slm_trigger=line1`、`slm_finish=line2`、`camera_trigger=line8`、`laser_405=line9`、`laser_488=line10`、`laser_561=line11`、`laser_638=line12`。配置 schema 升到 v12：`DaqLineConfig.laser_647_line` 改为 `laser_638_line`，`SUPPORTED_LASERS=(405,488,561,638)`，`ReconstructionConfig` 改用 `otf_638_path` / `estimated_params_638_path`，旧 `640`/`647` 字段和 `selected_laser_nm` 自动迁移到 638；`config/sim_control_config.json` 与 production example 已同步。GUI 的 DAQ Wiring、Laser 选择、Recon 波长、摘要、测试下拉和状态显示改为 `Laser 638` / `638 nm`，并从 `.ui` 重新生成 `ui_sim_settings_dialog.py`。SLM Running Order 对 638 nm 优先匹配 `638` RO，现场 repertoire 若仍只有旧命名则 fallback 到 `647` RO。验证：`compileall` 通过；局部 pytest（DAQ/testing、legacy migration、z-scan migration、UI regressions）`69 passed, 172 subtests passed`；补充 pytest（running_orders、config_validation、summary、efficiency、simulation）`50 passed, 6 subtests passed`；全量 `.venv\Scripts\python.exe -m pytest tests\ -q` 为 `346 passed, 197 subtests passed`。真机待确认：NI USB-6423 物理接线确实对应 line8/9/10/11/12，638 nm SMA 数字输入接到 `Dev1/port0/line12`，现场 RO 是否已改名为 638 或需继续依赖 647 fallback，以及 638 nm OTF/estimated params 标定文件路径。
- 2026-06-17：按 codex 多轮 review 跟进修复上一轮常驻热重建实现的 4 个真实 bug（均在 `sim_control/`，本轮不碰 `reconstruction/*`、`control_wangbo/`、`.gitignore`、UI/AGENTS、`models.py`/`config_store.py`/config JSON）。①**P1 关停握手竞态**：`gui.py` `closeEvent` 原先 emit shutdown 后立刻 `recon_thread.quit()`，`QThread.quit()→QEventLoop.exit()` 的 `interrupt()` 可能在派发 `slot_shutdown` 前就退出事件循环→drain 不保证。改为本地 `QEventLoop` 等 `signal_shutdown_finished`（窗口 `@pyqtSlot` handler + `done` 标志防"信号先到空等"、`QTimer.singleShot(8000)` 超时 fallback、`finally` 断连、`_closing` 防重入用 `event.ignore()`、`recon_thread.wait(2000)` 仍超时记 warning）。②**P2 startup 过早 import torch**：`slot_warmup` 原先无 OTF 也走 `warmup_environment()`→import torch。改为正常波长下**所有 OTF/`.mat` 检查在 `_get_warm_reconstructor()` 之前**，缺 OTF 直接 return（不 import 后端）；保留 `wavelength<=0` 哨兵做显式 import/CUDA 预热；saved+`fail` 缺 `.mat`→warning+return（不偷用 estimate 掩盖配置错误），saved+`estimate` 缺 `.mat`→以 `use_saved_params=False` 走 estimate 预热（避免 `reconstruct` 的 `_validate_file_path` 在 fallback 前抛错）。③**P2 `output_save_status` 语义**：emit 期值 `queued`→`requested`（已请求落盘，**终态 saved/failed 只由 `signal_reconstruction_saved` 给出**，不回填 metadata）。④**P2 后端 API 校验**：`_REQUIRED_RECONSTRUCTOR_METHODS` 补 `_load_estimated_params_file`/`_wiener_reconstruct_with_saved_params`，saved 路径方法缺失时加载期即清晰报错。新增/改测试：`test_efficiency_optimizations.py` `queued`→`requested`（断言+注释）+ 4 个 `slot_warmup` 回归（缺 OTF 不构造后端 / 哨兵走 warmup_environment / saved-estimate 传 `use_saved_params=False` / saved-fail 缺 .mat 跳过）；`test_sim_reconstruction.py` 加 `_load_backend` 缺 saved 方法报错回归。**验证（实跑）**：`pytest tests/ -q` **341 passed, EXIT=0**（11s）；`unittest discover` **Ran 338, OK**（10.7s，无 failure、无 flaky——上轮 `test_ui_regressions...program_patterns` 偶发顺序依赖与 `c10.dll` 噪声消失，证实 P2 修复去掉了 GUI 构造期的 torch import）。**首次真机 GPU 验证**：本机 F: `.venv` 实测 `torch 2.12.0+cu130`、`cuda=True`；`WarmSIMReconstructor` 在真实 9×1024×1024 SIM 栈（`reconstruction/image/...first9_stack.tif`，OTF `488OTF_512.tif`，NA=1.0）：call#0 冷 total_pipeline≈946ms（wall≈2290ms 含 CUDA/import），call#1–3 热≈563ms、`warm_cache_hit=True`——证实"首细胞冷、其余热"设计在真机+真数据成立。**重要发现**：`excitation_na=1.0`（demo 值）可重建；config 默认 `1.49` 在该数据上导致 `torch.linalg.inv` 奇异矩阵 `LinAlgError`（光学参数错→硬失败，pipeline 内会归为 `signal_reconstruction_failed`）——各波长 NA 基准需按真实光路确认，config 默认 1.49 对该样本不适用。独立 subagent review 结论 **APPROVE**（0 CRITICAL/0 HIGH）：P1 drain 握手经核实无死锁/无提前退出（`controller.shutdown()` 的 `QThread.wait()` 不泵 Qt 事件循环，cross-thread queued 的 `signal_shutdown_finished` 只能在 `loop.exec_()` 内派发，故 `done`-before-`exec_` 守卫正确、drain 必先于 `recon_thread.quit()`）；slot_warmup 启动确不 import torch；`output_save_status` 语义诚实；API 校验正确。按其 LOW 建议给 closeEvent 超时 timer 加 `loop.isRunning()` 守卫。**ty 类型检查已在本机 F: 跑过**（用 `uvx ty check --python .venv`，不污染 .venv；.venv 内未装 ty/pyright/mypy）：修复 `sim_reconstruction.py:526` 一处 `backend.torch` 的 `None`-收窄假阳（改用 `_load_backend()` 返回 Any）后 **All checks passed**；`pipeline.py`/`gui.py` 我改动的部分**无新增类型错误**——pipeline 仅剩既有 `cv2=None` 可选导入、gui 剩余 ~50 项均为既有 PyQt5 stub 的 `event` vs `a0` override 警告与 `Widget|None` 属性访问（项目级既有 pattern，非本次引入，未动）。真机待确认：关窗时全幅 `float32` TIFF 落盘 drain 是否在 8s/`_shutdown_timeout_s` 内、各波长 `excitation_na` 基准（默认 1.49 在 demo 数据上奇异崩溃，1.0 可用）。`docs/project_memory/decision_log.md` 本轮不新增（bugfix 非长期架构决策；主决策已记 2026-06-16）。
- 2026-06-16：按用户要求把"任务执行方式"固化为长期规则，写入 `AGENTS.md` 新增章节"## 任务执行方式（并行子代理）"（位于"## 修改后审查机制"之前）：每次执行任务前先读该节；复杂任务必须先分解为相互独立的子任务，再用多个 subagent 同时并行处理（子任务尽量无共享状态/无顺序依赖，有依赖则阶段内并行、阶段间串行，并在同一条消息里一次性发起多个 subagent 调用）；简单/单步任务不强行拆分；工具环境不支持并行 subagent 时须先向用户说明并请求确认，不得静默串行替代；该节与"修改后审查机制"互补（前者用于*执行*，后者用于*审查*）。仅改 `AGENTS.md` 与本记忆文件，未触碰任何源码、配置、`reconstruction/*` 或 `control_wangbo/`。
- 2026-06-16：把队友新版 GPU SIM9 Wiener 重建接入为**进程内常驻热重建**，解决"每个细胞只拿第一次（冷）速度"问题。根因：第一次慢是进程级一次性 GPU 预热（CUDA context、cuFFT plan、cuSOLVER、allocator、torch/EMD import），跨调用/跨新建实例在进程内保留，仅进程退出丢失——只要重建跑在常驻 GUI 进程并复用同一引擎实例，启动后只有第一个细胞冷、其余皆热。新增 `sim_control/sim_reconstruction.py`（唯一后端私有方法集成层，幂等 `_load_backend`+API 校验、`InMemorySIMWienerReconstructor.reconstruct_stack` 内存注入且不调引擎 `reconstruct()`/不写 `.mat`、`WarmSIMReconstructor` 常驻复用+自动首帧预热+OTF/params 指纹缓存+saved 容错、`reconstruct_sim9_stack` 兼容封装）。`sim_control/pipeline.py` `ReconstructionWorker` 改用热实例、**先 emit 后异步落盘**（有界单 writer 线程 + 队列；`slot_warmup(int)`/`slot_update_reconstruction_config(object)`/`slot_shutdown` 均 `@pyqtSlot`，关停置 `_shutting_down` 拒新任务并 drain）。`models.py` `ReconstructionConfig` 加 saved-params/异步落盘字段 + `estimated_params_path_for_wavelength`/`snapshot()`；`config_version` 10→11，`config_store._migrate_v10_to_v11`（旧配置无 `.mat`→`use_saved_params=false`）+ saved 缺 `.mat` 校验阻止采集。`gui.py` 新增 3 个 `pyqtSignal` 跨线程（启动预热、配置快照下发、关停握手），替换原直调 `set_reconstruction_config`。生产默认（用户确认）：真实实验 `use_saved_params=true`+`fail`+异步落盘；仓库默认 `use_saved_params=false`（仿真优先）；新增 `config/sim_control_config.production.example.json` 与 `docs/reconstruction_saved_params.md`。测试：删除已失效的 `tests/test_sim_wiener_wrapper.py`（旧入口），新增 `tests/test_sim_reconstruction.py`（fake-backend 覆盖校验顺序/saved-estimate 分派/fail-estimate fallback/热实例单例复用），重写 `tests/test_efficiency_optimizations.py` recon-worker 用例（warm reuse、emit-before-async-save、`output_save_status`、写盘失败非致命、关停 drain+拒新任务），迁移测试版本断言 10→11 并新增 v10→v11 迁移与 saved-params 校验用例。验证：`.venv\Scripts\python.exe -m pytest tests\ -q` 336 passed（EXIT=0）；`unittest discover -s tests` 332 OK（一次性 flaky 不复现，与本改动无关）；`py_compile` 全绿；本仓库未修改 `reconstruction/*`（引擎为队友交付）、未改 `control_wangbo/`。独立 subagent review 结论：核心机制/线程纪律/迁移安全/emit-before-save 均满足；其 HIGH"违反 reconstruction 只读"实为队友 working-tree 交付的误判（本会话未改引擎）；已修两处 MEDIUM writer 线程缺陷（`_restart_writer` 置空 self 引用导致的 None-race、关停后被排队重建重启 writer）并加回归测试。人工/真机确认项：各波长 `.mat` 标定与 saved-params 数值正确性、热/冷速度真机量化、保存队列容量/关停超时对全幅 TIFF 的充分性、生产模板 `camera.bit_depth=8`/全幅 ROI 是否符合预期、项目 `ty` 类型检查在本机不可用未跑。
- 2026-06-14：修复 SIM 采集设置弹窗 Z-Scan 页测试按钮和运行态布局抖动问题。`sim_control/sim_settings_dialog.ui` 将 `btn_zscan_test` 初始文本统一为英文 `Run Z-Scan Test`、运行提示改为“未运行。运行期间可点击 Cancel 取消。”并重新生成 `ui_sim_settings_dialog.py`；`sim_control/gui.py` 新增 Z-Scan 测试按钮文本常量与运行期宽度锁，worker 运行时临时固定 `group_zscan`、`group_zscan_preview`、`group_zscan_test` 当前宽度，结束后恢复原 min/max 宽度，避免按钮/状态文本变化挤压左右栏。新增/更新 UI 回归测试覆盖英文初始文本、按钮字体、运行态列宽稳定和完成后锁清理。验证：`.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py tests\test_sim_preview_restart.py -q` 通过（119 项、183 个 subtests）；`.venv\Scripts\python.exe -m compileall -q app.py sim_control tests` 通过；`git diff --check` 通过（仅打印既有 dirty 文件 CRLF/LF 警告）。`.venv\Scripts\python.exe -m unittest discover -s tests -q` 当前失败 11 项，均集中在会话开始前已有 dirty 状态的 `reconstruction/sim_wiener.py`/相关模块导入问题（`reconstruction` 缺 `sim_wiener` 属性、找不到 `sim_wiener_gpu_emdapp_batchInGroup_batchBetGroup`），与本次 GUI 改动无关；独立 subagent review 已覆盖 `.ui` 源文件、生成 UI、`gui.py`、测试和记忆文件，结论为本次 Z-Scan GUI 修改无阻塞问题，人工确认项仅为可选目标显示器/DPI 下的视觉确认；未修改 `control_wangbo/`、硬件链路、配置 schema 或重建链路。
- 2026-06-12（推送前收口）：按独立 subagent 发布前 review 的阻塞结论完成三项本地修复后准备推送 `dev`。（1）`sim_control/gui.py` 的 Z-Scan 测试 worker 不再在工作线程内刷新 Qt 标签：`_append_zscan_timing_history(..., refresh_preview=False)` 用于 worker 路径，主线程成功槽再刷新 preview；直接单元测试调用仍可在主线程刷新，保持既有测试合同。（2）`sim_control/sim_settings_dialog.ui` 压缩 Z-Scan 页与根布局边距/行距并重新生成 `ui_sim_settings_dialog.py`，使当前本机字体/DPI 下 `SimSettingsDialog` 高度回到回归阈值内，同时保留 Z-Scan 两栏与参数列对齐。（3）`.claude/commands/sim-code-review.md` 与 `.claude/commands/sim-apply-review.md` 去除复制维护项目硬件/目录边界的段落，改为显式以 `AGENTS.md` 为唯一项目规则来源。验证：`compileall -q app.py control_wangbo sim_control tests` 通过；`tests.test_ui_regressions` 30 项通过；`tests.test_sim_preview_restart + tests.test_review_followups + tests.test_sim_daq_testing` 119 项通过；`.venv\Scripts\python.exe -m unittest discover -s tests -q` 325 项通过；`git diff --check` 通过（仅保留 `ui_sim_settings_dialog.py` CRLF/LF 提示）。未纳入大量未跟踪 `.agents/skills/`、`.claude/skills/`、`skills-lock.json`、`test_out.txt`，这些判定为本机技能安装/临时产物，需另行明确后才版本化。仍需真机确认：诊断测试取消后的 TTL 拉低与 GUI 行为、真实 DCAM timestamp、Z-Scan/RO 恢复和 GPU Wiener 真机/目标环境表现。
- 2026-06-12（reflect-skills 续）：复盘最近 30 天会话（本仓库 19 个真实会话 + `E:\毕业文档` 7 个；其余工作区各 1 个，无法构成重复模式）。确认上一轮已固化的 `sim-code-review`/`phd-qual-report`/`skills-manager` 已覆盖三大高频簇，本轮只补两个**确实缺失且互补**的工作流：（1）新增项目级 `.claude/commands/sim-apply-review.md`——把"审查报告落地修复"全链路固化（逐条分流→保功能修复→跑测试→独立 subagent 复审→记忆同步），源自 `e578e58e` 多轮落地修复 + `c501a6e2` 按 DCAM SDK 证据核对修正注释；它补 `sim-code-review`（只审不改）的下游半边，并与既有 Codex 侧 `.agents/skills/sim-autosystem-change-closure`（纯收尾清单）分工——本命令管上游 triage+apply，收尾三步复用后者，不重复另造。（2）全局新增 `~/.claude/commands/phd-defense-talk.md`——博资考/组会"看图说话"答辩讲稿 + 逐页 PPT 配图方案（文献图复用/改绘 + 来源标注 + 讲者备注），源自 `7f8df98b`/`d3733ed0` 及 06-04 十余轮迭代（汇总产物 `E:\毕业文档\第一部分背景_讲稿与配图方案.md`）；它补 `phd-qual-report`（书面报告）的口头答辩半边，且与 `nature-paper2ppt`（单篇论文→.pptx）边界清晰——本命令是 plan 层，成品渲染委托 nature-paper2ppt/ppt-master。刻意跳过（证据不足/已覆盖）：SIM 采集图像-时序调试与 SIM 光路图绘制（各仅 1 会话）、技能安装/盘点与环境弹窗排障（已由 skills-manager 与跨会话记忆覆盖）。独立 subagent review 结论 **PASS-WITH-NITS**（报告存仓库外 `C:\Users\user\.claude\plans\reflect-skills-review-2026-06-12.md`，已实跑测试与核对王波 API 名/路径均真实）：据此把 sim-apply-review 测试基线由硬编码"285"改为"以实跑 `Ran N` 为准"（review 实跑当前为 325），并给 HTML 模板示例加"若存在"。subagent 最终消息仍被工具环境吞掉，沿用"写文件 + 主 agent 读回"。本次仅新增/微调 AI 命令文件与本记忆，未改任何 SIM 源码、配置或 `control_wangbo/`。
- 2026-06-11（reflect-skills）：运行 `/reflect-skills --all-projects` 复盘最近 14 天全部工作区会话（真实用户会话：本仓库 18 个、`E:\毕业文档` 7 个；其余工作区各 1 个，无法构成重复模式），把高频重复工作流固化为 3 个 slash 命令。新增 SIM 项目级命令 `.claude/commands/sim-code-review.md`（“只审查不改代码”的 PyQt5/显微成像代码审查，源自 `2910078a`/`a0d8b3e2` 两次逐字重复粘贴的同一审查 prompt，内置 8 条约束 + `control_wangbo` 只读 + 实时路径禁同步 I/O + 配置驱动 + latest-frame-wins + SLM 生命周期共享等项目硬约束；`allowed-tools` 故意不含 `Edit`，`Write` 仅用于写审查报告）。另在全局 `~/.claude/commands/` 新增 `phd-qual-report`（博资考书面报告 pipeline，串联 ars-full/nature/cnki/zotero/docx）与 `skills-manager`（skills/plugins/MCP 盘点与安装），二者不属本仓库。本次仅新增命令文件，未改任何源码、配置或 `control_wangbo/`。独立 subagent review 通过（前几次 subagent 最终消息被工具环境吞掉，改用“写文件 + 主 agent 读取”绕过，结论存临时文件 `…\Temp\reflect_skills_review.md`）：3 文件 frontmatter 合法、`sim-code-review` 与 `AGENTS.md` 硬约束逐条一致、未硬编码可能错误的学校名（review 实读 `E:\毕业文档\docs` 确认考核单位为“北京大学未来技术学院”，仓库路径里的 “NanKai” 仅为本地目录名，已在 `phd-qual-report` 补注说明，命令设计为运行时从 docs 读取学校/字数/章节而非凭记忆断言）。环境注记：本机 `python3` 解析到失效的 WindowsApps stub，跑脚本须用 `python` 或项目 `.venv\Scripts\python.exe`。
- 2026-06-11（晚）：按 Codex 审查残留项清单完成 #1/#6/#8/#10/#24 的完整修复（#17/#18 按审查意见保留为 GPU 环境/真机研究项）。（1）#1 取消路径补全：`gui.py` `_run_zscan_stage_only_test` 加 `stop_event` 参数（每次移动前检查点，取消抛 `HardwareError`，finally 仍尽力回起始层；取消不写 timing history，`_PulseTestWorker` 在 stop_event 置位时静默吞异常不弹错误框）；`_run_camera_trigger_test` 启动前检查 + 把 `stop_event` 透传 `read_frame_sequence`；`_run_laser_pulse_test` 预先置位跳过 + 透传 `pulse_line`；`pulse_line`（`protocols.py`/`adapters.py`/`sim_adapters.py` 三处）加可选 `stop_event`，`adapters.py` 新增 `_interruptible_sleep`（`stop_event=None` 退化单次 sleep 行为零变化；提供时算术倒计时分片 50ms，取消即提前结束高电平，finally 保证拉低）。（2）#6 收尾：`SimControlWindow._initialize_camera`/`_program_patterns` 改 `_PulseTestWorker` 异步模式（新增 `_cam_init_*`/`_program_patterns_*` 状态变量与 6 个槽；`_program_patterns` GUI 线程快照 `list(pattern_files)`）。（3）#8 完整修复（取代此前"缓解态"决策，详见 decision_log 2026-06-11）：`preview.py` 删除 `processEvents` 泵循环与 `QCoreApplication/QEventLoop` import；worker 新增 `_stopped_event`（finally 先 emit stopped 再 set）与 `_generation` 代数戳；controller `stop(wait=True)` 改 `wait_until_stopped(2.0)` + 本地收尾，`_handle_worker_status` 过滤旧代数陈旧信号并在对外转发前剥掉 `generation` 键（外部 payload 合同不变，main.py 订阅者零改动）。（4）#24：DCAM `read_frame_sequence` 优先 `buf_getframe`（一次 copyframe 同取像素 + `DCAMBUF_FRAME.timestamp`），无该方法回退 `buf_getframedata`；新增 `FusionBtCameraAdapter.get_last_hardware_timestamps()`（缺方法/缺字段/整组全 0 均置 None，返回副本），返回值软件时间戳 `time.time()` 语义不变；`SimulatedCameraAdapter` 加恒 None 同名方法。（5）#10：`test_sim_preview_restart.py` 新增模块级 `_wait_for_condition` 事件驱动等待（本机 PyQt5 无 `QTest.qWaitFor`），4 处固定 `qWait` 改条件等待，新增 5 个取消测试；`test_sim_daq_testing.py` 新增 3 个 pulse_line 取消测试；`test_sim_camera_adapter.py` 新增 3 个硬件时间戳测试（含全 0→None）+ 回退断言；`test_ui_regressions.py` 新增 2 个 worker 线程身份测试（`threading.get_ident` ≠ GUI 线程）。验证：`compileall` 通过；分批 unittest 全绿（39+103+58+52+72=324 项，含最厚的 preview 状态机测试集）。独立 subagent review 报告存仓库外 `C:\Users\user\.claude\plans\review-codex-residual-fixes.md`：重点项 A–E 全部满足、无功能性缺陷；其指出的"固件留 0 不抛异常→epoch-0 假时间戳"风险已当场加固（整组全 0 置 None + 测试）。注意：本会话前三次 subagent 调用的最终消息被工具环境吞掉（agent 实际完成了工作但只回传占位语），最终用"报告写文件 + 主 agent 读取"绕过；后续会话遇到同样现象可直接采用该方案。真机遗留：连续快速"停止→启动"预览压力测试（generation 时序）、真实 DCAM `buf_getframe`/timestamp 字段行为、激光脉冲取消到 TTL 拉低的示波器延迟。
- 2026-06-11：补全之前会话残留的测试修复（均在 `tests/test_sim_preview_restart.py` 与 `sim_control/preview_contrast.py`）。具体改动：（1）`fast_auto_preview_uint16_to_uint8` 改为 resize-before-LUT（删除冗余 `np.clip`，与 manual 路径对称；LUT 内置 `np.clip(...,0,255)` 隐式处理边界），`tests/test_preview_contrast.py` 同步更新测试名称与断言。（2）Z-Scan 测试 worker 化后三处 `with mock.patch(...)` 块内补 `QTest.qWait(500~600)` 等待 worker 完成（`test_zscan_test_button_click_ignores_qpushbutton_checked_argument`、`test_zscan_stage_only_restore_failure_uses_warning_dialog`、`test_zscan_failure_message_mentions_possible_zscan_running_order`），将 `processEvents()` 替换为 `qWait`，保证 mock 在 worker 信号 dispatch 时仍为 active。（3）`test_zscan_failure_message_mentions_possible_zscan_running_order` 补充 `mock.patch.object(dialog, "_current_daq_config")` + `mock.patch("sim_control.gui.validate_daq_line_config")` 绕过 GUI 线程双重校验（`_current_daq_config()` 因控件未初始化在 `read_daq_config_from_line_combos` 内就抛错，早于 `validate_daq_line_config`）；`_fail_after_select` 函数签名加 `**kwargs` 接受 `daq_config_override/stop_event`。（4）`test_sim_acquisition_pulse_message_shows_actual_and_daq_durations_from_command_start` 补 `selected_laser_nm=mock.ANY`（`_run_pulse_test` 已快照 `selected_laser_nm` 并以关键字参数传入）。验证：116 项全部 OK（`tests/test_preview_contrast + test_sim_preview_restart + test_legacy_sim_config_migration + test_z_scan_config_migration`）。独立 subagent review 通过，指出两项业务判断待确认：① `qWait` 固定时延在高负载 CI 上有 flaky 风险（建议 `qWaitFor` 事件驱动，当前接受现状）；② `resize-before-LUT` 在含荧光球离群帧上预览视觉需真机确认。`ZScanConfig.enabled` 默认 `True`，review 对默认值的担忧不成立。
- 2026-06-11：经用户条件批准（不改功能逻辑、不改王波代码功能），实施审查报告条目 #1/#6/#7/#8/#12/#13/#15/#22；随后由独立 subagent review 补修两项遗漏缺陷。具体改动：`sim_control/gui.py` 新增 `_PulseTestWorker` 类（一次性 QObject，支持取消）、将 `_run_pulse_test` 重构为异步 worker 派发（按钮切换为 Cancel、完成/错误/取消均走槽函数）、新增 `_execute_sim_capture_sequence` 采集序列 helper（消除 `_run_sim_acquisition_test` 与 `_run_slm_activation_timing_test` 的代码重复），并在 `_run_single_acquisition` 入口增加 `controller.is_busy` 重入防护。`sim_control/controller.py` 新增 `is_busy` 属性（`bool(self._active_stop_events)`），payload 组装时对 `timing`/`camera`/`daq_config` 等可变字段做 `copy.copy()` 快照防竞态。`sim_control/preview.py` 将 `processEvents()` 改为 `QCoreApplication.processEvents(QEventLoop.ExcludeUserInputEvents)`，排除停止等待期间的用户输入事件。`sim_control/preview_contrast.py` 的 `fast_manual_preview_uint16_to_uint8` 改为先 resize 后 clip（resize-before-clip），减少下采样前无效 clip 开销；已知权衡：INTER_AREA 均值会稀释极亮像素使其 clip 前低于阈值，属可接受的预览质量代价。`sim_control/focus_metrics.py` SML 计算改用 `float32`（原 float64），消除差分运算后不必要的精度提升并降低内存占用。`control_wangbo/main.py` 新增 `_ConnectWorker(QObject)` 类，将 `toggle_sim_slm_connection` 和 `btn_sCMOS_connection_function` 的连接分支移出 GUI 线程（worker 完成/错误/取消均走 QueuedConnection 槽函数）；为防止 GC 提前回收在用的 QThread/QObject，在 worker 启动后立即赋给 `self._slm_connect_thread`/`self._slm_connect_worker` 和 `self._cam_connect_thread`/`self._cam_connect_worker`，`_on_*_finished` 中清 None。独立 subagent review 后修补两项缺陷：（1）`_run_pulse_test` 闭包捕获的 `daq_config` 补 `copy.copy()` 快照，新增 `import copy`；（2）两个连接函数入口增加 `if getattr(self, "_*_connect_thread", None) is not None: return` 守卫，防止绕过按钮的路径覆盖旧 thread 引用。测试结果：`tests/test_sim_preview_restart.py` 三个相机连接测试均补齐 `btn_sCMOS_connection=mock.MagicMock()` 与 `QTest.qWait(200)`；发现 PyQt5 GC 崩溃（exit 127）根因为 local QThread 提前析构，通过实例变量保活修复；`unittest discover` 308 项全 OK。独立 subagent review 结论：Q1 八项标准均满足，Q2 两项遗漏缺陷已补修，Q3 剩余需真机确认或业务判断（`thread.wait()` 在真实硬件断连时的主线程阻塞时长、resize-before-clip 稀释效应对实验预览的影响、`refresh_sim_slm_devices()` 枚举边界）。本批对 `control_wangbo/main.py` 的修改属一次性条件批准，不改变其默认只读边界。
- 2026-06-11：按代码审查报告（第 2 版）继续实施经用户批准的低/中风险条目 #1/#3/#5/#10/#11/#14/#16/#20/#21/#23（约束：不改功能逻辑、不动 `control_wangbo/`）。#3+#11 schema v9→v10：`BackendConfig` 新增 `ti2_dll_path`/`ti2_sdk_module_path`（默认空串），`adapter_factory` 以「路径 or None」透传；新增 `_migrate_v9_to_v10`（setdefault 两键，幂等不覆盖用户已填）；`models.AppConfig.config_version` 默认与 `CURRENT_CONFIG_VERSION` 统一为 10，`app_config_to_dict` 写盘前强制打当前版本号防默认值漂移；`config/sim_control_config.json` 同步到 v10（backend 加两空串字段）。#5：新建 `sim_control/errors.py` 定义 `HardwareError(RuntimeError)`，`adapters.py` re-export（既有 `from sim_control.adapters import HardwareError` 不破）；`sim_adapters` 硬件状态错误统一抛 `HardwareError`（read_preview / read_frame-armed / RO out-of-range / program / activate / get_state），但 `read_frame_sequence` 取消信号仍抛裸 `RuntimeError("Acquisition cancelled.")`（由 acquisition_core 归一化为 AcquisitionCancelled，勿改）；`SimulatedCameraAdapter`/`SimulatedSlmAdapter` 加 `strict_connection`（默认 False 保持自动连接，True 时未连接抛 HardwareError）。#23：`validate_app_config` 的 ROI 边界改引用 `DEFAULT_SIM_CAMERA_SIZE`（值仍 2304）。#16：`SimulatedDaqAdapter` 加 `realtime=False`，默认仍截断 100ms，`realtime=True` 走全时长（仅 GUI 冻结/取消类测试显式启用，默认不可改）。#14/#20：`NIDaqAdapter.list_*` 与 `gui` 测试清理的 `except` 补 `logger.debug`/`logger.warning(exc_info=True)`（控制流不变，gui 新增 module logger）。#21：`controller.shutdown` 超时分支加 docstring 说明「不强断硬件以免与卡死 worker 竞争 R11/DCAM 句柄」的取舍（行为不变）。#1：诊断测试入口 `_run_pulse_test` 加 UX 缓解（禁 `btn_pulse_test` + WaitCursor + `processEvents(ExcludeUserInputEvents)` + finally 按 `cursor_overridden` flag 平衡恢复光标），不动测试逻辑本身；属临时缓解，真正方案仍是测试 worker 化（带取消、进度、取消后 DAQ 全低）。#10：新建 `tests/test_review_followups.py`（HardwareError 契约 / strict 拒绝 / 异常统一 / realtime / Ti2 透传 / `(9,H,W)` 集中合同 / raw 不进 GUI payload 负面断言 / preview 负载丢帧，共 20 项）；更新 `test_legacy_sim_config_migration`、`test_z_scan_config_migration` 的版本号断言 9→10、`BackendConfig` 字段集合加 ti2 两字段、新增 v9→v10 与默认版本号钉死。验证：`compileall` 通过；`unittest discover` 308 项全 OK（基线 285 + 新增 23）。独立 subagent review 通过（结论 `C:\Users\user\.claude\plans\review-of-impl-batch2.md`，无功能缺陷、两条硬约束满足）；已按 review 建议把 #1 的 `setOverrideCursor` 纳入 try + flag 保证 push/pop 平衡。真机遗留：Ti2 自定义路径端到端加载、UX 缓解在真实长测试中的表现、`simulation_mode=True` 下 preview 停止不再产生非预期 `signal_error` 的 GUI 联调确认。本批未触碰 `control_wangbo/`。
- 2026-06-11：将 `sim_control/adapters.py` 中 `FusionBtCameraAdapter.read_preview_frame` 的取帧注释（原「SDK 直接返回 NumPy 视图」）更正为与 DCAM 样例实际行为一致。经核对 `SDK\Hamamatsu_DCAMSDK4_v25056964\dcamsdk4\samples\python\dcam.py`：`buf_getlastframedata()`→`buf_getframedata(-1)`→`buf_getframe()` 每帧由 `dcammisc_alloc_ndarray`（dcam.py:573）新分配数组并经 `dcambuf_copyframe`（dcam.py:587）拷贝填充，样例中**不存在** `dcambuf_lockframe` 路径，因此返回的是独立 NumPy 数组而非驱动环形缓冲视图，GUI 可安全长期持有该引用；后续 `np.asarray(frame, dtype=np.uint16)` 对已是 uint16 的拷贝数组零拷贝直通。纯注释修正，无任何代码逻辑/行为变化；兄弟路径 `read_frame_sequence`（走同一 `buf_getframedata`→`buf_getframe`→copyframe 链）注释「逐帧拷贝出」本已准确，未改。来源：2026-06-11 独立 code review（仓库外 `C:\Users\user\.claude\plans\review-of-impl-2-4-9-19.md` ③3 与风险点 D）。验证：`.venv\Scripts\python.exe -m compileall -q sim_control` 通过（EXIT=0）；两次独立 subagent review 均 APPROVE（均独立打开 dcam.py 核对调用链、grep 确认无 `lockframe`、确认 diff 仅此一处注释）。
- 2026-06-11：完成全仓代码审查报告（仓库外 `C:\Users\user\.claude\plans\python-pyqt5-stateful-balloon.md`）第 2 版修订：按用户 17 条反馈逐条对照代码核实，修正 4 处事实错误（#10 summary-only 合同测试已存在于 `tests/test_ui_regressions.py` 与 `tests/test_sim_preview_restart.py`、#19 "死字段"实被测试用作合同锚点、#11 Ti2 构造函数已支持路径参数、#17 本开发工作区 .venv 无 torch），#18 移出实施批次仅留真机研究项；独立 subagent review 通过（6 项代码事实抽查 6/6，结论存仓库外 `review-of-balloon-revision.md`）。随后经用户条件批准（不改变现有功能逻辑、不改变王波代码功能）实施审查条目 #2/#4/#9/#19：`sim_control/stage_adapter.py` 的 `Ti2ZStageAdapter` 加 `threading.RLock` 串行化全部 SDK 调用（`connect` 内部重入调用 `get_position_um`/`get_z_ranges_um`，必须用 RLock 而非 Lock）；`control_wangbo/main.py` 的 `poll_sim_stage_position` 增加 `force` 形参并在 `sim_acquisition_in_progress` 期间跳过定时器轮询（z-scan 完成槽改用 `force=True` 主动刷新一次），except 分支补 `logger.debug`（main.py 新增 `import logging` 与模块 logger）；主界面摘要刷新新增 `get_sim_zscan_timing_records()` 缓存（按历史文件 `st_mtime_ns`/`st_size` 失效重读，覆盖设置弹窗追加与外部进程写入）并传入 `build_sim_settings_summary(z_scan_timing_records=...)`（review 后加固：load 失败时 OSError 退回旧缓存，摘要刷新不因历史文件不可读而失败）；`_render_sim_preview_frame` 增加 `copy_cached_frame` 形参，SIM 轮询路径（`poll_latest_sim_preview_frame`）存引用、王波相机线程路径（`slot_updata_display_sCMOSCameraFrameIndex_FPS`）维持 copy 防御；删除恒为 None 的 `sim_last_acquisition_batch`（main.py 两处赋值）并把 `tests/test_sim_preview_restart.py` 合同测试改写为负面断言（summary 槽与 trigger 路径均不得出现该属性），删除 `SimPreviewWorker.__init__` 未使用形参 `gui_preview_fps_limit`（`SimPreviewController` 同名参数保留，仍驱动 GUI 轮询间隔）。验证：`compileall` 通过；`unittest discover` 修改前后均为 285 项全部 OK（此前记录为环境性失败的字体/DPI 用例在本开发工作区通过）。真机遗留：#2 Ti2 SDK 线程安全与加锁前后 z-scan 节拍对比、#12 DAQ 页口径、#18 RO 激活语义、#21 shutdown 超时复现。本次对 `control_wangbo/main.py` 的修改是一次性条件批准，不改变其默认只读边界。
- 2026-06-10：按用户要求将 `control_wangbo/sCMOSCameraThread.py` 整文件替换为 `C:\Users\user\Desktop\20260606 SIM使用程序\sCMOSCameraThread.py`，SHA256 校验一致（`FBDF03C94DA36C5185A68E1C7BDD4D41170692E5122315A7F59137740935D8B3`）。在不整文件覆盖 `main.py` 和 UI 的前提下，合并王波最新版主界面逻辑：`CellSorting_ui.ui` 将旧 `ElasticityMeasurement` 语义改为 `FunctionMeasurement`，`Sort Trigger` 改为 `Release+Sort`，`Image Processing Settings` 改用 `spb_trapFrames`、`spb_trapBalance_Time`、`spb_collectFrames`，并从 `.ui` 重新生成 `CellSorting_ui.py`；`main.py` 改接新版 `Function`、`Release+Sort`、`trapFrames/collectFrames/trapBalance_Time` 参数和 MCU 槽函数，保留当前 SIM camera/SLM/runtime/reconstruction/preview/Z-scan helper 与 slot，配置读取继续兼容旧键 `spb_trappedIntervalTime`、`spb_sortFrames`、`spb_triggerElasticityMeasurement_time`、`spb_triggerSort_time`。未修改 `FastCameraThread.py`、`MCUTriggerThread.py`、`PublicClassCamera.py`、`mvsdk.py`。验证：`.venv\Scripts\python.exe -m compileall -q app.py control_wangbo sim_control tests` 通过；`.venv\Scripts\python.exe -m unittest tests.test_sim_preview_restart -q` 通过；`.venv\Scripts\python.exe -m unittest tests.test_ui_regressions tests.test_main_window_scroll_area tests.test_main_window_sim_footer -q` 与 `.venv\Scripts\python.exe -m unittest discover -s tests -q` 均仅剩既有环境性失败 `test_sim_settings_dialog_uses_compact_geometry_and_expected_bold_fonts`（本机 SimSettingsDialog 高度 710 > 660，先前已记录为字体/DPI 问题）。独立 subagent review 已完成，未发现阻塞项；主 agent 随后清理了 `main.py` 末尾重复旧 `slot_sCMOS_Updata_elasticityTrigger` 对 Function 触发槽的覆盖风险。`git diff --check` 仅报告王波最新版 `sCMOSCameraThread.py` 自带尾随空格；为保持整文件 SHA 一致未清理。
- 2026-06-10：根据 SDK 文档闭环 2026-05-27 DAQ 页 SIM 采集测试「第一帧黑帧 + 图案 3+3+2 错位」的根因并落实修复。根因链：`patterns/2d_3.5.repz11` 内全部 RO 为 `[HWA h]` 硬件激活（PD0011CA p.16），软件 `R11_RpcRoActivate` 后 EXT_RUN（`slm_enable_line`）未拉高时系统仍在 Maintenance 黑屏（AN0027AD §3.26 状态 0x54 MHW）；EXT_RUN 拉高后需 tHWAT=5~500 µs 才进入 Active（PD0011CA Table 7-1 p.26），旧 `slm_enable_guard_us=50` 使首个 trigger 落在激活窗口内被丢弃，9 个图案整体后移一帧、第九图案丢失。trigger→图案点亮 = tTSS≈3.5 µs + 位平面装载，1-bit Lit Pair 序列 `illumStart≈270.19 µs`（sequence timing report）。修改：`TimingConfig.slm_enable_guard_us` 默认 50→1000 µs，`config/sim_control_config.json` 同步并升 schema v9，`_migrate_v8_to_v9` 把 <1000 的旧值抬到 1000；`waveform.build()/build_z_scan()` 对 guard≤500 µs 发 tHWAT 告警；`_R11CommLib` 新增 `R11_RpcRoGetActivationState` 绑定（旧 DLL 缺函数返回 None）、模块级 `R11_ACTIVATION_STATES` 表与 `KopinSlmAdapter.get_running_order_activation_state()`；`acquisition_core` 在 activate 后 best-effort 上报 `slm_activation_state` 事件（getattr 守卫，查询失败仅 warning）；`NIDaqAdapter` 新增静态 `set_line()` 且 `pulse_line` 复用之；`SimulatedSlmAdapter`/`SimulatedDaqAdapter` 同步仿真（软件激活即 ACT、记录 set_line/set_all_low 调用）；DAQ 页测试目标新增「SLM激活时序」（`_run_slm_activation_timing_test`：选 RO→activate→读初始状态→拉高 enable→perf_counter 轮询至 ACT/2s 超时→finally enable 拉低+DAQ 全 0）。测试：更新 7 个版本钉死迁移测试、DAQ 目标列表、pulse_line mock（3 任务注册）、summary 两处期望值（552.000 ms / 531.492 ms）；新增 v8→v9 迁移、waveform tHWAT 告警、set_line、激活时序流程（duck-typed host）、`tests/test_slm_activation_state.py`（绑定/包装/采集事件）共 18 项。验证：`.venv\Scripts\python.exe -m unittest discover -s tests -q` 285 项仅剩 1 个既有环境性失败（`test_sim_settings_dialog_uses_compact_geometry_and_expected_bold_fonts`，本机 SimSun 字体/DPI 所致，已用 git stash 在干净工作树复现确认与本次改动无关；本机 .venv 无 pytest），`compileall` 通过。需真机确认：用「SLM激活时序」实测 enable→ACT 延迟与初始 MHW 状态；重跑「SIM采集」验证第一帧有图、3+3+3 分组、第九图案齐全；z-scan 每层 +1.9 ms（2×0.95 ms guard）节拍影响可忽略是否属实。
- 2026-05-26：执行 Pro 审查第一批低风险修改，仅处理 adapter factory 收口、`sim_control.__init__` lazy export、cleanup/shutdown warning 日志和 z-scan waveform plan 缓存。新增 `sim_control/adapter_factory.py`，由 `AdapterBundle` 与 `create_adapter_bundle()` 统一创建 camera/slm/daq/stage adapter；`SimAcquisitionController._create_adapters()` 改为委托统一 factory，`sim_control.gui` 保留原 `create_*_adapter_for_backend` 名称但从 factory 导入，`SimSettingsDialog` 新增可选 `daq_adapter` 注入且默认仍新建 DAQ adapter。`sim_control.__init__` 不再顶层导入 controller/gui，改用 `__getattr__` lazy export 保留 `SimAcquisitionController` 和 `SimControlWindow`。`acquisition_core`、`z_scan_core`、`controller`、`adapters` 中既有 cleanup/shutdown `except Exception: pass` 改为 `logger.warning(..., exc_info=True)` 后继续吞异常；`run_z_scan()` 在首次 arm/activate 后构建一次 z-scan waveform plan 并复用，同时保留每步 `z_scan_waveform_warning` 回调。未实现布尔解析、single-flight、GUI 线程迁移、相机重复 apply 去重、preview 缓存、重建链路或 release/sort 语义修改；未修改 `control_wangbo/`。新增最小导入、adapter factory、cleanup 日志、DAQ adapter 注入和 z-scan plan 缓存回归测试。验证：受影响 pytest 通过（126 项、187 个 subtests），`.venv\Scripts\python.exe -m pytest tests\ -q` 通过（270 项、197 个 subtests），`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（267 项），`.venv\Scripts\python.exe -m compileall -q app.py sim_control tests` 通过，`git diff --check` 通过但提示 `tests/test_sim_preview_restart.py` 的 CRLF/LF 规范化 warning。独立 subagent review 已完成，指出的新文件未纳入普通 diff 和新增测试缺少 future import 已修复。需真机/人工确认：cleanup warning 日志量和可读性、z-scan 复用同一 `WaveformPlan` 在 USB-6423 连续多层播放中的等价性、后续是否真正共享 DAQ adapter 生命周期。
- 2026-05-25：回顾最近 30 天 Codex 会话索引、session/rollout、auto-skills 记忆和现有 skills/automation 资产后，确认高频且缺失的可沉淀流程是“SIM_AutoSystem 修改后的收尾：验证、三问式独立 subagent review、PROJECT_MEMORY 回写和人工/真机确认项整理”。新增项目内 repo skill `.agents/skills/sim-autosystem-change-closure/`，包含 `SKILL.md`、`references/review-prompt.md` 与 `agents/openai.yaml` 元数据，用于在后续代码、配置、文档、测试或工作流修改结束前统一执行收尾 checklist；未新增决策日志，因为该 skill 只是固化既有 `AGENTS.md` 修改后审查与记忆回写规则。验证：`python C:\Users\user\.codex\skills\.system\skill-creator\scripts\quick_validate.py E:\Intelligent_SR\SIM_AutoSystem\.agents\skills\sim-autosystem-change-closure` 通过；需后续观察：新 skill 在后续 Codex 会话中的触发与执行是否稳定。
- 2026-05-25：继续修正 Z-Scan 历史耗时模型和测试弹窗文案。Display 从单一“估算总用时”拆成 `预估总用时(仅位移)` 和 `预估总用时(位移+采图)` 两行；历史 JSONL 在兼容旧 step 记录的基础上新增 `record_type="run"` 的分模块 run-summary，记录固定开销、起始定位、按 scan gap 的层间移动、测试回第一层、每层采图非移动耗时、最佳焦面移动和 TIFF 写盘耗时。新增 `estimate_z_scan_move_only_total_time_ms()` 与 `estimate_z_scan_capture_test_total_time_ms()`，GUI 和 summary 改用明确口径，summary 字段改为 `estimated_move_only_time_ms` / `estimated_move_capture_time_ms`。`run_z_scan()` 完成最佳焦面移动后会上报 `z_scan_best_focus_positioned` 和 `move_ms`。位移台测试弹窗第二行改为 `步数: N`，移动统计改为 `每步移动耗时(min/mean/max)`，且只统计用户设置的层间移动次数；独立 review 后补齐旧 JSONL 无 `record_type` 的兼容读取回归测试。验证：`.venv\Scripts\python.exe -m pytest tests\test_z_scan_timing_history.py tests\test_z_scan_core.py tests\test_sim_preview_restart.py tests\test_ui_regressions.py tests\test_sim_summary.py -q` 通过（128 项、185 个 subtests），`.venv\Scripts\python.exe -m pytest tests\ -q` 通过（262 项、197 个 subtests）。需真机确认：两个 ETA 口径与真实测试弹窗总耗时的贴合程度。
- 2026-05-25：按用户计划实现 Z-Scan 历史耗时模型并移除 `Return to start on cancel`：新增 `sim_control/z_scan_timing_history.py`，将 Z-Scan Test 的每步移动耗时和可选完整采图 cycle 耗时持久化到 `data/z_scan_timing_history.jsonl`，Display ETA 改为优先使用相同 scan gap / 曝光 preset 的历史中位数，缺少精确 gap 时用已有 gap 做距离模型，无历史时回退默认模型；stage-only 测试和“位移台 + 每层采图”测试都会刷新 ETA。配置 schema 升到 v8，删除 `ZScanConfig.return_to_start_on_cancel`、设置 GUI 中的 Cancel 行和 checkbox，并从 v7 迁移时丢弃旧键；正式 `run_z_scan()` 取消时不再移动回起点，但仍通过采集清理路径 disarm/DAQ 全低并传播取消。`z_scan.enabled=False` 时正式 SIM9 不调用 Z-stage 或 `run_z_scan()`，直接在当前 Z 轴位置采集 9 帧。新增/更新历史模型、配置迁移、核心取消、GUI 和正式采集测试；为当前 Windows sandbox 下 `tempfile.TemporaryDirectory()` 目录不可写问题新增 pytest `tests/conftest.py`，将 pytest 临时目录重定向到已忽略的 `.codex_tmp_pyc`。验证：`.venv\Scripts\python.exe -m pytest tests\test_z_scan_core.py tests\test_z_scan_config_migration.py tests\test_sim_preview_restart.py tests\test_ui_regressions.py tests\test_sim_summary.py -q` 通过（123 项、181 个 subtests），`.venv\Scripts\python.exe -m pytest tests\ -q` 通过（258 项、193 个 subtests）。需真机确认：不同 scan gap 历史模型与真实 ZDrive 耗时贴合程度、取消保持当前位置后的人工恢复/后续采集流程、真实 DAQ 全低与相机 disarm 时序。
- 2026-05-25：修复 `patterns/2d_3.5.repz11` 中 `488_3.5_2d_zscan3p_20ms` 无法烧录的问题。根因是此前 `tools/add_zscan_ro.py` 把 3ms lit-pair sequence 挂到 `H+/H-`，R11 编译/烧录时 `H+` 映射到越界 sequence number 16；已改为复用未被正式 SIM RO 使用的 `D+/D-` 承载 `48039 3ms 1-bit Lit Pair ±.seq11`，并让脚本只在 `488_3.5_2d_zscan3p_20ms` block 内把既有坏包中的 `(H±,9/10/11)` 迁移到 `(D±,9/10/11)`。当前四个 Z-Scan RO sequence 选择为：5ms=`A±/48030 500us`，8ms=`F±/48037 1ms`，14ms=`G±/48038 2ms`，20ms=`D±/48039 3ms`。新增 `tests/test_zscan_repertoire_patch.py` 覆盖生成规则、既有坏 `.repz11` 迁移和当前包内容，防止回退到 `H±`。
- 2026-05-22：按用户要求把集成主 GUI `SIM Runtime` 中的 `Reconstruction` 改为 `.ui` 中的静态行，包含 `led_simRuntimeReconstruction`、名称标签和状态标签，状态圆点使用与 `Camera/SLM/DAQ` 完全一致的 16x16 `QLabel` + `border-radius: 8px` 样式；`control_wangbo/main.py` 正常路径改为复用静态控件，不再动态补这一行。新增 `data/reconstruction` 作为 SIM 重建 TIFF 默认输出目录，将根目录 `test_captures` 移到 `data/test_captures`，并用 `data/.gitignore` 保留目录结构但忽略运行时数据；`data/.gitignore` 与 `data/reconstruction/.gitkeep` 已加入 Git 索引。`ReconstructionConfig.output_path` 字段名保留但语义统一为输出目录，默认值改为 `data/reconstruction`，配置 schema 升到 v7，v6->v7 会把空值补成默认目录、把旧 `.tif/.tiff` 文件路径迁移为父目录。Recon 页输出标签改为 `Output Folder`，Browse 改为选择文件夹；`ReconstructionWorker` 写盘时在输出目录内生成 `sim_reconstruction_YYYYMMDD_HHMMSS.tif`，且相对输出目录固定按项目根目录解析，避免受启动 cwd 影响。独立 subagent review 发现的目录未跟踪和相对路径解析问题已修复。验证：聚焦 pytest 通过（143 项、185 个 subtests），`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（243 项），`.venv\Scripts\python.exe -m pytest tests\ -q` 通过（246 项、199 个 subtests），`.venv\Scripts\python.exe -m compileall -q sim_control reconstruction control_wangbo tests app.py` 通过，`git diff --check` 仅有既有 CRLF/LF 规范化 warning。需真机/人工确认：默认输出目录权限、真实重建连续触发时同秒文件名覆盖风险是否可接受、全幅重建写盘耗时。
- 2026-05-22：按用户计划拆分 SIM9 正式采集完成后的 raw 数据通道与 GUI 状态通道：`SimAcquisitionWorker` / `SimAcquisitionController` 新增 `signal_acquisition_summary_ready(dict)`，summary 只包含 `task_id`、`stack_shape`、`stack_dtype` 和 `metadata`；原 `signal_acquisition_ready(AcquisitionBatch)` 保留为 raw `(9, H, W) uint16` 栈通道，继续直连 `ReconstructionWorker.slot_reconstruct` 和后续保存/分析 worker。独立 SIM GUI 与集成主界面改为只订阅 summary 更新状态，不再把 raw batch 存入 GUI 状态，也不再取 `stack[0]` 做主界面预览缓存；controller 的 stop_event 清理改挂 summary，避免清理回调接收 raw stack。新增/更新 controller、独立 GUI、集成主界面回归测试覆盖 summary payload、raw batch 仍进重建路径、GUI 不接触 stack。验证：`.venv\Scripts\python.exe -m pytest tests\test_sim_camera_adapter.py tests\test_ui_regressions.py tests\test_sim_preview_restart.py tests\test_efficiency_optimizations.py -q` 通过（144 项、185 个 subtests），`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（239 项），`.venv\Scripts\python.exe -m pytest tests\ -q` 通过（242 项、195 个 subtests），`.venv\Scripts\python.exe -m compileall -q sim_control reconstruction control_wangbo tests app.py` 通过。需真机确认：真实 Hamamatsu + R11 + USB-6423 采集完成后 summary 与 raw 重建信号的实际时序、全幅 stack 内存释放表现和重建启动延迟。
- 2026-05-22：修复集成主 GUI `SIM Runtime` 中 `Reconstruction` 前状态圆点视觉尺寸小于 `Camera/SLM/DAQ` 的问题。根因是前三个圆点来自 `.ui` 中 16x16 的 `QLabel` 背景圆，而运行时动态补充的 `Reconstruction` 使用 `LedIndicator`，该控件在 16x16 内缩 2px 绘制，视觉直径更小。已在 `control_wangbo/main.py` 新增 `create_sim_runtime_led()`，让动态 `Reconstruction` 圆点也使用同样的 16x16 `QLabel` + `border-radius: 8px` 样式；若未来没有 `.ui` 中的 SIM Runtime 组，fallback 动态组也使用同一 helper。新增 `test_reconstruction_runtime_led_matches_existing_led_visual_size` 锁定类型、最小/最大尺寸和样式一致。验证：`.venv\Scripts\python.exe -m pytest tests\test_sim_preview_restart.py tests\test_main_window_sim_footer.py -q` 通过（78 项、11 个 subtests），`.venv\Scripts\python.exe -m compileall -q control_wangbo tests` 通过。
- 2026-05-22：按用户要求微调 SIM 采集设置弹窗 `Recon` 页的 `Reconstruction Parameters` 模块：参数区由单列 4 行改为 2 行 x 2 组，左上为 `Wiener para`、右上为 `Illumination NA`、左下为 `Pixel Size`、右下为 `Ex Wavelength`；`Wiener para` 显示保留 1 位小数，`Illumination NA` 显示保留 2 位小数，配置读写逻辑、重建接口和 pipeline 未改。已从 `sim_control/sim_settings_dialog.ui` 重新生成 `sim_control/ui_sim_settings_dialog.py`，并更新 UI 回归测试锁定标签、小数位和左右两组不重叠。验证：`.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py -q` 通过（26 项、170 个 subtests），`.venv\Scripts\python.exe -m compileall -q sim_control tests` 通过。
- 2026-05-22：独立 subagent review 发现 Recon 页初始化时若 `selected_laser_nm` 为非默认 488 nm，构造期 Laser radio `toggled` 信号会在 OTF 输入框尚未填值前把当前波长 OTF 清空。已在 `SimSettingsDialog` 的配置回填阶段加入 `_populating_widgets` guard，使构造期联动不写回配置，同时保留用户手动在 Laser/Recon 页切换波长时保存当前 OTF 并同步 `selected_laser_nm` 的行为；新增 `test_sim_settings_dialog_keeps_initial_non_default_recon_otf_path` 覆盖 405/561/647 nm 初始 OTF 保留。最新验证：`.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py tests\test_config_validation.py tests\test_efficiency_optimizations.py tests\test_legacy_sim_config_migration.py tests\test_z_scan_config_migration.py -q` 通过（61 项、170 个 subtests），`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（236 项），`.venv\Scripts\python.exe -m pytest tests\ -q` 通过（239 项、192 个 subtests），`.venv\Scripts\python.exe -m compileall -q sim_control reconstruction control_wangbo tests app.py` 通过。
- 2026-05-22：按用户确认计划为 SIM 采集设置弹窗新增 `Recon` 页：页签顺序为 `Laser / DAQ / Z-Scan / Recon`，页面按 DAQ/Z-Scan 风格居中排版，包含 `Reconstruction Parameters`、`Calibration Files` 和 `Output` 三组；支持设置 Wiener、照明 NA、pixel size、与 Laser 页共用的波长、当前波长 OTF、background 和输出 TIFF 路径，路径项带 Browse 按钮。`ReconstructionConfig` 增加 `output_path`，默认 `enabled=true`，配置 schema 升到 v6，v5->v6 会补 `output_path` 并启用重建；`device/dtype/theta_ratio/recon_group_batch` 不暴露在 GUI，继续固定为 `cuda/single/(1,1,1)/1`。设置页保存时会跑整份 `validate_app_config`，因此默认启用重建但当前波长 OTF 为空会阻止保存。`ReconstructionWorker` 在 GPU Wiener 成功后若 `output_path` 非空，会在线程内保存 `float32` reconstruction TIFF，写盘失败发 `signal_reconstruction_failed` 且不回退占位均值。新增/更新 UI、配置迁移、控件同步、pipeline 写 TIFF 回归测试。验证：`.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py tests\test_config_validation.py tests\test_efficiency_optimizations.py tests\test_legacy_sim_config_migration.py tests\test_z_scan_config_migration.py -q` 通过（60 项、167 个 subtests），`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（235 项），`.venv\Scripts\python.exe -m pytest tests\ -q` 通过（238 项、189 个 subtests），`.venv\Scripts\python.exe -m compileall -q sim_control reconstruction control_wangbo tests app.py` 通过。需真机/人工确认：各波长真实 OTF/background、输出目录权限、全幅重建耗时/显存和重建质量。
- 2026-05-21：按用户合并计划继续修复 SIM 采集设置 GUI：保留 DAQ/Z-Scan combobox 当前 editable/read-only 居中显示方案，但 `_ComboLineEditPopupFilter` 现在在鼠标按下时记录 `combo.view().isVisible()`，释放时若下拉框按下前已展开则通过 `QTimer.singleShot(0, combo.hidePopup)` 收起，否则继续延迟 `showPopup()` 打开，从而实现点击中间文本区第一次展开、第二次收起的原生 toggle 行为。Z-Scan 页 `gridLayout_zscan_range_section` 与 `gridLayout_zscan_capture_section` 的固定控件列统一到 270 px，与 Starting Position 一致；三个模块 label 列仍为 120 px、label/control gap 仍为 24 px，Range 输入控件自身保持 200 px，Image Acquisition 的 exposure combo 仍不超过 200 px。同步重新生成 `sim_control/ui_sim_settings_dialog.py`，并更新 UI 回归测试覆盖 combobox show/hide toggle、实际 DAQ/Z-Scan combobox 文本区点击、三模块 label 右边界一致和 24 px 间距。验证：`.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py -q` 通过（23 项、126 个 subtests），`.venv\Scripts\python.exe -m pytest tests\test_sim_preview_restart.py tests\test_ui_regressions.py -q` 通过（99 项、134 个 subtests），`.venv\Scripts\python.exe -m compileall -q sim_control tests app.py` 通过，`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（230 项），全量 `.venv\Scripts\python.exe -m pytest tests\ -q` 通过（233 项、148 个 subtests）；`git diff --check -- sim_control\sim_settings_dialog.ui sim_control\ui_sim_settings_dialog.py sim_control\gui.py tests\test_ui_regressions.py PROJECT_MEMORY.md` 仅提示生成 UI 文件的 CRLF/LF 规范化 warning。
- 2026-05-21：按用户计划完成 SIM9 GPU Wiener 重建接入：新增 `ReconstructionConfig` 与 v5 配置迁移，默认配置加入 `reconstruction` 段；`reconstruction/sim_wiener.py` 从硬编码脚本改为安全 import 的内存接口 `reconstruct_sim9_stack()`，延迟导入 GPU 后端，输入校验 `(9, H, W)` `numpy.uint16` 与 OTF/background 路径，输出 `float32` 重建图和包含 `c6`、`angle6`、`R2_angles`、timing、device、dtype、OTF/background 路径的 metadata；未修改 `sim_wiener_gpu_emdapp_batchInGroup_batchBetGroup.py`、`emd.py`、`emd_fast_torch.py`。`ReconstructionWorker` 在启用重建时调用 GPU Wiener，失败发 `signal_reconstruction_failed` 且不回退占位均值，未启用时保留原占位均值；独立 SIM GUI 和集成主界面均保存 `sim_last_reconstruction_result`，集成主界面新增 Reconstruction 状态行，重建失败不再误标 Camera/SLM/DAQ 硬件失败；controller preflight 会在启用重建但缺当前波长 OTF 时阻断采集。暂不自动回传 `signal_isTarget(7, ...)` 或 release/sort。新增 wrapper 安全 import/错误输入/缺依赖/fake backend 成功路径、pipeline 成功/失败、配置迁移/校验和主界面重建状态回归测试。验证：`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（230 项），`.venv\Scripts\python.exe -m pytest tests\ -q` 通过（233 项、148 个 subtests），`.venv\Scripts\python.exe -m compileall -q reconstruction sim_control control_wangbo tests app.py` 通过；独立 subagent review 已执行，提出的主界面状态与 preflight 问题已修复。需真机/人工确认：安装匹配 `torch/scipy/CUDA`，配置各波长 OTF/background，验证全幅 `2304 x 2304` 显存、耗时、重建质量和后续目标细胞判据。
- 2026-05-21：根据用户最新反馈继续调整 SIM 采集设置 GUI 的 Z-Scan 页细节：`group_zscan_start_section` 和 `group_zscan_capture_section` 改为与 Range 一致的左右弹性空白 + 固定 label 列 + 固定控件列布局，label/control 目标间距为 24 px，label 的 layout item 显式保持 `Qt::AlignRight|Qt::AlignVCenter`；Starting Position 的控件列宽度按 `spin_zscan_start_um + Read Current Z` 组合保留 270 px，Image Acquisition 的控件列保留 checkbox 所需 240 px，`combo_zscan_exposure_preset` 自身仍不超过 200 px。`_ComboLineEditPopupFilter` 改为鼠标按下只设置焦点，释放后通过 `QTimer.singleShot(0, combo.showPopup)` 打开下拉栏，避免点击 combobox 文本区时 popup 被同一次 release 立即关闭。Z-Scan Display 静态名称改为 `起始 Z：`、`终止 Z：`、`总位移：`、`估算总用时：`，`formLayout_zscan_preview` 显式设置 `horizontalSpacing=12`，右侧 value label 改为 `Qt::AlignLeft|Qt::AlignVCenter`，保留四行显示和中英文字体规则。同步重新生成 `sim_control/ui_sim_settings_dialog.py` 并更新 UI 回归测试覆盖 start/range/capture 24 px 间距、combobox press/release 行为、Display 冒号/左对齐/12 px 间距。验证：`.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py -q` 通过（23 项、126 个 subtests），`.venv\Scripts\python.exe -m pytest tests\test_sim_preview_restart.py tests\test_ui_regressions.py -q` 通过（98 项、134 个 subtests），`.venv\Scripts\python.exe -m compileall -q sim_control tests app.py` 通过；当前全量 `.venv\Scripts\python.exe -m unittest discover -s tests -q` 与 `.venv\Scripts\python.exe -m pytest tests\ -q` 均剩余 3 个失败，失败集中在既有配置迁移测试仍期望 `config_version == 4`，而当前代码返回 `5`，不属于本次 GUI 修改范围。
- 2026-05-21：按用户计划修复 SIM 采集设置 GUI 的 DAQ/Z-Scan 交互与 Z-Scan 页显示：`configure_centered_combobox()` 为只读居中的 `lineEdit()` 安装私有 event filter，点击 combobox 中间文本区域也会调用 `showPopup()`，因此 DAQ 和 Z-Scan 页所有通过该 helper 配置的 combobox 不再只能点右侧箭头下拉；该 filter 现在按 combo 子对象查找并移除旧实例，避免重复调用 helper 时累积 event filter。Z-Scan Display 模块删除 `层数 × 步长` 第四行，保留起始 Z、终止 Z、总位移、估算总用时四行，表单整体水平/垂直居中，value 文本居中显示，静态中文 label 使用 Microsoft YaHei（微软雅黑）10pt 粗体，英文/数字 value 默认 Arial 10pt 粗体，并在运行时根据 value 文本是否包含中文动态切换字体；Range 模块改为左右弹性空白 + 固定 label 列 + 固定控件列，label/control gap 目标 24 px 且整体居中，三个 Range label 的 layout item 继续保持 `Qt::AlignRight|Qt::AlignVCenter`，右侧控件宽度保持 200 px。同步重新生成 `sim_control/ui_sim_settings_dialog.py`，更新 UI 回归测试覆盖 helper 幂等性、实际 DAQ/Z-Scan combobox 文本区点击、Display 四行/居中/字体、Range 24 px 居中间距，并更新 Z-Scan preview 旧测试以适配删除的 breakdown 行。验证：`.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py -q` 通过（23 项、118 个 subtests），`.venv\Scripts\python.exe -m pytest tests\test_sim_preview_restart.py tests\test_ui_regressions.py -q` 通过（98 项、126 个 subtests），`.venv\Scripts\python.exe -m compileall -q sim_control tests app.py` 通过，`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（218 项），全量 `.venv\Scripts\python.exe -m pytest tests\ -q` 通过（221 项、140 个 subtests）。
- 2026-05-21：根据用户反馈再次调整 SIM 采集设置 GUI 的 Z-Scan 页 Range 模块横向视觉效果：上一轮把 label/control 间距压到 8 px 后，整体表单块偏左；本轮改为对齐 `Image Acquisition` 模块的表单排布。`gridLayout_zscan_range_section` 保持两列结构，设置 `leftMargin=60`、`rightMargin=60`、`horizontalSpacing=56`、第二列最小宽度 240 px，三个 Range 输入控件自身仍固定 200 px；运行时测得 Range 三行 `label_left=61`、`field_left=239`、`gap=58`，与 Image Acquisition 的 `label_left=61`、`field_left=237`、`gap=56` 基本一致。同步重新生成 `sim_control/ui_sim_settings_dialog.py`，并将 UI 回归测试改为直接比较 Range 与 Image Acquisition 的 label/control 横向位置。验证：`.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py -q` 通过（18 项、103 个 subtests），`.venv\Scripts\python.exe -m pytest tests\test_sim_preview_restart.py tests\test_ui_regressions.py -q` 通过（93 项、111 个 subtests），`.venv\Scripts\python.exe -m compileall -q sim_control tests app.py` 通过，`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（213 项），全量 `.venv\Scripts\python.exe -m pytest tests\ -q` 通过（216 项、125 个 subtests）。
- 2026-05-21：继续优化 SIM 采集设置 GUI 的 Z-Scan 页 Range 模块横向排布：`gridLayout_zscan_range_section` 改为 label 列、输入列、右侧弹性空白列三列结构，设置 `horizontalSpacing=8`、右侧 `horizontalSpacer_zscan_range_right` 吸收多余空间，并将 `combo_zscan_direction`、`spin_zscan_step_um`、`spin_zscan_num_steps` 的最小宽度固定到 200 px，使左侧三个 label 与右侧控件的实际间距从约 178 px 缩小到 8 px，同时保持输入框宽度稳定。同步重新生成 `sim_control/ui_sim_settings_dialog.py`，并在 UI 回归测试中加入 Range label/control gap 断言。验证：`.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py -q` 通过（18 项、103 个 subtests），`.venv\Scripts\python.exe -m pytest tests\test_sim_preview_restart.py tests\test_ui_regressions.py -q` 通过（93 项、111 个 subtests），`.venv\Scripts\python.exe -m compileall -q sim_control tests app.py` 通过，`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（213 项），全量 `.venv\Scripts\python.exe -m pytest tests\ -q` 通过（216 项、125 个 subtests）。
- 2026-05-21：按用户要求继续细化 SIM 采集设置 GUI 的 Z-Scan 页：在 `sim_control/sim_settings_dialog.ui` 的 `gridLayout_zscan_range_section` 中，为 `label_zscan_direction`、`label_zscan_step_um`、`label_zscan_num_steps` 三个 range 标签的 layout item 显式设置 `Qt::AlignRight|Qt::AlignVCenter`，使标签不仅自身文本右对齐，也在 grid 单元内靠右；同时将 Z-Scan 页中含中文文案或动态中文选项的控件字体改为 Microsoft YaHei（微软雅黑）粗体，英文控件继续保持 Arial 粗体，并重新生成 `sim_control/ui_sim_settings_dialog.py`。同步调整 UI 回归测试，覆盖 range 标签 layout item alignment、Z-Scan 中英文分字体规则、当前紧凑几何约束以及用户已调短的 range/capture 字段宽度。根据独立 review 发现的运行时问题，进一步让 `configure_centered_combobox()` 在创建只读居中 `lineEdit()` 后同步 `combo.font()`，确保 Z-Scan combo 的可见文字也分别使用 Arial 粗体或 Microsoft YaHei 粗体；测试同步覆盖 `combo.lineEdit().font()`。验证：`.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py -q` 通过（18 项、100 个 subtests），`.venv\Scripts\python.exe -m pytest tests\test_sim_preview_restart.py tests\test_ui_regressions.py -q` 通过（93 项、108 个 subtests），`.venv\Scripts\python.exe -m compileall -q sim_control tests app.py` 通过，`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（213 项），全量 `.venv\Scripts\python.exe -m pytest tests\ -q` 通过（216 项、122 个 subtests）。
- 2026-05-21：按用户要求调整 SIM 采集设置 GUI 的 Z-Scan 页布局与字体：`group_zscan_start_section`、`group_zscan_range_section` 和 `group_zscan_capture_section` 改为与 DAQ 线路区域一致的 grid 表单风格，标签统一固定宽度并右对齐，字段控件限制宽度和高度以避免被拉伸；SIM 设置弹窗 `.ui` 中显式字体统一为 Arial 粗体，并在 `SimSettingsDialog` 运行时将所有 `QComboBox` 配置为只读居中显示。为避免只读 editable combo 使用 `setCurrentText()` 后出现 `currentIndex/currentData` 旧值，新增 index-based `set_combobox_current_text()` 并替换 DAQ 设备/线位选择路径；同时修正 Z-Scan 分组标题拼写为 `Image Acquisition`。同步重新生成 `sim_control/ui_sim_settings_dialog.py`，新增 UI 回归测试覆盖 Z-Scan 分组布局、Arial 粗体、combo 居中以及 centered combo 的 data/index 同步。验证：`.venv\Scripts\python.exe -m pytest tests\test_ui_regressions.py -q` 通过（18 项、97 个 subtests），`.venv\Scripts\python.exe -m pytest tests\test_sim_preview_restart.py tests\test_ui_regressions.py -q` 通过（93 项、105 个 subtests），`.venv\Scripts\python.exe -m compileall -q sim_control tests app.py` 通过，`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（213 项），全量 `.venv\Scripts\python.exe -m pytest tests\ -q` 通过（216 项、119 个 subtests）；`git diff --check -- sim_control\sim_settings_dialog.ui sim_control\ui_sim_settings_dialog.py sim_control\gui.py tests\test_ui_regressions.py PROJECT_MEMORY.md` 仅提示 `ui_sim_settings_dialog.py` 的既有 CRLF/LF 规范化 warning。
- 2026-05-21：修正 Z-Scan 参数单位与步数语义：SIM 设置弹窗 Z-Scan 页的层扫间隔改为 `Scan gap`，界面以 nm 显示和输入，保留内部配置字段 `step_um`（旧配置 `0.3 μm` 自动显示为 `300 nm`），spinbox 单步为 `10 nm`；`num_steps` 统一解释为 ZDrive 移动次数，因此 `10 moves × 300 nm` 预览为总位移 `3.000 μm` 并实际采集 `11` 层图像。`scan_positions()` 改为返回起始层加每次移动后的 `num_steps + 1` 个位置，Test B stack/focus curve 校验同步按图像层数处理；配置校验允许最小移动次数为 1。主 GUI 右侧 SIM 参数摘要的 Z-Scan 区块新增 `scan_gap_nm`、`scan_moves`、`image_layers`、`total_distance_um` 和 `estimated_scan_time_ms`，预计用时按 `(z-scan DAQ waveform duration + 25 ms ZDrive 小步移动估计) × image_layers` 计算；为避免操作者混淆，摘要不再显示内部字段 `step_um/num_steps`，Test B TIFF 文件名前缀也从 `steps` 改为 `moves`。验证：`.venv\Scripts\python.exe -m compileall -q sim_control control_wangbo tests app.py` 通过，`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（210 项），`.venv\Scripts\python.exe -m pytest tests\ -q` 通过（213 项、24 个 subtests）；`git diff --check` 仅报告既有 CRLF 规范化提示。
- 2026-05-20：完成 SIM 设置弹窗 Z-Scan 页的双栏重构与联动测试入口增强：左侧保留 Z-Scan 参数，右侧新增计算预览与“仅位移台移动 / 位移台 + 每层采图”两类测试；Test B 复用生产 z-scan 核心，保留 TIFF stack、报告 SML 最佳层和最佳 Z，并在失败时根据 `z_scan_stage_positioned` 状态只在确已移动后尽力回起始层，同时执行 DAQ 全低和相机清理；失败弹窗会提示 SLM 当前 RO 可能仍为 z-scan RO。正式 SIM9 前置 z-scan 失败或取消时会尝试恢复正式 SIM Running Order，恢复失败时发出 `running_order_restore_warning` 并把“SLM may still be on z-scan RO”提示带到主界面错误弹窗；取消路径若恢复正式 RO 失败会升级为硬件失败，避免吞掉风险提示。`run_z_scan()` 新增 `keep_captured_stack=False`，`ZScanResult` 新增 `captured_stack`，公开 `scan_positions()` 并新增 stage-positioned 状态回调；集成主界面 SIM 预览下方 FPS、Z 位置、Auto Contrast、max_gray 控件统一为 24 px 高、Y=486。随后修复 Z-Scan 页 `Read current Z` 按钮因 `QPushButton.clicked(bool)` 多传参导致的槽函数报错，并把左侧参数区拆成“起始位置 / 扫描范围 / 采集与取消”三段式垂直布局，使左栏空间被充分利用。验证：`.venv\Scripts\python.exe -m compileall -q sim_control control_wangbo tests app.py` 通过，`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（208 项），`.venv\Scripts\python.exe -m pytest tests\ -q` 通过（211 项、24 个 subtests）；`git diff --check` 仅报告既有 CRLF 规范化提示。
- 2026-05-20：在真机电脑上重新验证 Nikon Ti2 ZDrive 接入：确认 `Ti2_Mic_Driver.dll` 存在并可加载，`MIC_GetDeviceList()` 返回 `[11]`，`MIC_Open(0)` 成功且报告 Z stage，物理/逻辑范围均为 `0..10000 μm`，当前 Z 为 `2674.74 μm`。以当前 Z 为中心执行 `+0.3/0/-0.3/0 μm` 往返 12 次，全部 `retcode=0`，耗时 min/median/p95/max 为 `21.347/21.749/21.943/23.609 ms`，readback 最大误差 `0.0 μm`，最终回到 `2674.74 μm`。同时修复 `Ti2ZStageAdapter.get_z_ranges_um()` 对底层 `ZRange(lower_um/upper_um)` 字段的兼容问题，并新增回归测试；验证：`.venv\Scripts\python.exe -m unittest tests.test_stage_adapter -q` 通过（4 项），`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（183 项），`.venv\Scripts\python.exe -m pytest tests\ -q` 通过（186 项、22 个 subtests），`.venv\Scripts\python.exe -m compileall -q sim_control\stage_adapter.py tests\test_stage_adapter.py` 通过，`git diff --check` 仅保留既有 CRLF 提示。
- 2026-05-20：实现 SIM9 前置 Z-Scan 自动对焦链路：配置 schema 升级到 v4 并新增 `ZScanConfig`；新增 `stage_adapter.py`、`focus_metrics.py`、`z_scan_core.py`；controller/acquisition_core 在正式 SIM9 前可执行 Ti2 ZDrive 层扫、单帧 z-scan 采图、SML 选焦和恢复正式 RO；`NIDaqWaveformBuilder.build_z_scan()` 生成 `slm_enable` 先行、trigger/camera/488 同步、无 `slm_finish` 且末尾归零的单帧波形；SIM 设置弹窗新增 Z-Scan 页，集成主界面新增当前 Z 显示；`patterns/2d_3.5.repz11` 已通过 `tools/add_zscan_ro.py` 就地追加 `488_3.5_2d_zscan3p_{5,8,14,20}ms` RO 并生成 `patterns/2d_3.5.repz11.bak` 备份。验证：`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（181 项），`.venv\Scripts\python.exe -m pytest tests\ -q` 通过（184 项、22 个 subtests），`.venv\Scripts\python.exe -m compileall -q app.py sim_control tests z-scan control_wangbo tools` 通过；另用仿真 adapter 直接执行 z-scan + SIM9 得到 `(9, 512, 512) uint16` stack。需真机确认：Ti2 SDK DLL、ZDrive 移动、合并 repertoire 的 RO 枚举、示波器 TTL 时序、z-scan 后恢复正式 SIM9 RO 和 Stop/cancel 返回起点。
- 2026-05-20：推送 `dev` 前补齐待提交改动的验证修正：`z-scan/z_scan_timing.py` 的默认步长统一为 `0.3 μm`，与 `z-scan/README.md` 和 `test_z_scan_timing.py` 预期一致；`sim_control/sim_acquisition_app.py` 的模块注释移除 `control_wangbo` 字符串，避免独立 SIM 入口边界测试把说明文字误判为依赖集成主界面。验证：`.venv\Scripts\python.exe z-scan\test_z_scan_timing.py` 通过（10 项），`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（166 项），`.venv\Scripts\python.exe -m pytest tests\ -q` 通过（169 项，22 个 subtests），`.venv\Scripts\python.exe -m compileall -q app.py sim_control tests z-scan` 和 `git diff --check` 通过。
- 2026-05-19：完成项目手写 Python 文件全面教学式注释重写，配合 SIM 9 帧重建代码对接。范围：`sim_control/`（18 个文件，排除 `ui_sim_settings_dialog.py`）、`tests/`（16 个文件）、`app.py`、`z-scan/`（3 个文件：`z_scan_timing.py`、`ti2_sdk.py`、`test_z_scan_timing.py`），共 38 个文件，新增约 3100 行教学式中文注释（净 +3367 - 262 = +3105 行）。性质：仅重写模块/类/函数 docstring 与行内段落注释（包含"作用 / 协作关系 / 关键概念 / 维护要点"四节模块 docstring 和"# 1) ... # 2) ..."编号式逻辑段落注释），未改动任何可执行代码、接口签名、配置 schema 或硬件控制逻辑。验证：`.venv\Scripts\python.exe -m compileall -q app.py sim_control tests z-scan` 通过，`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（166 项），冒烟 `import app` 与 `import sim_control` 成功。需人工确认：注释中"协作"与"维护要点"为当前快照，后续硬件链路或 SDK 绑定改变时需同步刷新对应 docstring；`control_wangbo/`、`sim_control/ui_sim_settings_dialog.py`、`SDK/` 厂商样例与 `mvsdk.py` 按计划全部跳过未改。
- 2026-05-19：主界面 SIM 参数摘要在 `TIMING_READOUTTIME` 下方新增 `SIM9_ESTIMATED_TOTAL_TIME`，按 9 帧 DAQ 完整波形时长加固定 10 ms stack 整理开销估算，并随曝光时间和有效帧间隔更新；SIM 设置弹窗 DAQ 页 `SIM采集` 测试完成弹窗新增实际采集用时（从发起测试指令到 9 帧 stack 入内存，不含 TIFF 写盘）和 DAQ 完整播放时长。验证：`.venv\Scripts\python.exe -m unittest tests.test_sim_summary tests.test_sim_preview_restart -q` 通过（62 项），`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（166 项）；真机实际耗时仍需 Hamamatsu + R11 + USB-6423 联调确认。
- 2026-05-18：为 `SDK/Ti2-E/Installer_CD_V1.0.0.68/Ti2 SDK使用許諾契約書.pdf` 提取原始日文条款并生成中文参考译文交付物：`Ti2 SDK使用许可契约书_中文翻译.md`、`.html` 和可复制/可检索文本的 `.pdf`。验证：源 PDF 可抽取 3 页日文文本，中文 Markdown 覆盖第 1 条至第 12 条，最终中文 PDF 可抽取文本字符数为 2333、`selectable=True`、共 4 页；已按修改后审查机制由独立 subagent 复核，结论为无阻塞交付问题，仍需用户/法律层面确认译文措辞和 4 页重排版式是否可接受。
- 2026-05-13：在 `AGENTS.md` 中新增“修改后审查机制”：每次项目修改后必须使用独立 subagent review，并固定审查输出为“已满足标准及证据、未满足标准及原因、需要用户人工确认、真机确认或额外业务判断的事项”；若当前工具环境无法启动独立 subagent，主 agent 必须先向用户说明原因并请求确认，不得静默自审替代；同步将该长期规则写入 `PROJECT_MEMORY.md` 和决策日志。
- 2026-05-12：按 `/review` 结果清理手写 Python 中文结构注释中的模板化低价值 docstring，将 `封装...小段规则`、`执行本模块...`、`创建本模块...` 等泛化说明替换为具体职责说明或删除；保留模块说明、主要类/函数说明和关键流程块注释，不改变运行逻辑、接口或生成 UI 文件。验证：`.venv\Scripts\python.exe -m compileall -q app.py sim_control control_wangbo\main.py tests` 通过，`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（164 项测试）；当前 `.venv` 未安装 `pytest`。
- 2026-05-12：为项目手写 Python 文件补充中文结构注释：覆盖 `app.py`、`sim_control/` 手写模块、`tests/` 和 `control_wangbo/main.py`，增加模块说明、主要类/函数说明和关键流程块注释；未修改生成 UI 文件、`mvsdk.py`、SDK 厂商样例或运行逻辑。验证：`.venv\Scripts\python.exe -m compileall -q app.py sim_control control_wangbo\main.py tests` 通过，`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（164 项测试）；当前 `.venv` 未安装 `pytest`，因此 `pytest tests\ -q` 未运行。
- 2026-05-12：增强 `docs/project_file_annotations.html` 的 Python 代码选区解释：新增离线 `PYTHON_CODE_EXPLANATIONS` 预置解释索引，覆盖 SIM 主模块、集成主界面和历史相机/MCU 线程的关键函数、类和常量；右键解释优先展示教学式分节说明，未命中时回退到本地规则解释；仍不调用外部 AI、网络 API 或后端。
- 2026-05-12：审查并修正 `docs/project_file_annotations.html` 模块页渲染：模块“推荐阅读顺序”和“模块下所有文件”只展示本模块文件，跨模块依赖改列为“相关外部文件”，避免配置、脚本、硬件资源等模块误把外部分组文件显示为本组文件。
- 2026-05-12：增强 `docs/project_file_annotations.html` 静态项目注释页：改为左右栏独立滚动的离线阅读工具，新增模块总览、模块内文件关系和阅读顺序、文件完整内容查看、`.repz11` 按 ZIP 资源包解读方式，以及 Python 代码选区右键解释功能；仍不改变运行代码、配置 schema 或硬件控制逻辑。
- 2026-05-12：修正项目文件注释页中 `patterns/2d_3.5.repz11` 的阅读说明：该 R11 repertoire 资源应按 zip 容器处理，复制后改后缀为 `.zip` 并解压查看内部 `.rep`、`.seq11` 和 PNG 图案文件；不要把它作为普通二进制十六进制文件解释。
- 2026-05-12：修正 `docs/project_file_annotations.html` 注释页自身的版本文件说明条目，并让搜索索引覆盖右侧实际展示的阅读建议、协作关系和修改注意事项，避免新增文档入库后覆盖声明失真或搜索漏项。
- 2026-05-12：新增 `docs/project_file_annotations.html` 静态中文项目文件注释页，覆盖当前版本管理内项目文件，面向首次接触项目和代码基础较弱的读者解释每个文件的项目作用、阅读顺序、代码结构、协作关系和修改注意事项；该文档不改变运行代码、配置 schema 或硬件控制逻辑。
- 2026-05-11：将 OMC / oh-my-claudecode 生成的本地项目缓存目录 `.omc/` 加入 `.gitignore`，避免工具元数据污染 `git status` 或被误提交；该目录不属于远程源码内容。
- 2026-05-11：继续优化 SIM 采集设置弹窗 DAQ 页布局：在 `DAQ Wiring` 组内为设备行、线路矩阵、测试行和底部之间增加均衡的垂直弹性间隔，并将线路矩阵行距加大，避免空白集中在组底部；同步重新生成 `ui_sim_settings_dialog.py`，新增 UI 回归断言验证三段垂直空白分布和底部剩余空间。
- 2026-05-11：美化 SIM 采集设置弹窗布局：保留 `Laser` / `DAQ` 双页签和原生 PyQt5 外观，将标题区固定为紧凑高度，页签区域改为主体扩展，DAQ 页移除左右居中 spacer 并让 `DAQ Wiring` 区域铺开；放宽 DAQ 线路与测试目标下拉框宽度，并用底部弹性 spacer 避免内容被垂直分散。已从 `.ui` 重新生成 `ui_sim_settings_dialog.py`，并新增 UI 几何回归断言覆盖标题高度、页签起点、DAQ 组宽度和下拉框宽度。
- 2026-05-11：根据 review 发现的问题补齐 SIM Timing 默认配置修正：`config/sim_control_config.json` 中 `timing.inter_frame_gap_us` 已从 `10_000 us` 改为 `50_000 us`，与 `TimingConfig` 默认值、controller/worker 正式采集生效规则和主界面摘要显示保持一致。
- 2026-05-11：简化 SIM 设置弹窗 DAQ 页面：移除 `Timing` 模块中 `sample_rate_hz`、`edge_pulse_us`、`inter_frame_gap_us`、`slm_enable_guard_us` 四个可编辑控件，保留这些字段作为内部采集时序参数；主界面 SIM 参数摘要最底部新增只读 `Timing` 区块显示四项时序；`inter_frame_gap_us` 默认改为 `50_000 us`，并统一 controller/worker 正式采集路径为“仅当相机推荐 gap 小于 50ms 时才覆盖默认值”。新增/更新 UI、摘要和 controller 回归测试；验证中新增目标用例通过，相关非临时目录用例通过；全量 pytest 在当前 sandbox 中剩余 6 项 `tempfile.TemporaryDirectory()` 写入权限失败，非本次业务断言失败。
- 2026-05-11：整理 GUI 启动入口：根目录默认入口由 `sim_control_app.py` 改为 `app.py`，职责调整为启动 `control_wangbo/main.py` 的项目集成主界面；Windows 快速启动脚本由 `start_sim_control.cmd` 改为 `start.cmd`；原独立 SIM 采集窗口入口迁移到 `sim_control/sim_acquisition_app.py`，通过 `python -m sim_control.sim_acquisition_app --config config\sim_control_config.json` 单独启动；新增入口回归测试覆盖默认入口、启动脚本和独立 SIM 入口命名。
- 2026-05-11：建立当前仓库 Git 工作流：当前大改动开发线迁移到 `dev`，远程开发分支为 `origin/dev`；稳定主线保留为 `main` / `origin/main`；新增 `E:\Intelligent_SR\SIM_AutoSystem-stable` detached `origin/main` worktree 作为远程稳定版运行和真机对照目录；项目说明新增 `dev -> PR -> main -> stable worktree 更新` 的代码更新流程；旧 `codex/*` 和 `backup/*` 分支按用户确认清理。
- 2026-05-11：针对 SIM 主链路防卡死方案的 review 结论继续收敛实现：Hamamatsu DCAM 分片等待现在只对 `TIMEOUT` 继续短轮询，遇到非 timeout SDK 错误会立即抛 `HardwareError`，避免真实硬件错误被拖到总超时才暴露；`run_single_acquisition()` 改为先校验 9 帧 `uint16` stack 与 timestamps，再补发缺失 `frame_captured` 进度，避免无效采集结果造成 9 帧进度噪声；standalone SIM GUI 的 Prepare/Run 正式路径不再在 GUI 线程执行 `initialize_hardware()`、`apply_camera_config()` 或 Running Order 选择，而是通过 controller worker 的 `prepare_only`/正式采集 payload 后台完成；standalone GUI 会从 worker 的 `running_order_selected` 状态同步 `selected_running_order`；集成主界面在 preview 停止失败并阻止正式采集时会恢复原 live preview 请求状态。新增对应 DCAM 非 TIMEOUT、采集校验前帧事件、standalone worker preflight、worker RO 状态同步、controller prepare-only payload 与 preview-stop 状态恢复回归测试。验证：`.venv\Scripts\python.exe -m pytest tests\ -q` 通过（161 项测试，16 个子测试），`.venv\Scripts\python.exe -m unittest discover -s tests -q` 通过（161 项测试），`.venv\Scripts\python.exe -m compileall -q sim_control control_wangbo tests` 通过；真机 Hamamatsu + R11 + NI USB-6423 停止响应和示波器全低仍需硬件验收。
- 2026-05-11：执行 SIM 主链路采集闭环与防卡死修复：`run_single_acquisition()` 增加 `AcquisitionCancelled`、`stop_event` 传播和 9 帧 `uint16` 栈/timestamps 强校验，避免异常相机返回误报成功；`SimAcquisitionController.stop()` 改为只设置取消事件，不再从 GUI/调用线程直接操作 DAQ 或相机，硬件 `disarm` 与 `set_all_low` 统一由采集 worker 的 `finally` 路径执行；NI USB-6423 波形等待改为 `is_task_done()` 短轮询并在取消时 `task.stop()`；Hamamatsu DCAM frame wait 改为 50ms 分片等待并保留总 timeout 与 `captured X/9` 诊断；standalone SIM GUI 默认正式采集路径改为 Running Order；集成主界面正式采集 preflight 移入 worker，并在 preview 未确认停止时阻止采集。验证通过软件测试；真机 Hamamatsu + R11 + NI USB-6423 停止响应和示波器全低仍需硬件验收。
- 2026-05-09：按保持可运行前提的治理策略做低风险规范化修改：默认配置不再持久化本机绝对 `config_path`；手动预览 LUT 增加只读有限缓存并在全量 `uint16` 范围下避免不必要 clip 拷贝；controller payload 类测试改为断开内部 worker 后仅捕获发射 payload，降低普通测试误触发采集 worker/硬件路径的风险；新增 `.editorconfig` 与 `.gitattributes` 统一文本文件编码/换行约定，并把 README/项目说明中的失效机器路径改为相对路径或实际工作区说明。
- 2026-05-09：执行 SIM 实时预览与 FeatureWorker 效率优化：预览显示新增速度优先 helper，按显示窗口先裁剪 `uint16`、再 resize、再 LUT 映射为 `uint8`，集成主界面 SIM 预览改用该路径，保持 `latest-frame-wins` 与原始 `uint16` 数据链路不变；`FusionBtCameraAdapter.read_preview_frame()` 对 DCAM 已返回的 NumPy 帧使用 `np.asarray(..., dtype=np.uint16)` 避免额外复制；`FeatureWorker` 优先使用 OpenCV `meanStdDev` / `minMaxLoc` 计算强度特征，并保留 NumPy fallback。
- 2026-05-09：执行低风险运行效率优化：`NIDaqWaveformBuilder.build()` 新增 `include_role_matrix` 参数，默认保留完整 `role_matrix` 诊断行为；正式采集、`SIM采集测试` 和准备实验路径改为 `include_role_matrix=False`，直接构建 packed `uint32` 端口波形，避免为 8 路 TTL role 生成完整 `uint8` 矩阵。
- 2026-05-08：修复 `SIM采集测试` 使用真实 Hamamatsu DCAM 时可能误报 `DCAM frame wait failed: TIMEOUT` 的问题；`FusionBtCameraAdapter.read_frame_sequence()` 会先读取 DCAM transfer count 并消费已进入 buffer 的帧，只在没有新帧时等待 `FRAMEREADY` 事件，timeout 报错包含 `captured X/9`。
- 2026-05-08：修复 `SimSettingsDialog` DAQ 页 `Camera Trigger` / `SIM采集` 测试重复打开 Hamamatsu DCAM camera 0 导致 `FAILOPENCAMERA` 的问题；设置弹窗现在可接收并复用主窗口共享的 `camera_adapter`。
- 2026-05-07：统一 AI 工具项目说明入口：`AGENTS.md` 已重写为唯一主指令文件；`CLAUDE.md` 改为薄壳引用。后续任一 AI 工具修改项目说明时只改 `AGENTS.md`。
- 2026-05-07：按项目计划变更回退到 USB-6423-only 代码状态，并删除未跟踪的 PXIe-7857R / NI-RIO / LabVIEW rebuild 相关模块、文档和测试面；当前只保留 USB-6423 波形驱动的 SIM9 采集流程。
- 2026-04-26：实现 SLM Running Order 迁移；`SimSettingsDialog` 不再管理 SLM 连接或手动 9-pattern 加载；`control_wangbo` 主界面新增共享 SLM 连接控件，统一使用 `SimAcquisitionController.slm_adapter`。
- 2026-04-23：建立仓库内跨对话记忆机制，新增 `PROJECT_MEMORY.md` 与 `docs/project_memory/decision_log.md`；约定新对话先读取 `AGENTS.md`、`PROJECT_MEMORY.md` 和决策日志。
